---
phase: 05-level-design-overhaul
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scenes/player/player.tscn
  - scenes/shared/moving_platform.gd
  - scenes/shared/moving_platform.tscn
  - scenes/shared/damage_zone.gd
  - scenes/shared/damage_zone.tscn
  - scenes/shared/pushable_box.tscn
  - scenes/shared/timed_obstacle.gd
  - scenes/shared/timed_obstacle.tscn
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - Player sees the full level width (6400px) without camera getting stuck at edges
    - Moving platforms carry the player smoothly during transit without jitter
    - Damage zones on the ground inflict continuous HP loss while player stands on them
    - Pushable boxes can be shoved by the player and block enemy movement
    - Timed obstacles open/close on schedule, allowing passage windows and blocking when closed
  artifacts:
    - path: scenes/player/player.tscn
      provides: Camera2D child node attached to player for wide-level following
      min_lines: 5
    - path: scenes/shared/moving_platform.gd
      provides: AnimatableBody2D patrol using Tween for smooth, looped motion
      exports: ["move_distance", "move_speed", "move_axis"]
    - path: scenes/shared/moving_platform.tscn
      provides: Scene with AnimatableBody2D root, CollisionShape2D, and Visual (Polygon2D)
      contains: "AnimatableBody2D"
    - path: scenes/shared/damage_zone.gd
      provides: Area2D that deals repeated damage when player enters; stops on exit
      exports: ["damage_interval", "damage_amount"]
    - path: scenes/shared/damage_zone.tscn
      provides: Scene with Area2D root, collision shape, semi-transparent ColorRect visual
      contains: "Area2D"
    - path: scenes/shared/pushable_box.tscn
      provides: RigidBody2D physics object with locked rotation, mass=3, gravity_scale=3
      contains: "RigidBody2D"
    - path: scenes/shared/timed_obstacle.gd
      provides: StaticBody2D toggling collision on/off via timer; shows open/closed visuals
      exports: ["open_time", "closed_time", "start_open"]
    - path: scenes/shared/timed_obstacle.tscn
      provides: Scene with StaticBody2D root, two Polygon2D visuals (open + closed state)
      contains: "StaticBody2D"
  key_links:
    - from: scenes/player/player.tscn
      to: scenes/world1/fase1_rua.tscn
      via: Camera2D following player position
      pattern: "Camera2D.*enabled.*true"
    - from: scenes/shared/moving_platform.gd
      to: physics system
      via: AnimatableBody2D with sync_to_physics
      pattern: "sync_to_physics.*true"
    - from: scenes/shared/damage_zone.gd
      to: player.take_damage()
      via: Area2D body_entered signal
      pattern: "body_entered.connect"
    - from: scenes/shared/pushable_box.tscn
      to: player move_and_slide()
      via: RigidBody2D physics collision
      pattern: "RigidBody2D"
---

<objective>
Create shared environmental mechanics (Camera2D, moving platforms, damage zones, pushable boxes, timed obstacles) that will be reused across all 6 expanded phases.

Purpose: Establish reusable, configurable building blocks with clear export parameters so level designers can populate fases with these mechanics without scripting.

Output: 4 shared mechanic scenes (moving_platform, damage_zone, pushable_box, timed_obstacle) + Camera2D on player, all tested in isolation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-level-design-overhaul/05-CONTEXT.md
@.planning/phases/05-level-design-overhaul/05-RESEARCH.md
@.planning/phases/05-level-design-overhaul/05-PATTERNS.md

Reference codebase patterns:
@../../../scenes/world1/malandro.gd — Enemy patrol + physics pattern
@../../../scenes/world1/malandro.tscn — Enemy scene structure
@../../../scenes/world1/checkpoint.gd — Area2D activation pattern
@../../../scenes/world1/fase1_rua.tscn — Level scene expansion template
@../../../scenes/player/player.tscn — Player scene (to be modified)
@../../../autoloads/save_manager.gd — Checkpoint persistence (no changes)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Camera2D to player.tscn</name>
  <files>scenes/player/player.tscn</files>
  <read_first>
    - scenes/player/player.tscn (current player scene structure)
  </read_first>
  <action>
    Add a new Camera2D node as a child of the Player root (CharacterBody2D). Set properties: enabled=true, zoom=Vector2(1, 1), limit_left=0, limit_right=6400, limit_top=-500, limit_bottom=200, drag_horizontal_enabled=true, drag_horizontal_offset=0.1, position_smoothing_enabled=true, position_smoothing_speed=5.0. This ensures the camera follows the player smoothly across all 6400px expanded phases without leaving dead zones or showing out-of-bounds areas.
  </action>
  <verify>
    <automated>grep -A 5 "Camera2D" scenes/player/player.tscn | grep -c "enabled.*true"</automated>
  </verify>
  <acceptance_criteria>
    - Camera2D node exists as direct child of Player root
    - enabled=true, zoom=Vector2(1, 1)
    - limit_right=6400 (matches new phase width)
    - drag_horizontal_enabled=true for smooth tracking
    - position_smoothing_enabled=true, position_smoothing_speed=5.0
  </acceptance_criteria>
  <done>
    Camera2D node is configured and committed. Player will follow camera in all 6400px phases without out-of-bounds viewing.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create moving_platform.gd + moving_platform.tscn</name>
  <files>
    - scenes/shared/moving_platform.gd
    - scenes/shared/moving_platform.tscn
  </files>
  <read_first>
    - scenes/world1/malandro.gd (for GDScript idiom and export pattern)
    - scenes/world1/malandro.tscn (for scene structure)
  </read_first>
  <action>
    Create moving_platform.gd extending AnimatableBody2D with exports: move_distance (float, default 80.0), move_speed (float, default 40.0), move_axis (Vector2, default Vector2(1, 0) for horizontal). In _ready(), store _start_pos and call _start_tween(). In _start_tween(), create a looping Tween that oscillates between _start_pos and _start_pos + move_axis * move_distance, with duration = move_distance / move_speed. Use set_loops() to repeat indefinitely. In moving_platform.tscn, create AnimatableBody2D root with sync_to_physics=true, add CollisionShape2D (RectangleShape2D size 40×8), and Visual Polygon2D (light grey trapezoid or rectangle for visible platform). Do NOT use AnimationPlayer — physics requires Tween-based movement.
  </action>
  <verify>
    <automated>grep -c "create_tween" scenes/shared/moving_platform.gd && grep -c "AnimatableBody2D" scenes/shared/moving_platform.tscn</automated>
  </verify>
  <acceptance_criteria>
    - moving_platform.gd extends AnimatableBody2D
    - Exports: move_distance, move_speed, move_axis (all visible in Inspector)
    - _start_tween() creates looping Tween with set_loops()
    - moving_platform.tscn has AnimatableBody2D root with sync_to_physics=true
    - CollisionShape2D and Visual (Polygon2D) as children
    - Platform can be instantiated and dragged in the editor without errors
  </acceptance_criteria>
  <done>
    Moving platform scene created. When placed in a fase scene and exported with move_distance=80 and move_speed=40, the platform will oscillate horizontally at constant speed carrying the player along.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create damage_zone.gd + damage_zone.tscn</name>
  <files>
    - scenes/shared/damage_zone.gd
    - scenes/shared/damage_zone.tscn
  </files>
  <read_first>
    - scenes/world1/checkpoint.gd (for Area2D activation pattern)
    - scenes/test_movement/damage_trigger.gd (if it exists, for damage pattern; else use checkpoint as template)
  </read_first>
  <action>
    Create damage_zone.gd extending Area2D with exports: damage_interval (float, default 0.5), damage_amount (int, default 1). In _ready(), create a Timer node programmatically with wait_time=damage_interval, one_shot=false. Connect body_entered and body_exited to track when the player is inside. When the player enters, start the timer. When player exits, stop the timer. On timer timeout, if player is still valid and in the zone, call player.take_damage(global_position). In damage_zone.tscn, create Area2D root with monitoring=true and monitorable=false. Add CollisionShape2D (RectangleShape2D size 64×48 by default). Add a semi-transparent ColorRect visual (modulate.a=0.3, color for lama/mud brown or névoa purple). Layer behind platforms using z_index=-1 or similar.
  </action>
  <verify>
    <automated>grep -c "Timer.new()" scenes/shared/damage_zone.gd && grep -c "Area2D" scenes/shared/damage_zone.tscn && grep -c "take_damage" scenes/shared/damage_zone.gd</automated>
  </verify>
  <acceptance_criteria>
    - damage_zone.gd extends Area2D
    - Exports: damage_interval, damage_amount (Inspector visible)
    - Timer created in _ready() with correct configuration
    - body_entered/body_exited signals connected and tracked
    - take_damage() called repeatedly while player is in zone
    - Timer stops when player leaves
    - damage_zone.tscn has Area2D root, CollisionShape2D, semi-transparent ColorRect visual
  </acceptance_criteria>
  <done>
    Damage zone scene created. When placed in a fase with damage_interval=0.5, player takes 1 HP damage every 0.5 seconds while standing on it. Visual is semi-transparent so player can see what they're standing on.
  </done>
</task>

<task type="auto">
  <name>Task 4: Create pushable_box.tscn (no script needed)</name>
  <files>scenes/shared/pushable_box.tscn</files>
  <read_first>
    - scenes/world1/malandro.tscn (for scene structure reference)
  </read_first>
  <action>
    Create pushable_box.tscn with RigidBody2D root (physics body that player can push). Set mass=3.0, lock_rotation=true, gravity_scale=3.0, continuous_cd=true (to prevent tunneling through thin walls). Add CollisionShape2D child with RectangleShape2D (size 24×24 by default, representing a cargo box). Add Visual child as Polygon2D (brown/tan colored box shape, e.g., trapezoid or rectangle with slightly inset sides). Do NOT add script — pushing happens automatically via player.move_and_slide() physics collision with RigidBody2D. Ensure NOT in "enemies" group to avoid conflict with enemy detection.
  </action>
  <verify>
    <automated>grep -c "RigidBody2D" scenes/shared/pushable_box.tscn && grep -c "lock_rotation.*true" scenes/shared/pushable_box.tscn && grep -c "mass.*3" scenes/shared/pushable_box.tscn</automated>
  </verify>
  <acceptance_criteria>
    - pushable_box.tscn has RigidBody2D root
    - lock_rotation=true, mass=3.0, gravity_scale=3.0
    - continuous_cd=true for physics robustness
    - CollisionShape2D child with RectangleShape2D (24×24)
    - Visual child Polygon2D for box appearance
    - NOT in "enemies" group
    - No script attached (physics-only)
  </acceptance_criteria>
  <done>
    Pushable box scene created. When player walks into it, player.move_and_slide() imparts momentum and the box rolls/slides away. Can be positioned as obstacle or stepping stone in fase scenes.
  </done>
</task>

<task type="auto">
  <name>Task 5: Create timed_obstacle.gd + timed_obstacle.tscn</name>
  <files>
    - scenes/shared/timed_obstacle.gd
    - scenes/shared/timed_obstacle.tscn
  </files>
  <read_first>
    - scenes/world1/checkpoint.gd (for Area2D/collision toggle pattern reference)
  </read_first>
  <action>
    Create timed_obstacle.gd extending StaticBody2D with exports: open_time (float, default 1.5), closed_time (float, default 2.0), start_open (bool, default false). Track _is_open state. In _ready(), set _is_open=start_open, call _update_state() to sync visuals, then call _schedule_next() to start the cycle. In _schedule_next(), calculate wait time based on current state and use get_tree().create_timer(wait) to schedule the next toggle via _toggle() callback (CONNECT_ONE_SHOT). In _toggle(), flip _is_open, call _update_state(), and call _schedule_next() again. In _update_state(), use set_deferred("disabled", _is_open) on the CollisionShape2D to toggle collision (open=disabled, closed=enabled). Show/hide visual_closed and visual_open Polygon2D nodes to indicate state change. In timed_obstacle.tscn, create StaticBody2D root. Add CollisionShape2D child (RectangleShape2D, size 32×32 by default). Add two Polygon2D visual children: visual_closed (gate closed appearance, grey/solid) and visual_open (gap appearance, lighter/empty). Initially hide the non-start_open visual.
  </action>
  <verify>
    <automated>grep -c "create_timer" scenes/shared/timed_obstacle.gd && grep -c "set_deferred.*disabled" scenes/shared/timed_obstacle.gd && grep -c "visual_closed\|visual_open" scenes/shared/timed_obstacle.tscn</automated>
  </verify>
  <acceptance_criteria>
    - timed_obstacle.gd extends StaticBody2D
    - Exports: open_time, closed_time, start_open (Inspector visible)
    - _is_open state tracked correctly
    - _schedule_next() uses create_timer() with correct wait time
    - _toggle() flips state and reschedules
    - _update_state() uses set_deferred("disabled", _is_open) for safe collision toggle
    - Visuals show open/closed state (visual_closed and visual_open Polygon2D)
    - timed_obstacle.tscn has StaticBody2D root with collision + two visual alternatives
  </acceptance_criteria>
  <done>
    Timed obstacle scene created. When placed in a fase with open_time=1.5 and closed_time=2.0, the obstacle opens for 1.5 seconds (allowing passage), closes for 2.0 seconds (blocking), and repeats. Visuals clearly show open/closed state.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries in this plan. All mechanics are local/offline physics interactions.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| (none applicable) | — | — | — | Mechanics are physics-only, no network/input validation required |
</threat_model>

<verification>
After all tasks complete:
1. Open the project in Godot and verify player.tscn has Camera2D child node visible in scene tree.
2. Instantiate moving_platform.tscn in a test scene, set move_distance=100, move_speed=50, and observe smooth oscillation.
3. Instantiate damage_zone.tscn in a test scene, set damage_interval=0.5, walk into zone, verify repeated damage SFX/visual feedback.
4. Instantiate pushable_box.tscn, walk into it, verify it slides away without getting stuck.
5. Instantiate timed_obstacle.tscn, set open_time=2.0, closed_time=2.0, observe gate opening/closing on schedule.
</verification>

<success_criteria>
- Camera2D is active and follows the player across the full 6400px phase width
- Moving platforms smoothly oscillate without jitter or physics artifacts
- Damage zones inflict continuous damage without glitching when player enters/exits
- Pushable boxes push naturally and respond to player momentum without tunneling
- Timed obstacles open/close on the correct schedule and block/allow passage as expected
</success_criteria>

<output>
After completion, create `.planning/phases/05-level-design-overhaul/05-01-SUMMARY.md`
</output>
