---
phase: 05-level-design-overhaul
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - scenes/world1/fase1_rua.tscn
  - scenes/world1/fase1_rua.gd
  - scenes/world1/fase2_parque.tscn
  - scenes/world1/fase2_parque.gd
  - scenes/world1/fase3_restaurante.tscn
  - scenes/world1/fase3_restaurante.gd
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - Player enters fase1_rua at x=0 and can travel all the way to x=6400px
    - Checkpoint is at exactly x=3200px (halfway point) and saves correctly
    - Exit trigger is at x≈6355px and advances to the next fase
    - Each fase has 2+ distinct enemy compositions in different sections (0-2800px vs 3600-6200px)
    - At least one environmental mechanic (moving platform, damage zone, obstacle) is present per fase
    - Each World 1 fase has 2 ParallaxLayer nodes (far=0.2, mid=0.5) with building/pole silhouettes showing depth
  artifacts:
    - path: scenes/world1/fase1_rua.tscn
      provides: Level scene expanded from 1600px to 6400px with checkpoint repositioned and parallax layers
      width_pixels: 6400
      checkpoint_position: "Vector2(3200, 118)"
    - path: scenes/world1/fase1_rua.gd
      provides: Level script initializing camera limits and section-specific enemy/mechanic data
      exports: ["section_1_enemies", "section_2_enemies"]
    - path: scenes/world1/fase2_parque.tscn
      provides: Level scene expanded from 1600px to 6400px with parallax layers
      width_pixels: 6400
      checkpoint_position: "Vector2(3200, 118)"
    - path: scenes/world1/fase2_parque.gd
      provides: Level script for fase 2
      exports: ["section_1_enemies", "section_2_enemies"]
    - path: scenes/world1/fase3_restaurante.tscn
      provides: Level scene expanded from 1600px to 6400px with NPCs/boss entry and parallax
      width_pixels: 6400
      checkpoint_position: "Vector2(3200, 118)"
    - path: scenes/world1/fase3_restaurante.gd
      provides: Level script for fase 3
      exports: ["section_1_enemies", "section_2_enemies"]
  key_links:
    - from: scenes/world1/fase*.tscn
      to: Camera2D.limit_right
      via: fase _ready() script call
      pattern: "Camera2D.limit_right = 6400"
    - from: scenes/world1/fase*.tscn
      to: Background ColorRect
      via: offset_right property expanded
      pattern: "offset_right = 6400"
    - from: scenes/world1/fase*.tscn
      to: Floor StaticBody2D
      via: position and collision size extended
      pattern: "position = Vector2(3200, 168)" 
    - from: Checkpoint node
      to: SaveManager.CHECKPOINT_SCENES
      via: checkpoint_id matching dict key
      pattern: "mundo1_fase1_cp1"
    - from: ParallaxBackground
      to: Player Camera2D
      via: automatic scroll_offset tracking
      pattern: "ParallaxBackground.*ParallaxLayer"
---

<objective>
Rewrite all 3 World 1 fase scenes (fase1_rua, fase2_parque, fase3_restaurante) from 1600px to 6400px width with checkpoint repositioned to the midpoint (3200px), enemies redistributed into 2+ themed sections, environmental mechanics, and parallax background layers for visual depth.

Purpose: Create playable, expanded World 1 fases that showcase vertical progression, mechanic variety, and improved visual depth before moving to World 2 rewrite.

Output: 3 fully playable expanded fase scenes with enemies, checkpoints, mechanics, and parallax backgrounds; fase scripts for camera/scene setup.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-level-design-overhaul/05-CONTEXT.md
@.planning/phases/05-level-design-overhaul/05-RESEARCH.md
@.planning/phases/05-level-design-overhaul/05-PATTERNS.md

Reference implementation (fase1_rua before expansion):
@../../../scenes/world1/fase1_rua.tscn — Current 1600px layout to expand
@../../../scenes/world1/fase1_rua.gd — Current script to extend

Shared mechanics (created in Wave 1):
@../../../scenes/shared/moving_platform.tscn — For platform sections
@../../../scenes/shared/damage_zone.tscn — For hazard areas
@../../../scenes/shared/timed_obstacle.tscn — For gated passages

Enemy scenes:
@../../../scenes/world1/malandro.tscn — Standard 1-hit enemy
@../../../scenes/world1/checkpoint.tscn — Checkpoint scene (position only changes)
@../../../scenes/world1/prova_item.tscn — Collectible provas (optional, for variety)

SaveManager checkpoint dict (no changes):
@../../../autoloads/save_manager.gd — CHECKPOINT_SCENES already has all 3 keys
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand fase1_rua.tscn from 1600px to 6400px + reposition checkpoint</name>
  <files>scenes/world1/fase1_rua.tscn</files>
  <read_first>
    - scenes/world1/fase1_rua.tscn (current layout)
  </read_first>
  <action>
    Expand the Background ColorRect offset_right from 1600.0 to 6400.0. Expand the Floor StaticBody2D position from Vector2(800, 168) to Vector2(3200, 168), and its CollisionShape2D size from Vector2(1600, 16) to Vector2(6400, 16). Move WallRight from Vector2(1608, 90) to Vector2(6408, 90). Move ExitTrigger from Vector2(1555, 144) to Vector2(6355, 144). Move Checkpoint from its current position (≈Vector2(790, 118)) to Vector2(3200, 118) — this is the exact midpoint of the new 6400px width. Do NOT change checkpoint_id (remains "mundo1_fase1_cp1"). Keep WallLeft at Vector2(-8, 90). If a TileMapLayer exists for terrain, verify its collision bounds cover 0-6400px. Create a simple ColorRect "SectionDivider" at x=3000 with height 180, width 10, color Color(0.8, 0.8, 0.8, 0.15) to visually mark the section break at x=3200.
  </action>
  <verify>
    <automated>grep "offset_right = 6400" scenes/world1/fase1_rua.tscn && grep "position = Vector2(3200, 168)" scenes/world1/fase1_rua.tscn && grep "position = Vector2(3200, 118)" scenes/world1/fase1_rua.tscn</automated>
  </verify>
  <acceptance_criteria>
    - Background ColorRect expanded to offset_right=6400.0
    - Floor StaticBody2D position=Vector2(3200, 168)
    - Floor collision RectangleShape2D size=Vector2(6400, 16)
    - WallRight at Vector2(6408, 90)
    - ExitTrigger at Vector2(6355, 144)
    - Checkpoint at Vector2(3200, 118) with checkpoint_id="mundo1_fase1_cp1"
    - WallLeft unchanged at Vector2(-8, 90)
    - SectionDivider ColorRect visible at x≈3000 as visual marker
    - File saves without validation errors
  </acceptance_criteria>
  <done>
    fase1_rua.tscn is now 6400px wide with checkpoint at midpoint and exit repositioned. Layout is ready for enemy/mechanic population.
  </done>
</task>

<task type="auto">
  <name>Task 2: Populate fase1_rua with enemies and mechanics (Section 1 + Section 2)</name>
  <files>scenes/world1/fase1_rua.tscn</files>
  <read_first>
    - scenes/world1/fase1_rua.tscn (expanded layout from Task 1)
    - scenes/world1/malandro.tscn (enemy reference)
    - scenes/shared/moving_platform.tscn (mechanic reference)
  </read_first>
  <action>
    Instantiate enemies and mechanics into fase1_rua.tscn. Section 1 (x=0-2800px): Place 3-4 standard malandros (1-hit kill) at x positions 200, 600, 1200, 1800 on ground level. Add 1 moving_platform at x=1500, moving horizontally 80px at speed 40. Section 2 (after checkpoint at x=3200, going to x=6200): Place 4-5 enemies including mix of standard malandros and variants (2-hit resistant will be added later in Wave 4) at x positions 3600, 4100, 4600, 5100, 5600. Add 1 damage_zone (lama puddle, brown tint) at x=4200, modulate color to brownish, damage_interval=0.5. Add 1 timed_obstacle (portão automático) at x=5300, open_time=2.0, closed_time=2.0. Each enemy node should have position set explicitly and patrol_speed exported property visible. Instance all enemy/mechanic scenes using ExtResource references (add to load_steps header if needed).
  </action>
  <verify>
    <automated>grep -c "instance=ExtResource.*malandro" scenes/world1/fase1_rua.tscn && grep -c "instance=ExtResource.*moving_platform" scenes/world1/fase1_rua.tscn && grep -c "instance=ExtResource.*damage_zone" scenes/world1/fase1_rua.tscn && grep -c "instance=ExtResource.*timed_obstacle" scenes/world1/fase1_rua.tscn</automated>
  </verify>
  <acceptance_criteria>
    - Section 1 has 3-4 malandro instances at distinct x positions (0-2800px)
    - Section 2 has 4-5 enemy instances at distinct x positions (3600-6200px)
    - 1 moving_platform instance in Section 1
    - 1 damage_zone instance in Section 2 with brown tint
    - 1 timed_obstacle instance in Section 2
    - All instances have position explicitly set
    - ExtResource references are correct (load_steps updated if needed)
    - File saves and can be played without runtime errors
  </acceptance_criteria>
  <done>
    fase1_rua is now populated with enemies and mechanics across both sections. Level is playable from start to x=6355px (exit).
  </done>
</task>

<task type="auto">
  <name>Task 3: Add ParallaxBackground + 2 ParallaxLayers to all 3 World 1 fases</name>
  <files>
    - scenes/world1/fase1_rua.tscn
    - scenes/world1/fase2_parque.tscn
    - scenes/world1/fase3_restaurante.tscn
  </files>
  <read_first>
    - scenes/world1/fase1_rua.tscn (layout from Task 2)
    - RESEARCH.md section 7 (Parallax Background pattern)
  </read_first>
  <action>
    For all 3 World 1 fases (fase1_rua, fase2_parque, fase3_restaurante), add a ParallaxBackground node (z_index=-5, positioned before Floor so it renders behind all gameplay). Within ParallaxBackground, add two ParallaxLayer children. Layer 1 (far, motion_scale=Vector2(0.2, 0)): Create a ColorRect or Polygon2D visual showing silhouettes of distant prédios (buildings) or structures. Use dark grey/brown color (e.g., Color(0.15, 0.15, 0.15, 1.0)) offset_right=6400, offset_bottom=180 (covering full height). Layer 2 (mid, motion_scale=Vector2(0.5, 0)): Create thin Polygon2D elements representing postes de luz (light poles) spaced every ~150px across the 6400px width, or horizontal bars for windows. Use medium grey (Color(0.4, 0.4, 0.4, 1.0)). Both layers should have motion_mirroring=Vector2(6400, 0) to prevent seams. The parallax will create visual depth — far layer moves at 20% camera speed, mid layer at 50% camera speed. Test by moving the camera/player horizontally and observing the background shift.
  </action>
  <verify>
    <automated>grep -c "ParallaxBackground" scenes/world1/fase1_rua.tscn && grep -c "motion_scale = Vector2(0.2, 0)" scenes/world1/fase1_rua.tscn && grep -c "motion_scale = Vector2(0.5, 0)" scenes/world1/fase1_rua.tscn && grep -c "ParallaxBackground" scenes/world1/fase2_parque.tscn && grep -c "ParallaxBackground" scenes/world1/fase3_restaurante.tscn</automated>
  </verify>
  <acceptance_criteria>
    - ParallaxBackground node exists in all 3 fases at z_index=-5 (behind Floor)
    - Each fase has exactly 2 ParallaxLayer children
    - Layer 1: motion_scale=Vector2(0.2, 0), contains ColorRect or Polygon2D with building silhouettes
    - Layer 2: motion_scale=Vector2(0.5, 0), contains Polygon2D poles/windows or similar mid-ground elements
    - Both layers have motion_mirroring=Vector2(6400, 0) to prevent horizontal seams
    - Colors are distinct from main background (darker for far, medium for mid)
    - All 3 fases have this structure applied
    - File saves without errors
  </acceptance_criteria>
  <done>
    Parallax background layers added to all 3 World 1 fases. Visual depth created via two scrolling layers: distant buildings (0.2×) and mid-ground poles/windows (0.5×). Criterion #6 satisfied.
  </done>
</task>

<task type="auto">
  <name>Task 4: Create/update fase1_rua.gd, fase2_parque.gd, fase3_restaurante.gd scripts for camera setup</name>
  <files>
    - scenes/world1/fase1_rua.gd
    - scenes/world1/fase2_parque.gd
    - scenes/world1/fase3_restaurante.gd
  </files>
  <read_first>
    - scenes/world1/fase1_rua.gd (if exists, current implementation)
  </read_first>
  <action>
    Create or update all three World 1 fase scripts to extend Node2D. In _ready(), get the player via get_tree().get_first_node_in_group("player") or $Player if direct reference exists. Call player.$Camera2D.limit_right = 6400 to set per-scene camera limit (or verify player.tscn Camera2D has limit_right=6400 by default — if yes, this line is optional). Store references to all enemies and mechanics for potential future reset logic (optional, can be empty for now). Example structure for each:
    ```gdscript
    extends Node2D
    
    func _ready() -> void:
        var player = get_tree().get_first_node_in_group("player")
        if player and player.has_node("Camera2D"):
            player.$Camera2D.limit_right = 6400
    ```
    This ensures the camera limit matches the new fase width. Apply identical logic to fase2_parque.gd and fase3_restaurante.gd.
  </action>
  <verify>
    <automated>grep -c "Camera2D.limit_right = 6400" scenes/world1/fase1_rua.gd && grep -c "Camera2D.limit_right = 6400" scenes/world1/fase2_parque.gd && grep -c "Camera2D.limit_right = 6400" scenes/world1/fase3_restaurante.gd</automated>
  </verify>
  <acceptance_criteria>
    - All three .gd files extend Node2D
    - Each _ready() function sets Camera2D.limit_right = 6400
    - Scripts have no syntax errors
    - Files are committed to repo
  </acceptance_criteria>
  <done>
    All 3 World 1 fase scripts are in place with camera setup. Parallax and gameplay mechanics fully configured across World 1.
  </done>
</task>

<task type="auto">
  <name>Task 5: Expand fase2_parque and fase3_restaurante similarly (structure + population + parallax)</name>
  <files>
    - scenes/world1/fase2_parque.tscn
    - scenes/world1/fase2_parque.gd
    - scenes/world1/fase3_restaurante.tscn
    - scenes/world1/fase3_restaurante.gd
  </files>
  <read_first>
    - scenes/world1/fase2_parque.tscn (current layout)
    - scenes/world1/fase3_restaurante.tscn (current layout with boss setup)
  </read_first>
  <action>
    Repeat Tasks 1, 2, and 3 for fase2_parque and fase3_restaurante. For fase2_parque: Expand Background to offset_right=6400, Floor to Vector2(3200, 168) with size Vector2(6400, 16), walls repositioned, Checkpoint to Vector2(3200, 118), checkpoint_id="mundo1_fase2_cp1" (unchanged from original). Add section divider at x≈3000. Populate with 3-4 enemies in Section 1 (0-2800px) and 4-5 in Section 2 (3600-6200px). Add mechanics: 1 moving_platform in Section 1, 1 damage_zone in Section 2, 1 timed_obstacle in Section 2 (e.g., sprinkler gate instead of automated gate). Add ParallaxBackground with 2 layers. For fase3_restaurante: Expand Background to offset_right=6400, Floor to Vector2(3200, 168) with size Vector2(6400, 16), walls repositioned, Checkpoint to Vector2(3200, 118), checkpoint_id="mundo1_fase3_cp1". Add section divider. Populate Section 1 (0-2800px) with 3-4 enemies building to the boss entry. Section 2 (3600-6200px) should lead to the boss arena. Add mechanics: moving_platform in Section 1, damage_zone (indoor heat/fog hazard) in Section 2, timed_obstacle (restaurant kitchen door opening/closing). Boss arena (boss_pai.tscn instance) positioned at x≈5800-6000. Add ParallaxBackground with 2 parallax layers. Create their respective .gd scripts with camera setup.
  </action>
  <verify>
    <automated>grep "offset_right = 6400" scenes/world1/fase2_parque.tscn && grep "offset_right = 6400" scenes/world1/fase3_restaurante.tscn && grep "position = Vector2(3200, 118)" scenes/world1/fase2_parque.tscn && grep "position = Vector2(3200, 118)" scenes/world1/fase3_restaurante.tscn && grep -c "ParallaxBackground" scenes/world1/fase2_parque.tscn && grep -c "ParallaxBackground" scenes/world1/fase3_restaurante.tscn</automated>
  </verify>
  <acceptance_criteria>
    - Both fases expanded to offset_right=6400
    - Both have Floor at Vector2(3200, 168) with size Vector2(6400, 16)
    - Both have Checkpoint at Vector2(3200, 118) with correct IDs
    - fase2_parque: 3-4 enemies in Section 1, 4-5 in Section 2; 1 moving_platform, 1 damage_zone, 1 timed_obstacle; ParallaxBackground with 2 layers
    - fase3_restaurante: 3-4 enemies in Section 1, boss arena at x≈5800-6000; 1 moving_platform, 1 damage_zone, 1 timed_obstacle; ParallaxBackground with 2 layers
    - Both have .gd scripts with camera setup
  </acceptance_criteria>
  <done>
    All 3 World 1 fases are now 6400px wide with enemies, mechanics, parallax backgrounds, and camera setup. World 1 is fully expanded and playable.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries. All mechanics are deterministic physics and spawning logic.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| (none applicable) | — | — | — | Mechanics are local only, no network/user input manipulation |
</threat_model>

<verification>
After all tasks complete:
1. Open World 1 fase 1 in Godot: Player should spawn at x=0, camera should follow smoothly across parallax layers.
2. Walk to checkpoint at x≈3200, verify checkpoint activation and save.
3. Continue to exit at x≈6355, verify fase completion and transition.
4. Observe parallax background: far buildings move at 20% camera speed, mid poles/windows at 50%.
5. Repeat for fase2_parque and fase3_restaurante.
6. Verify all enemies spawn, patrol, and can be killed.
7. Verify all mechanics (moving platform, damage zone, timed obstacle) work as designed.
8. Run a full playthrough from fase1 start to boss arena to confirm progression flow with visual depth.
</verification>

<success_criteria>
- All 3 World 1 fases are 6400px wide with checkpoint at midpoint (x=3200)
- Each fase has 2+ distinct enemy compositions in sections
- All environmental mechanics (moving platform, damage zone, timed obstacle) are present and functional
- ParallaxBackground with 2 layers present in all 3 fases showing building/pole silhouettes
- Player can traverse from start to exit without getting stuck or out-of-bounds
- Camera follows smoothly with parallax depth creating visual richness
</success_criteria>

<output>
After completion, create `.planning/phases/05-level-design-overhaul/05-02-SUMMARY.md`
</output>
