---
phase: 05-level-design-overhaul
plan: 03
type: execute
wave: 3
depends_on: ["05-01", "05-02"]
files_modified:
  - scenes/world2/fase1_campus.tscn
  - scenes/world2/fase1_campus.gd
  - scenes/world2/fase2_atelie.tscn
  - scenes/world2/fase2_atelie.gd
  - scenes/world2/fase3_madrugada.tscn
  - scenes/world2/fase3_madrugada.gd
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - Player enters fase1_campus at x=0 and can travel to x=6400px
    - Checkpoint is positioned at exactly x=3200px in all 3 World 2 fases
    - Exit trigger is at x≈6355px in all 3 World 2 fases
    - Each fase has 2+ themed sections with distinct enemy compositions and/or mechanics
    - Environmental mechanics (moving platform, damage zone, pushable box, timed obstacle) are distributed across fases
    - Each World 2 fase has 2 ParallaxLayer nodes (far=0.2, mid=0.5) with library/workspace and table/corridor silhouettes showing depth
  artifacts:
    - path: scenes/world2/fase1_campus.tscn
      provides: Level scene expanded from 1600px to 6400px with checkpoint at 3200px and parallax layers
      width_pixels: 6400
      checkpoint_position: "Vector2(3200, 118)"
    - path: scenes/world2/fase1_campus.gd
      provides: Level script with camera setup for wide scene
      exports: []
    - path: scenes/world2/fase2_atelie.tscn
      provides: Level scene expanded with movable/environment mechanic focus and parallax
      width_pixels: 6400
      checkpoint_position: "Vector2(3200, 118)"
    - path: scenes/world2/fase2_atelie.gd
      provides: Level script with camera setup
      exports: []
    - path: scenes/world2/fase3_madrugada.tscn
      provides: Level scene expanded with timed/hazard mechanics emphasis and parallax
      width_pixels: 6400
      checkpoint_position: "Vector2(3200, 118)"
    - path: scenes/world2/fase3_madrugada.gd
      provides: Level script with camera setup and Professor entrance
      exports: []
  key_links:
    - from: scenes/world2/fase*.tscn
      to: Camera2D.limit_right
      via: fase _ready() script call
      pattern: "Camera2D.limit_right = 6400"
    - from: scenes/world2/fase*.tscn
      to: Checkpoint node
      via: position and checkpoint_id
      pattern: "Vector2(3200, 118).*mundo2_fase._cp1"
    - from: "Ateliê phase"
      to: "pushable objects"
      via: "artist workspace theme"
      pattern: "pushable_box.*atelie"
    - from: "Madrugada phase"
      to: "hazard zones"
      via: "midnight fog/cold theme"
      pattern: "damage_zone.*madrugada"
    - from: ParallaxBackground
      to: Player Camera2D
      via: automatic scroll_offset tracking
      pattern: "ParallaxBackground.*ParallaxLayer"
---

<objective>
Rewrite all 3 World 2 fase scenes (fase1_campus, fase2_atelie, fase3_madrugada) from 1600px to 6400px width with checkpoint at midpoint, enemies distributed across 2+ themed sections, environmental mechanics tailored to each fase's theme, and parallax background layers for visual depth.

Purpose: Complete the quality overhaul for both existing worlds before adding new enemy variants or the Moto boss. World 2 fases emphasize pushable objects (ateliê workspace) and hazard mechanics (madrugada cold night). Parallax adds visual richness to distinguish World 2 environments.

Output: 3 fully expanded World 2 fase scenes with thematic enemy/mechanic distribution and parallax backgrounds.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-level-design-overhaul/05-CONTEXT.md
@.planning/phases/05-level-design-overhaul/05-RESEARCH.md
@.planning/phases/05-level-design-overhaul/05-PATTERNS.md

Reference World 2 scenes (before expansion):
@../../../scenes/world2/fase1_campus.tscn — Current 1600px layout
@../../../scenes/world2/fase1_campus.gd — Current script
@../../../scenes/world2/fase2_atelie.tscn
@../../../scenes/world2/fase3_madrugada.tscn

Enemies for World 2:
@../../../scenes/world2/impressora_raivosa.tscn — Firing enemy (appears in campus/ateliê)
@../../../scenes/world1/malandro.tscn — Reusable standard enemy (cross-world)

Shared mechanics (Wave 1):
@../../../scenes/shared/moving_platform.tscn
@../../../scenes/shared/damage_zone.tscn
@../../../scenes/shared/pushable_box.tscn
@../../../scenes/shared/timed_obstacle.tscn

SaveManager checkpoint dict already has all 3 World 2 keys.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand fase1_campus.tscn from 1600px to 6400px + populate with enemies/mechanics + add parallax</name>
  <files>
    - scenes/world2/fase1_campus.tscn
    - scenes/world2/fase1_campus.gd
  </files>
  <read_first>
    - scenes/world2/fase1_campus.tscn (current layout)
    - scenes/world2/impressora_raivosa.tscn (for enemy reference)
  </read_first>
  <action>
    Expand Background to offset_right=6400, Floor to Vector2(3200, 168) with size Vector2(6400, 16), walls repositioned to x=6408 (right wall), ExitTrigger to x=6355. Checkpoint moves to Vector2(3200, 118), checkpoint_id="mundo2_fase1_cp1". Add visual section divider at x≈3000. Theme: University campus with classrooms, library areas. Section 1 (0-2800px): Mostly malandros (reuse from World 1) mixed with 1-2 impressora_raivosa (firing printers). Add 1 moving_platform in Section 1 at elevated area (platform height ≈70px, move_distance=60). Section 2 (3600-6200px): More impressora enemies and malandros. Add 1 damage_zone (academic stress fog, purple tint) at x=4500, damage_interval=0.5. Add 1 timed_obstacle (library door) at x=5500, open_time=2.0, closed_time=2.0. Create fase1_campus.gd with _ready() function setting Camera2D.limit_right=6400. Add ParallaxBackground with 2 ParallaxLayers (far layer with library shelves silhouettes at motion_scale 0.2, mid layer with study tables/corridors at motion_scale 0.5).
  </action>
  <verify>
    <automated>grep "offset_right = 6400" scenes/world2/fase1_campus.tscn && grep "checkpoint_id = \"mundo2_fase1_cp1\"" scenes/world2/fase1_campus.tscn && grep -c "instance=ExtResource.*impressora" scenes/world2/fase1_campus.tscn && grep -c "ParallaxBackground" scenes/world2/fase1_campus.tscn && grep -c "motion_scale = Vector2(0.2, 0)" scenes/world2/fase1_campus.tscn && grep -c "motion_scale = Vector2(0.5, 0)" scenes/world2/fase1_campus.tscn</automated>
  </verify>
  <acceptance_criteria>
    - Background offset_right=6400
    - Floor at Vector2(3200, 168), size Vector2(6400, 16)
    - Checkpoint at Vector2(3200, 118), checkpoint_id="mundo2_fase1_cp1"
    - Section 1 has malandros + impressora_raivosa instances (total 4-5 enemies)
    - Section 2 has mix of malandros + impressora (total 4-5 enemies)
    - 1 moving_platform in Section 1
    - 1 damage_zone (purple fog) in Section 2
    - 1 timed_obstacle (door) in Section 2
    - fase1_campus.gd script with camera setup present
    - ParallaxBackground with 2 ParallaxLayers (far=0.2, mid=0.5) showing library/table silhouettes
  </acceptance_criteria>
  <done>
    fase1_campus is expanded to 6400px with mixed enemy types (malandros + firing printers), mechanics, and parallax depth. Player navigates campus with platform, hazard, and visual depth variety.
  </done>
</task>

<task type="auto">
  <name>Task 2: Expand fase2_atelie.tscn from 1600px to 6400px + emphasize pushable objects + add parallax</name>
  <files>
    - scenes/world2/fase2_atelie.tscn
    - scenes/world2/fase2_atelie.gd
  </files>
  <read_first>
    - scenes/world2/fase2_atelie.tscn (current layout)
    - scenes/shared/pushable_box.tscn (mechanic reference)
  </read_first>
  <action>
    Expand Background to offset_right=6400, Floor to Vector2(3200, 168), walls repositioned, Checkpoint to Vector2(3200, 118), checkpoint_id="mundo2_fase2_cp1". Add section divider at x≈3000. Theme: Artist's ateliê (art studio) with maquettes and tools. Section 1 (0-2800px): Malandros + 1-2 impressora enemies on ground/platforms. Add 2 pushable_box instances (wooden crates, maquettes) at x=1000 and x=1800, positioned at ground level so player can use them as stepping stones or shields. Add 1 moving_platform at x=2200 (before checkpoint, for vertical challenge). Section 2 (3600-6200px): More malandros + impressora. Add 2-3 more pushable_box instances spread across (x=4000, x=5000, x=5800) to create a "puzzle" feel where player must navigate around movable obstacles. Add 1 timed_obstacle (easel/display gate) at x=5300, open_time=1.5, closed_time=2.5 (faster cycle than campus). No damage_zone here; pushable boxes are the environmental focus. Create fase2_atelie.gd with camera setup. Add ParallaxBackground with 2 ParallaxLayers (far layer with workshop shelves/frames at motion_scale 0.2, mid layer with art tables/easels at motion_scale 0.5).
  </action>
  <verify>
    <automated>grep "offset_right = 6400" scenes/world2/fase2_atelie.tscn && grep "checkpoint_id = \"mundo2_fase2_cp1\"" scenes/world2/fase2_atelie.tscn && grep -c "instance=ExtResource.*pushable_box" scenes/world2/fase2_atelie.tscn && grep -c "ParallaxBackground" scenes/world2/fase2_atelie.tscn && grep -c "motion_scale = Vector2(0.2, 0)" scenes/world2/fase2_atelie.tscn && grep -c "motion_scale = Vector2(0.5, 0)" scenes/world2/fase2_atelie.tscn</automated>
  </verify>
  <acceptance_criteria>
    - Background offset_right=6400
    - Floor at Vector2(3200, 168), size Vector2(6400, 16)
    - Checkpoint at Vector2(3200, 118), checkpoint_id="mundo2_fase2_cp1"
    - Section 1: malandros + impressora (3-4 enemies) + 2 pushable_box + 1 moving_platform
    - Section 2: malandros + impressora (3-4 enemies) + 2-3 pushable_box + 1 timed_obstacle
    - Pushable boxes positioned at ground level or near enemy groupings
    - fase2_atelie.gd script present
    - ParallaxBackground with 2 ParallexLayers (far=0.2, mid=0.5) showing workshop/table silhouettes
  </acceptance_criteria>
  <done>
    fase2_atelie is expanded with pushable objects as primary environmental mechanic, parallax depth, and camera setup. Ateliê theme uses movable maquettes, crates, and visual depth to create workshop atmosphere.
  </done>
</task>

<task type="auto">
  <name>Task 3: Expand fase3_madrugada.tscn from 1600px to 6400px + emphasize hazard zones + add parallax</name>
  <files>
    - scenes/world2/fase3_madrugada.tscn
    - scenes/world2/fase3_madrugada.gd
  </files>
  <read_first>
    - scenes/world2/fase3_madrugada.tscn (current layout with Professor boss setup)
  </read_first>
  <action>
    Expand Background to offset_right=6400, Floor to Vector2(3200, 168), walls repositioned, Checkpoint to Vector2(3200, 118), checkpoint_id="mundo2_fase3_cp1". Add section divider. Theme: Late-night ateliê (madrugada = midnight hour) with cold fog and hazardous conditions. Section 1 (0-2800px): Malandros + 1 impressora at ground/low platforms. Add 1 moving_platform at x=1500 (horizontal, moving_distance=100, move_speed=50 for slower, more precarious feel). Add 1 damage_zone (icy fog, light blue tint, color≈Color(0.3, 0.5, 1.0, 1.0)) at x=2000-2300, damage_interval=1.0 (slower than other damage zones to represent cold stun). Section 2 (3600-6200px): Build tension toward Professor boss. Place 3-4 enemies with increasing difficulty (malandros + impressora). Add 2 damage_zones (cold fog patches) at x=4000 and x=5000, both with blue tint and damage_interval=1.0. Add 2 timed_obstacles (automatic heater vents, opening/closing) at x=4600 and x=5700, open_time=2.0, closed_time=3.0 (longer closed period = more danger). Boss arena (professor_perpetuo.tscn, if exists) positioned at x≈5900-6000. Create fase3_madrugada.gd with camera setup and boss initialization. Add ParallaxBackground with 2 ParallaxLayers (far layer with workshop interior silhouettes at motion_scale 0.2, mid layer with tables/support structures at motion_scale 0.5, both with cool blue tint to suggest cold/midnight).
  </action>
  <verify>
    <automated>grep "offset_right = 6400" scenes/world2/fase3_madrugada.tscn && grep "checkpoint_id = \"mundo2_fase3_cp1\"" scenes/world2/fase3_madrugada.tscn && grep -c "instance=ExtResource.*damage_zone" scenes/world2/fase3_madrugada.tscn && grep -c "ParallaxBackground" scenes/world2/fase3_madrugada.tscn && grep -c "motion_scale = Vector2(0.2, 0)" scenes/world2/fase3_madrugada.tscn && grep -c "motion_scale = Vector2(0.5, 0)" scenes/world2/fase3_madrugada.tscn</automated>
  </verify>
  <acceptance_criteria>
    - Background offset_right=6400
    - Floor at Vector2(3200, 168), size Vector2(6400, 16)
    - Checkpoint at Vector2(3200, 118), checkpoint_id="mundo2_fase3_cp1"
    - Section 1: malandros + impressora (2-3 enemies) + 1 moving_platform + 1 damage_zone (blue tint)
    - Section 2: malandros + impressora (3-4 enemies) + 2 damage_zones (blue tint) + 2 timed_obstacles + boss arena
    - Damage zones have damage_interval=1.0 (slower tick than other fases)
    - Timed obstacles have longer closed periods (3.0s) to emphasize hazard
    - Boss arena positioned at x≈5900-6000
    - fase3_madrugada.gd script with camera setup and boss init present
    - ParallaxBackground with 2 ParallexLayers (far=0.2, mid=0.5) with cool blue tint showing interior/table silhouettes
  </acceptance_criteria>
  <done>
    fase3_madrugada is expanded with hazard zones, timed obstacles, and parallax depth with cold blue tint. Midnight atmosphere emphasizes slow, careful movement through danger zones toward the boss with visual richness.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries. All mechanics are local physics and spawning.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| (none applicable) | — | — | — | No external input or state manipulation |
</threat_model>

<verification>
After all tasks complete:
1. Open World 2 fase 1 (campus) in Godot: Player should navigate through mixed enemy types, platform, and parallax depth. Reach checkpoint at x≈3200.
2. Verify impressora_raivosa enemies appear and behave correctly. Observe parallax: library shelves move at 20%, study tables at 50%.
3. In fase 2 (ateliê): Walk around pushable boxes, verify they move with player collision. Observe workshop-themed parallax depth.
4. In fase 3 (madrugada): Enter damage zones, verify slow continuous damage (1 HP every 1.0s). Observe timed obstacles opening/closing. Check for cool blue parallax tint.
5. Progress through all 3 fases to exit trigger at x≈6355.
6. Play full World 1 + World 2 sequence from start to end to confirm progression flow, mechanic variety, and visual depth via parallax.
</verification>

<success_criteria>
- All 3 World 2 fases are 6400px wide with checkpoint at midpoint (x=3200)
- Each fase has 2+ distinct sections with enemy and mechanic variation
- fase1_campus showcases platform challenges with mixed enemy types and library-themed parallax
- fase2_atelie emphasizes pushable objects for navigation and puzzle-solving with workshop-themed parallax
- fase3_madrugada emphasizes hazard zones, timed obstacles, and boss buildup with cold-themed parallax
- Player can traverse from World 1 through World 2 without getting stuck or out-of-bounds
- Parallax backgrounds add visual depth and atmosphere to each World 2 fase
</success_criteria>

<output>
After completion, create `.planning/phases/05-level-design-overhaul/05-03-SUMMARY.md`
</output>
