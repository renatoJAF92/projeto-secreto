---
phase: 05-level-design-overhaul
plan: 04
type: execute
wave: 4
depends_on: ["05-01", "05-02", "05-03"]
files_modified:
  - scenes/world1/moto_dois_homens.gd
  - scenes/world1/moto_dois_homens.tscn
  - scenes/world1/malandro_resistente.gd
  - scenes/world1/malandro_resistente.tscn
  - scenes/world1/malandro_coraza.gd
  - scenes/world1/malandro_coraza.tscn
  - scenes/world1/fase1_rua.tscn
  - scenes/world1/fase2_parque.tscn
  - scenes/world1/fase3_restaurante.tscn
  - scenes/world2/fase1_campus.tscn
  - scenes/world2/fase2_atelie.tscn
  - scenes/world2/fase3_madrugada.tscn
autonomous: false
requirements: []
user_setup: []

must_haves:
  truths:
    - Moto com Dois Homens spawns in World 1 fases and moves at patrol speed until engaged
    - On first hit, passenger is knocked off and moto speeds up (phase 2)
    - On second hit (after passenger is gone), moto is killed and disappears
    - Malandro Resistente takes 2 hits to kill (first hit shows red tint + smaller bounce)
    - Malandro Coraza can ONLY be killed by dash; normal stomp bounces player but damages them instead
    - At least 1 moto instance placed in each World 1 fase (3 total)
    - At least 1 malandro_resistente instance placed in each World 1 and World 2 fase (6 total minimum)
    - At least 1 malandro_coraza instance placed in each World 2 fase and at least 1 in World 1 (4+ total minimum)
  artifacts:
    - path: scenes/world1/moto_dois_homens.gd
      provides: Two-phase enemy with passenger + pilot HP tracking
      exports: ["moto_speed_normal", "moto_speed_solo"]
    - path: scenes/world1/moto_dois_homens.tscn
      provides: CharacterBody2D with Moto body + Passenger + Pilot visuals (Polygon2D)
      contains: "Visual/Passenger"
    - path: scenes/world1/malandro_resistente.gd
      provides: Extended malandro with _hp=2 and red tint on first hit
      exports: []
    - path: scenes/world1/malandro_resistente.tscn
      provides: Enemy scene with red/orange color variant
      contains: "Polygon2D"
    - path: scenes/world1/malandro_coraza.gd
      provides: Extended malandro with dash-only kill + takes damage on normal stomp
      exports: []
    - path: scenes/world1/malandro_coraza.tscn
      provides: Enemy scene with blue/shield visual variant
      contains: "Polygon2D"
  key_links:
    - from: moto_dois_homens.gd
      to: take_hit()
      via: HP counter + phase state machine
      pattern: "_hp.*_phase.*passenger_visual"
    - from: malandro_resistente.gd
      to: first hit detection
      via: red tint flash on first stomp
      pattern: "modulate = Color(1, 0.3, 0.3)"
    - from: malandro_coraza.gd
      to: player._is_dashing
      via: dash-only kill check
      pattern: "body._is_dashing"
    - from: all enemy variants
      to: stomp_zone.body_entered signal
      via: identical signal connection
      pattern: "stomp_zone.body_entered.connect"
    - from: World 1 fases
      to: moto_dois_homens instances
      via: ExtResource instantiation
      pattern: "instance=ExtResource.*moto_dois_homens"
    - from: All 6 fases
      to: malandro_resistente instances
      via: ExtResource instantiation at x>3200px range per mundo
      pattern: "instance=ExtResource.*malandro_resistente"
    - from: World 2 fases + some World 1
      to: malandro_coraza instances
      via: ExtResource instantiation at Section 2 enemy groups
      pattern: "instance=ExtResource.*malandro_coraza"
---

<objective>
Create 3 new enemy variants (Moto com Dois Homens, Malandro Resistente, Malandro Coraza) with distinct mechanics, then integrate them into the expanded World 1 and World 2 fases created in Plans 02-03. These enemies extend existing patterns and add behavioral variety to the 6400px fases.

Purpose: Provide challenge progression and enemy diversity across all encounters. Moto is exclusive to World 1; Resistente and Coraza variants appear in both worlds as the player progresses. This plan makes Wave 4 self-contained by including both creation and integration tasks.

Output: 3 fully functional enemy scene + script pairs with distinct kill mechanics, plus instantiated placements in all 6 fases showing variety and challenge progression.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-level-design-overhaul/05-CONTEXT.md
@.planning/phases/05-level-design-overhaul/05-RESEARCH.md
@.planning/phases/05-level-design-overhaul/05-PATTERNS.md

Base enemy pattern:
@../../../scenes/world1/malandro.gd — Standard 1-hit enemy (stomp detection)
@../../../scenes/world1/malandro.tscn — Standard enemy scene structure

Variant pattern (multi-phase):
@../../../scenes/world2/impressora_raivosa.gd — Firing variant with _fire_timer state

Player mechanics:
@../../../scenes/player/player.gd — For _is_dashing field reference and jump_velocity

AudioManager:
@../../../autoloads/audio_manager.gd — For stomp SFX reference

Expanded World 1 fases (from Plan 02):
@../../../scenes/world1/fase1_rua.tscn — Expanded layout ready for variant placement
@../../../scenes/world1/fase2_parque.tscn
@../../../scenes/world1/fase3_restaurante.tscn

Expanded World 2 fases (from Plan 03):
@../../../scenes/world2/fase1_campus.tscn — Expanded layout ready for variant placement
@../../../scenes/world2/fase2_atelie.tscn
@../../../scenes/world2/fase3_madrugada.tscn
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create moto_dois_homens.gd with 2-phase HP state machine</name>
  <files>scenes/world1/moto_dois_homens.gd</files>
  <read_first>
    - scenes/world1/malandro.gd (base pattern: stomp_zone signal, die(), reset_to_origin())
    - scenes/world2/impressora_raivosa.gd (variant pattern: _fire_timer state, phase tracking)
    - scenes/player/player.gd (for jump_velocity reference)
  </read_first>
  <action>
    Create moto_dois_homens.gd extending CharacterBody2D with: exports moto_speed_normal (float, 80.0) and moto_speed_solo (float, 120.0). State variables: _hp (int, initialized to 2), _phase (int, 1 or 2 representing both or pilot-only), _is_dead (bool), _stomped_this_frame (bool), _origin (Vector2), _direction (float, 1.0), const GRAVITY (900.0). In _ready(), save _origin, add to "enemies" group, connect stomp_zone.body_entered to _on_stomp_zone_body_entered. In _physics_process(), skip if _is_dead. Apply gravity, set velocity.x = _direction * patrol_speed (use moto_speed_normal or moto_speed_solo depending on _phase). Call move_and_slide(). Check edge detection via is_on_wall() or RayCast2D; reverse _direction if needed. Call _update_visual() to show/hide passenger. In _on_stomp_zone_body_entered(), check if body is player and velocity.y >= 0.0. If not already _stomped_this_frame, set _stomped_this_frame=true, call take_hit(). In take_hit(), decrement _hp. If _hp <= 0, call die(). Else if _hp == 1, set _phase=2, hide $Visual/Passenger node, set patrol_speed=moto_speed_solo. In die(), set _is_dead=true, play death animation if exists, disable collision, call AudioManager.play_sfx("stomp"), bounce player with body.velocity.y = body.jump_velocity * 0.6, schedule queue_free after 0.3s. In reset_to_origin(), restore state. In _update_visual(), called every frame, optionally oscillate passenger position for idle animation.
  </action>
  <verify>
    <automated>grep -c "_hp.*_phase" scenes/world1/moto_dois_homens.gd && grep -c "moto_speed_solo" scenes/world1/moto_dois_homens.gd && grep -c "passenger_visual.hide()" scenes/world1/moto_dois_homens.gd</automated>
  </verify>
  <acceptance_criteria>
    - moto_dois_homens.gd extends CharacterBody2D
    - Exports: moto_speed_normal (80.0), moto_speed_solo (120.0)
    - _hp initialized to 2, _phase to 1
    - take_hit() decrements _hp, transitions to phase 2 at _hp==1
    - Passenger visual hidden on phase 2 transition
    - patrol_speed changes from moto_speed_normal to moto_speed_solo on phase 2
    - die() called when _hp <= 0
    - stomp_zone signal properly connected in _ready()
    - Gravity and patrol movement working
  </acceptance_criteria>
  <done>
    moto_dois_homens.gd script complete. Enemy has 2-phase behavior: both riders present (normal speed), then pilot-only (faster).
  </done>
</task>

<task type="auto">
  <name>Task 2: Create moto_dois_homens.tscn with multi-piece Polygon2D visuals</name>
  <files>scenes/world1/moto_dois_homens.tscn</files>
  <read_first>
    - scenes/world1/malandro.tscn (scene structure: CharacterBody2D, collision, stomp zone, visual)
    - scenes/world1/moto_dois_homens.gd (script to attach)
  </read_first>
  <action>
    Create moto_dois_homens.tscn as CharacterBody2D root with script ExtResource(moto_dois_homens.gd). Add CollisionShape2D child with CapsuleShape2D (radius 8, height 24 for motorcycle-sized body). Add StompZone Area2D at position (0, -20) with RectangleShape2D (size 32×4). Add BodyHitbox Area2D with RectangleShape2D (size 30×26). Add EdgeRayCast2D at position (16, 0) with target_position (0, 20). Add Visual Node2D grouping all Polygon2D pieces. Within Visual, create: MotoBody (trapezoid shape ~32×14, color dark grey), Wheel1 (octagon ~8×8, dark grey), Wheel2 (octagon ~8×8, dark grey), Pilot (Node2D grouping PilotBody + PilotHead, color brown/tan), Passenger (Node2D grouping PassengerBody + PassengerHead, color similar to pilot, INITIALLY HIDDEN via visible=false or custom _ready() call). Each body part should be a distinct Polygon2D with recognizable silhouette. Total visual should suggest a motorcycle with two riders on top.
  </action>
  <verify>
    <automated>grep -c "Visual/Passenger" scenes/world1/moto_dois_homens.tscn && grep -c "Polygon2D" scenes/world1/moto_dois_homens.tscn && grep -c "CharacterBody2D" scenes/world1/moto_dois_homens.tscn</automated>
  </verify>
  <acceptance_criteria>
    - moto_dois_homens.tscn has CharacterBody2D root with script attached
    - CollisionShape2D with CapsuleShape2D child
    - StompZone and BodyHitbox Area2D children with proper collision shapes
    - EdgeRayCast2D for patrol edge detection
    - Visual node with multi-part Polygon2D structure
    - Pilot and Passenger nodes as sub-groups within Visual
    - Passenger node initially visible but will be hidden on phase 2
    - File has no validation errors and can be instantiated
  </acceptance_criteria>
  <done>
    moto_dois_homens.tscn scene complete. Visual shows motorcycle with recognizable pilot and passenger shapes. Ready for placement in World 1 fases.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create malandro_resistente.gd extending malandro with 2-hit HP</name>
  <files>scenes/world1/malandro_resistente.gd</files>
  <read_first>
    - scenes/world1/malandro.gd (base pattern: _hp counter, die() function, stomp detection)
  </read_first>
  <action>
    Create malandro_resistente.gd extending CharacterBody2D (copy structure from malandro.gd). Initialize _hp=2 instead of implicit 1-hit. In _on_stomp_zone_body_entered(), check if body.is_in_group("player") and body.velocity.y >= 0.0 and not _stomped_this_frame. Set _stomped_this_frame=true. Decrement _hp. If _hp <= 0, call die() to kill enemy and bounce player normally. Else (_hp == 1 after first hit), set a visual red tint on the enemy: $Visual.modulate = Color(1.0, 0.3, 0.3) to show damage state. Bounce player with smaller velocity: body.velocity.y = body.jump_velocity * 0.4 (vs 0.6 for normal enemy — less bouncy). For body hitbox damage, check if _hp > 1 before calling body.take_damage() to match stomp behavior. All other behavior (patrol, gravity, reset, death) identical to malandro.gd.
  </action>
  <verify>
    <automated>grep -c "_hp = 2" scenes/world1/malandro_resistente.gd && grep -c "Color(1, 0.3, 0.3)" scenes/world1/malandro_resistente.gd && grep -c "jump_velocity.*0.4" scenes/world1/malandro_resistente.gd</automated>
  </verify>
  <acceptance_criteria>
    - malandro_resistente.gd extends CharacterBody2D
    - _hp initialized to 2
    - First stomp: HP becomes 1, red tint applied, player bounces with 0.4x velocity
    - Second stomp: HP becomes 0, enemy dies normally
    - die() function unmodified from malandro.gd pattern
    - Visual modulate shows damage state (red tint)
    - All patrol/physics logic identical to standard malandro
  </acceptance_criteria>
  <done>
    malandro_resistente.gd script complete. Enemy survives first hit with visual feedback, dies on second hit.
  </done>
</task>

<task type="auto">
  <name>Task 4: Create malandro_resistente.tscn scene (red/orange color variant)</name>
  <files>scenes/world1/malandro_resistente.tscn</files>
  <read_first>
    - scenes/world1/malandro.tscn (structure template)
    - scenes/world1/malandro_resistente.gd (script to attach)
  </read_first>
  <action>
    Create malandro_resistente.tscn as CharacterBody2D with ExtResource(malandro_resistente.gd) attached. Copy the full structure from malandro.tscn (CollisionShape2D, AnimatedSprite2D or Visual Polygon2D, StompZone, BodyHitbox, EdgeRayCast). The key difference: change the Visual Polygon2D base color to a red/orange hue (e.g., Color(1.0, 0.6, 0.2, 1.0)) instead of the standard bright red, so the enemy is visually distinct even before taking the first hit. This telegraphs to the player "this enemy is different" before engagement.
  </action>
  <verify>
    <automated>grep -c "malandro_resistente" scenes/world1/malandro_resistente.tscn && grep "color = Color" scenes/world1/malandro_resistente.tscn | grep -c "0.6.*0.2"</automated>
  </verify>
  <acceptance_criteria>
    - malandro_resistente.tscn has CharacterBody2D root with script attached
    - Full collision/stomp/body-hit zone structure identical to malandro.tscn
    - Visual Polygon2D has red/orange base color (e.g., Color(1.0, 0.6, 0.2))
    - Can be instantiated without errors
    - Script logic will red-tint further on first hit
  </acceptance_criteria>
  <done>
    malandro_resistente.tscn complete. Orange/red appearance signals durability to the player.
  </done>
</task>

<task type="auto">
  <name>Task 5: Create malandro_coraza.gd with dash-only kill mechanics</name>
  <files>scenes/world1/malandro_coraza.gd</files>
  <read_first>
    - scenes/world1/malandro.gd (base pattern)
    - scenes/player/player.gd (for _is_dashing field)
  </read_first>
  <action>
    Create malandro_coraza.gd extending CharacterBody2D. No _hp counter needed (single enemy instance, but with special kill condition). In _on_stomp_zone_body_entered(), check if body.is_in_group("player") and not _stomped_this_frame. Set _stomped_this_frame=true. Check if body._is_dashing is true. If true, the player killed the enemy via dash, so call die() and bounce player normally with body.velocity.y = body.jump_velocity * 0.6. If false (normal stomp), the player tried to stomp but enemy is armored: bounce player with SMALL velocity body.velocity.y = body.jump_velocity * 0.3 (barely bounces), AND call body.take_damage(global_position) to penalize the player for the incorrect kill method. All other behavior (patrol, gravity) identical to malandro.gd.
  </action>
  <verify>
    <automated>grep -c "body._is_dashing" scenes/world1/malandro_coraza.gd && grep -c "jump_velocity.*0.3" scenes/world1/malandro_coraza.gd && grep -c "take_damage" scenes/world1/malandro_coraza.gd</automated>
  </verify>
  <acceptance_criteria>
    - malandro_coraza.gd extends CharacterBody2D
    - Checks body._is_dashing on stomp
    - If dashing: enemy dies, normal bounce (0.6x)
    - If not dashing: tiny bounce (0.3x), player takes damage
    - Patrol/physics identical to malandro.gd
    - No _hp state needed (single-instance kill mechanic)
  </acceptance_criteria>
  <done>
    malandro_coraza.gd script complete. Enemy is only killed by dash, teaching player a new strategy.
  </done>
</task>

<task type="auto">
  <name>Task 6: Create malandro_coraza.tscn scene (blue/shield visual variant)</name>
  <files>scenes/world1/malandro_coraza.tscn</files>
  <read_first>
    - scenes/world1/malandro.tscn (structure template)
    - scenes/world1/malandro_coraza.gd (script to attach)
  </read_first>
  <action>
    Create malandro_coraza.tscn as CharacterBody2D with ExtResource(malandro_coraza.gd). Copy structure from malandro.tscn. Key differences: Visual Polygon2D base color is blue (e.g., Color(0.3, 0.5, 1.0, 1.0)) to signal armored/protected state. Optionally add a semi-transparent shield Polygon2D layer around the main body (ring or perimeter) to reinforce the "armored" visual. The blue tint is the primary signal that stomp won't work.
  </action>
  <verify>
    <automated>grep -c "malandro_coraza" scenes/world1/malandro_coraza.tscn && grep "color = Color" scenes/world1/malandro_coraza.tscn | grep -c "0.3.*0.5.*1"</automated>
  </verify>
  <acceptance_criteria>
    - malandro_coraza.tscn has CharacterBody2D root with script attached
    - Full collision/stomp/body-hit structure identical to malandro.tscn
    - Visual Polygon2D has blue base color (e.g., Color(0.3, 0.5, 1.0))
    - Optional shield overlay Polygon2D for extra visual clarity
    - Can be instantiated without errors
  </acceptance_criteria>
  <done>
    malandro_coraza.tscn complete. Blue coloring and optional shield telegraph dash-only kill mechanic to the player.
  </done>
</task>

<task type="auto">
  <name>Task 7: Integrate moto_dois_homens into World 1 fases (fase1_rua, fase2_parque, fase3_restaurante)</name>
  <files>
    - scenes/world1/fase1_rua.tscn
    - scenes/world1/fase2_parque.tscn
    - scenes/world1/fase3_restaurante.tscn
  </files>
  <read_first>
    - scenes/world1/fase1_rua.tscn (expanded layout from Plan 02)
    - scenes/world1/moto_dois_homens.tscn (enemy scene created in Task 2)
  </read_first>
  <action>
    For each World 1 fase, instantiate 1 moto_dois_homens instance into Section 1 (x=0-2800px range). Positioning guidance: fase1_rua place moto at x≈2400 y≈90 (ground level), fase2_parque at x≈2600, y≈90, fase3_restaurante at x≈2800 y≈90. Each moto should be added as a distinct node in the fase scene with ExtResource reference to moto_dois_homens.tscn. Verify each instance has position explicitly set. These moto placements add visual variety and challenge within Section 1 before the checkpoint, introducing the player to the 2-phase enemy mechanic early.
  </action>
  <verify>
    <automated>grep -c "instance=ExtResource.*moto_dois_homens" scenes/world1/fase1_rua.tscn && grep -c "instance=ExtResource.*moto_dois_homens" scenes/world1/fase2_parque.tscn && grep -c "instance=ExtResource.*moto_dois_homens" scenes/world1/fase3_restaurante.tscn</automated>
  </verify>
  <acceptance_criteria>
    - fase1_rua has exactly 1 moto_dois_homens instance at x≈2400
    - fase2_parque has exactly 1 moto_dois_homens instance at x≈2600
    - fase3_restaurante has exactly 1 moto_dois_homens instance at x≈2800
    - All instances positioned at ground level (y≈90)
    - All instances have ExtResource references correct
    - Files save without errors
  </acceptance_criteria>
  <done>
    Moto com Dois Homens instances placed in all 3 World 1 fases, Section 1 range. Criterion #2 satisfied: moto created and instantiated across World 1.
  </done>
</task>

<task type="auto">
  <name>Task 8: Integrate malandro_resistente and malandro_coraza into all 6 fases (Section 2 enemy groups)</name>
  <files>
    - scenes/world1/fase1_rua.tscn
    - scenes/world1/fase2_parque.tscn
    - scenes/world1/fase3_restaurante.tscn
    - scenes/world2/fase1_campus.tscn
    - scenes/world2/fase2_atelie.tscn
    - scenes/world2/fase3_madrugada.tscn
  </files>
  <read_first>
    - scenes/world1/fase1_rua.tscn (current Section 2 enemy group, x≈3600-6200)
    - scenes/world1/malandro_resistente.tscn (enemy scene from Task 4)
    - scenes/world1/malandro_coraza.tscn (enemy scene from Task 6)
  </read_first>
  <action>
    For all 6 fases, add variant enemy instances to Section 2 (x=3600-6200px range) among existing enemy placements. For each World 1 fase (1_rua, 2_parque, 3_restaurante): add 2-3 malandro_resistente instances at x positions > 3600, spacing them among standard enemies (e.g., x≈3800, x≈5200 per fase). These represent "tougher" enemies appearing mid-level. Add 1-2 malandro_coraza instances per World 1 fase (e.g., x≈4600 per fase) to introduce the "dash-only" mechanic in the second half. For each World 2 fase (1_campus, 2_atelie, 3_madrugada): add 2-3 malandro_resistente instances at x positions > 3600 (same x-range as World 1). Add 2-3 malandro_coraza instances per World 2 fase (more than World 1) to emphasize the dash mechanic as a learned player skill. All instances must have explicit position set via ExtResource references. World 2 should show variant density higher than World 1 to represent difficulty escalation.
  </action>
  <verify>
    <automated>grep -c "instance=ExtResource.*malandro_resistente" scenes/world1/fase1_rua.tscn && grep -c "instance=ExtResource.*malandro_resistente" scenes/world2/fase1_campus.tscn && grep -c "instance=ExtResource.*malandro_coraza" scenes/world2/fase1_campus.tscn && grep -c "instance=ExtResource.*malandro_coraza" scenes/world1/fase1_rua.tscn</automated>
  </verify>
  <acceptance_criteria>
    - World 1 fases: each has 2-3 malandro_resistente instances in Section 2 (x>3600)
    - World 1 fases: each has 1-2 malandro_coraza instances in Section 2
    - World 2 fases: each has 2-3 malandro_resistente instances in Section 2 (x>3600)
    - World 2 fases: each has 2-3 malandro_coraza instances in Section 2 (showing escalation)
    - All instances positioned explicitly via ExtResource
    - All 6 fases save without errors
    - Variant density clearly increases from World 1 to World 2 showing difficulty progression
  </acceptance_criteria>
  <done>
    Enemy variant instances placed across all 6 fases in Section 2. Criteria #3-4 satisfied: variants created and distributed. Challenge progression visible: World 1 introduces variants, World 2 emphasizes them as player skills mature.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Three new enemy variants created and integrated:
    1. Moto com Dois Homens — 2-phase vehicle with passenger + pilot (1 instance per World 1 fase)
    2. Malandro Resistente — 2-hit orange variant with red stun tint (2-3 instances per World 1 fase, 2-3 per World 2)
    3. Malandro Coraza — Dash-only killable blue variant (1-2 per World 1 fase, 2-3 per World 2 fase)
    All instances placed in expanded 6400px fases from Plans 02-03.
  </what-built>
  <how-to-verify>
    1. Open Godot and load the project.
    2. Load World 1 fase 1 (fase1_rua): 
       - Walk right and encounter moto_dois_homens around x≈2400.
       - Stomp it once, verify passenger disappears, moto speeds up noticeably.
       - Stomp a second time, verify moto dies.
    3. Continue to Section 2 (after checkpoint) and encounter malandro_resistente and malandro_coraza:
       - Stomp a resistente, verify it turns red and bounces with less height.
       - Stomp it again, verify it dies.
       - Try stomping a coraza normally, verify player bounces slightly and takes damage.
       - Dash into a coraza, verify it dies and player bounces normally.
    4. Repeat for World 1 fase 2 and 3 — same enemy types, verify all instances behave correctly.
    5. Load World 2 fase 1 (fase1_campus) and observe higher variant density in Section 2.
    6. Verify World 2 phase 3 (madrugada) with even more variants present before boss.
    7. Run a full playthrough from World 1 start to World 2 boss to confirm difficulty progression and all enemy variants working correctly.
  </how-to-verify>
  <resume-signal>Type "approved" when all new enemy variants and their placements behave as described. If any behavior is wrong or instances are missing, describe the issue and location.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries. All mechanics are local enemy behavior.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| (none applicable) | — | — | — | No external state or input validation |
</threat_model>

<verification>
After checkpoint:human-verify approval:
1. Verify all enemy variants spawn at correct positions across all 6 fases.
2. Verify moto 2-phase mechanic works: passenger drop on first hit, speed increase, death on second hit.
3. Verify resistente takes 2 hits with red feedback on first hit.
4. Verify coraza only dies to dash; normal stomp damages player instead.
5. Verify variant density increases from World 1 to World 2, showing difficulty progression.
6. Run a full playthrough of Worlds 1 and 2 to confirm all enemies appear, behave correctly, and provide engaging challenge.
</verification>

<success_criteria>
- Moto com Dois Homens functions with 2-phase behavior (passenger → pilot) in World 1 fases
- Malandro Resistente takes 2 hits with visual red feedback in all 6 fases
- Malandro Coraza can ONLY be killed by dash; normal stomp damages player instead in all 6 fases
- All three enemies patrol, edge-detect, and die on correct conditions
- 1 moto instance per World 1 fase (3 total)
- 2-3 resistente instances per World 1 fase (6-9 total World 1) + 2-3 per World 2 fase (6-9 total World 2)
- 1-2 coraza instances per World 1 fase (3-6 total World 1) + 2-3 per World 2 fase (6-9 total World 2)
- Player receives correct bounces and damage depending on kill method
- Checkpoint approval received before Wave 4 is considered complete
</success_criteria>

<output>
After checkpoint approval, create `.planning/phases/05-level-design-overhaul/05-04-SUMMARY.md`
</output>
