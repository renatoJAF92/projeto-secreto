---
phase: 03-mundo-1-osasco-vertical-slice-completo
plan: 04
type: execute
wave: 4
depends_on: [01, 03]
files_modified:
  - dialogic/timelines/boss_abertura.dtl
  - dialogic/timelines/boss_renato_entrada.dtl
  - dialogic/timelines/boss_vitoria.dtl
  - dialogic/timelines/renato_restaurante.dtl
  - scenes/world1/boss_pai.tscn
  - scenes/world1/boss_pai.gd
autonomous: true
requirements: [BOSS-01, NPC-01]
cross_plan_notes:
  - "renato_restaurante.dtl is AUTHORED here (Plan 04) but CONSUMED by Plan 03's renato_npc.gd (fase3 restaurant dialogue). Plan 03 guards the call with ResourceLoader.exists, so Plan 03 runs without it; Renato's FULL fase3 dialogue is only active once this Plan 04 has run. Forward-reference is intentional — both plans are in this phase, executed in wave order (03 wave 3, 04 wave 4), so by phase end the timeline exists and the dialogue is live."
must_haves:
  truths:
    - "Entering boss_pai with fewer than 2 provas shows a blocking line and returns to fase3"
    - "Each collected prova auto-presents on boss entry and raises the trust bar +20%"
    - "Correct dialogue choices raise trust +10%, wrong choices lower it -15%"
    - "Trust bar fill color steps red <20%, green 20-79%, gold 80-100%"
    - "Trust reaching 0% triggers boss game over and reloads the boss scene (provas preserved)"
    - "Trust reaching ~80% brings Renato into the scene; reaching 100% plays victory and exits"
    - "Direct physical contact never damages the player in the boss scene (purely narrative)"
    - "renato_restaurante.dtl (authored here) is the timeline Plan 03's renato_npc.gd loads for the fase3 date dialogue — full Renato fase3 dialogue is live only after this plan runs"
  artifacts:
    - path: "scenes/world1/boss_pai.gd"
      provides: "Boss controller: provas gate, trust mechanics, Dialogic integration, Renato entrance, win/lose"
      contains: "func add_trust"
    - path: "scenes/world1/boss_pai.tscn"
      provides: "Boss scene with BossHUD (layer 51), ProvaCard (52), GameOverFlash (99), Luis sprite, Renato entrance"
      min_lines: 30
    - path: "dialogic/timelines/boss_abertura.dtl"
      provides: "Boss opening dialogue with choices emitting choice_correct/choice_wrong signals"
      contains: "boss"
    - path: "dialogic/timelines/renato_restaurante.dtl"
      provides: "Fase3 restaurant date dialogue consumed by Plan 03 renato_npc.gd (forward-reference)"
      contains: "Renato"
  key_links:
    - from: "scenes/world1/boss_pai.gd"
      to: "SaveManager.current_save['provas_mundo1']"
      via: "defensive .get read to gate entry + present provas"
      pattern: "current_save\\.get\\(\"provas_mundo1\""
    - from: "scenes/world1/boss_pai.gd"
      to: "Dialogic.signal_event"
      via: "connect → match choice_correct/choice_wrong/renato_entrada"
      pattern: "Dialogic\\.signal_event\\.connect"
    - from: "scenes/world1/boss_pai.gd"
      to: "BossHUD TrustBarFill"
      via: "_update_hud sets fill width + step color"
      pattern: "TrustBarFill"
    - from: "dialogic/timelines/renato_restaurante.dtl"
      to: "scenes/world1/renato_npc.gd (Plan 03)"
      via: "Plan 03 Dialogic.start('renato_restaurante') guarded by ResourceLoader.exists"
      pattern: "renato_restaurante"
---

<objective>
Build the non-violent boss fight against Luis (O Pai Desconfiante): a trust-bar HUD overlaid on Dialogic dialogue, automatic presentation of collected provas, dialogue choices that raise/lower trust, Renato's entrance at ~80% as the final proof, victory at 100%, and game-over-with-reload at 0%. The boss is purely narrative — no physical damage (D-18). This plan also authors the fase3 restaurant dialogue timeline (renato_restaurante.dtl) that Plan 03's renato_npc.gd consumes.

Purpose: Implements BOSS-01 (dialogue boss with verifiable non-violent victory condition) and completes NPC-01 (Renato as the decisive proof). Honors D-13 (≥2 provas gate), D-16/D-17 (Luis trust mechanic +20 prova / +10 correct / -15 wrong), D-19 (HUD CanvasLayer 51 above Dialogic 50), D-20 (0% = game over → reload, provas kept), D-21 (100% = victory, Renato at 80%), D-22 (sala de estar). Uses RESEARCH Pattern 5 (Dialogic.signal_event) and mitigates A1 (if signal_event is unavailable, the fallback is Dialogic variables — note in SUMMARY).

Cross-plan note: `renato_restaurante.dtl` is authored in this plan (Task 1) but consumed by Plan 03's `renato_npc.gd`. Plan 03 (wave 3) runs BEFORE this plan (wave 4) and guards its `Dialogic.start("renato_restaurante")` with `ResourceLoader.exists`, so Plan 03 does not crash on a missing timeline. Renato's full fase3 dialogue becomes active once this plan has run — by phase end (both plans complete) the dialogue is live. This forward-reference is intentional and documented in `cross_plan_notes` + must_haves.

Output: 4 Dialogic timelines (boss_abertura, boss_renato_entrada, boss_vitoria, renato_restaurante) + boss_pai.tscn + boss_pai.gd.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-mundo-1-osasco-vertical-slice-completo/03-PATTERNS.md
@.planning/phases/03-mundo-1-osasco-vertical-slice-completo/03-UI-SPEC.md
@.planning/phases/03-mundo-1-osasco-vertical-slice-completo/03-RESEARCH.md

<interfaces>
<!-- Contracts from Plan 01, existing Dialogic setup, and UI-SPEC. -->

SaveManager (Plan 01): current_save["provas_mundo1"]: Array  # READ defensively: .get("provas_mundo1", [])
AudioManager (Plan 01): play_sfx(key) for "prova_apresentada", "dialogo_errado", "vitoria"; play_music(stream)
SceneTransition (existing): go_to(scene_path)  # for game-over reload + victory exit
Dialogic 2 Alpha 19 (existing): Dialogic.start(timeline), await Dialogic.timeline_ended, Dialogic.signal_event (argument:String),
  Dialogic.current_timeline; characters Natalia.dch + Renato.dch already exist (Plan 2).

renato_npc.gd (Plan 03 — CONSUMER of renato_restaurante.dtl): in fase3, on interact, calls
  Dialogic.start("renato_restaurante") guarded by ResourceLoader.exists. This plan AUTHORS that timeline (Task 1).

PATTERNS.md boss pattern (lines 299-340) — trust state machine:
  var _trust: float = 0.0 ; const TRUST_MAX := 100.0
  _start_boss_sequence(): provas = .get("provas_mundo1", []); for id in provas: await _show_prova_card(id); add_trust(20)
                          Dialogic.start("boss_abertura"); Dialogic.signal_event.connect(_on_dialogic_signal)
  _on_dialogic_signal(arg): match arg: "choice_correct": +10 / "choice_wrong": -15 / "renato_entrada": entrance
  add_trust(amount): _trust = clampf(_trust+amount, 0, 100); _update_hud(); if <=0 game_over elif >=100 victory

UI-SPEC Boss scene tree (lines 567-597) — node names & layers:
  BossHUD CanvasLayer layer=51 → HUDContainer → TrustLabel "CONFIANÇA" (x8,y6), TrustBarContainer NinePatchRect (200x8, x56,y4, border #0F3460),
    TrustBarFill ColorRect (width=200*pct, color #4CAF50), TrustPctLabel "0%" (x260,y6)
  ProvaCardLayer CanvasLayer layer=52 → ProvaCard NinePatchRect 80x64 (visible=false) → ProvaSprite TextureRect 32x32, ProvaNameLabel
  GameOverFlash CanvasLayer layer=99 → FlashRect ColorRect 320x180 #E53935 (alpha 0, visible false)
  LuisSprite AnimatedSprite2D 32x48 (x200,y90); RenatoEntrance Node2D (x270,y100, visible false) → RenatoSprite 32x32 #2980B9
  _update_hud(): fill width 200*(_trust/100); color red <20, green <80, gold(#D4A017) >=80; pct label int
</interfaces>

Copy (UI-SPEC Copywriting Contract, Portuguese): insufficient provas = `Preciso de mais provas antes de encarar o pai.`; HUD label `CONFIANÇA`; game over label `CONFIANÇA PERDIDA`; ProvaCard names `Foto`/`Carta`/`Presente`. CPUParticles2D ONLY for victory burst.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the 4 Dialogic timelines (boss_abertura, boss_renato_entrada, boss_vitoria, renato_restaurante)</name>
  <files>dialogic/timelines/boss_abertura.dtl, dialogic/timelines/boss_renato_entrada.dtl, dialogic/timelines/boss_vitoria.dtl, dialogic/timelines/renato_restaurante.dtl</files>
  <read_first>
    - dialogic/timelines/test_dialogue.dtl (existing timeline format reference — join/leave events, character lines, choices)
    - .planning/phases/02-infraestrutura/02-005-SUMMARY.md (Dialogic gotchas: join/leave required for portraits; dialogic_default_action; portrait .dch format)
    - dialogic/characters/Natalia.dch, dialogic/characters/Renato.dch (existing speakers to reference by name)
    - .planning/phases/03-mundo-1-osasco-vertical-slice-completo/03-RESEARCH.md (Pattern 5 + Pitfall 4 — Signal event placement after each choice branch)
    - .planning/phases/03-mundo-1-osasco-vertical-slice-completo/03-UI-SPEC.md (boss copy + flow; opening narrative is Plan 05, not here)
  </read_first>
  <action>
    Author four `.dtl` timelines in `dialogic/timelines/`, following the test_dialogue.dtl format (join Natalia left / join Renato right; leave all at end — these join/leave events are MANDATORY for portraits per 02-005-SUMMARY).
    1. `boss_abertura.dtl`: Luis (use Natalia/Renato characters plus a Luis speaker label — if no Luis.dch exists, use a narrator/Natalia line styled as Luis dialogue and note in SUMMARY that a Luis.dch is a Phase 12 polish item). Open with Luis's skepticism, then present at least 2 multiple-choice questions. After EACH choice branch, add a Dialogic "Signal" event with argument `"choice_correct"` (for the right answer) or `"choice_wrong"` (for wrong answers) — per RESEARCH Pitfall 4, the Signal event is what drives the trust bar; a plain choice does nothing. Near the end, add a Signal event `"renato_entrada"` to cue Renato's arrival.
    2. `boss_renato_entrada.dtl`: Renato enters and delivers commitment lines (join Renato right). Short — 3-5 lines.
    3. `boss_vitoria.dtl`: Luis relents and accepts Renato into the family (D-21). Conclusion lines, then leave all.
    4. `renato_restaurante.dtl`: the fase3 restaurant date dialogue between Natalia and Renato (referenced by Plan 03 renato_npc.gd — this is the forward-referenced timeline; authoring it here completes the link). 4-6 warm lines, join/leave events.
    All text in Portuguese, consistent with the narrative tone. Keep choices binary or ternary (one correct).
  </action>
  <verify>
    <automated>test -f dialogic/timelines/boss_abertura.dtl && test -f dialogic/timelines/boss_renato_entrada.dtl && test -f dialogic/timelines/boss_vitoria.dtl && test -f dialogic/timelines/renato_restaurante.dtl && grep -q 'choice_correct' dialogic/timelines/boss_abertura.dtl && grep -q 'choice_wrong' dialogic/timelines/boss_abertura.dtl && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - All four `.dtl` files exist in `dialogic/timelines/`
    - `boss_abertura.dtl` contains at least 2 choice points, each followed by a Signal event with argument `choice_correct` or `choice_wrong`
    - `boss_abertura.dtl` contains a Signal event with argument `renato_entrada`
    - Each timeline uses join/leave events for character portraits (per 02-005-SUMMARY requirement)
    - `renato_restaurante.dtl` exists (consumed by Plan 03 renato_npc.gd — this closes the forward-reference; Plan 03's guarded call now resolves to a real timeline)
    - Behavior: open each timeline in the Dialogic editor — no parse errors; F6 a scratch scene calling `Dialogic.start("boss_abertura")` advances through choices without crash
  </acceptance_criteria>
  <done>Four authored Dialogic timelines drive the boss flow with signal-emitting choices and Renato's arrival cue, plus the fase3 restaurant dialogue consumed by Plan 03.</done>
</task>

<task type="auto">
  <name>Task 2: Boss scene + controller (boss_pai.tscn + boss_pai.gd) with trust HUD, provas gate, Renato entrance, win/lose</name>
  <files>scenes/world1/boss_pai.gd, scenes/world1/boss_pai.tscn</files>
  <read_first>
    - scenes/test_dialogue/test_dialogue.gd (Dialogic.start + await timeline_ended + signal connect/disconnect pattern)
    - .planning/phases/03-mundo-1-osasco-vertical-slice-completo/03-PATTERNS.md (boss_pai.gd pattern lines 269-340 — trust state machine + signal handler)
    - .planning/phases/03-mundo-1-osasco-vertical-slice-completo/03-UI-SPEC.md (Boss scene tree lines 567-597; CanvasLayer ordering; trust bar update method lines 601-623; Sections 4/5/6 game over + victory + Renato entrance)
    - .planning/phases/03-mundo-1-osasco-vertical-slice-completo/03-RESEARCH.md (Pitfall 7 CanvasLayer ordering; A1 signal_event assumption + fallback)
    - autoloads/save_manager.gd (defensive .get read; current_save structure)
    - dialogic/timelines/boss_abertura.dtl (the timeline this controller starts — from Task 1)
  </read_first>
  <action>
    Create `scenes/world1/boss_pai.tscn` matching the UI-SPEC node tree (lines 567-597) EXACTLY for CanvasLayer ordering (Pitfall 7): root `BossPai` Node2D; TileMapLayer_bg (z -1) + TileMapLayer_fg (z 1) painted as a sala de estar using osasco_tileset.tres (placeholder); `LuisSprite` AnimatedSprite2D 32x48 placeholder (`#8B0000` darker) at x200 y90; `RenatoEntrance` Node2D (x270 y100, visible=false) → RenatoSprite Sprite2D 32x32 `#2980B9`; Player instance (player.tscn) at x60 y120; `ProvaCardLayer` CanvasLayer layer=52 → ProvaCard NinePatchRect 80x64 (visible=false) with ProvaSprite TextureRect 32x32 + ProvaNameLabel; `BossHUD` CanvasLayer layer=51 → HUDContainer Control → TrustLabel Label ("CONFIANÇA", x8 y6, `#E8E8F0`), TrustBarContainer NinePatchRect (200x8, x56 y4, border `#0F3460`, bg `#16213E`), TrustBarFill ColorRect (color `#4CAF50`), TrustPctLabel Label ("0%", x260 y6); `GameOverFlash` CanvasLayer layer=99 → FlashRect ColorRect 320x180 `#E53935` alpha 0 visible=false. Use unique-name access (`%TrustBarFill`, `%TrustPctLabel`, `%ProvaCard`, etc.) so the script reaches them via `%`.
    Create `scenes/world1/boss_pai.gd` as `extends Node2D` per PATTERNS.md lines 299-340. State: `var _trust: float = 0.0`, `const TRUST_MAX: float = 100.0`, `var _renato_entered: bool = false`. `@onready` refs to HUD nodes via `%`.
    `_ready()`: call `_start_boss_sequence()`. Also wire the boss scene so NO physical damage occurs (D-18) — do not place enemies/obstacles; the boss is dialogue-only.
    `_start_boss_sequence()`: `var provas: Array = SaveManager.current_save.get("provas_mundo1", [])` (DEFENSIVE). GATE (D-13): `if provas.size() < 2:` show blocking line `Preciso de mais provas antes de encarar o pai.` (via `Dialogic.start` of a short inline timeline OR a Label) then `SceneTransition.go_to("res://scenes/world1/fase3_restaurante.tscn")` and `return`. Otherwise `_trust = 0.0`, show HUD, then for each `prova_id in provas`: `await _show_prova_card(prova_id)`; `add_trust(20.0)`; `AudioManager.play_sfx("prova_apresentada")`. Then guard `if Dialogic.current_timeline != null: return`; `Dialogic.start("boss_abertura")`; `Dialogic.signal_event.connect(_on_dialogic_signal)`; `await Dialogic.timeline_ended`; `Dialogic.signal_event.disconnect(_on_dialogic_signal)`.
    `_on_dialogic_signal(argument: String)`: `match argument: "choice_correct": add_trust(10.0) ; "choice_wrong": add_trust(-15.0); AudioManager.play_sfx("dialogo_errado") ; "renato_entrada": _trigger_renato_entrance()`.
    `add_trust(amount)`: `_trust = clampf(_trust + amount, 0.0, TRUST_MAX)`; `_update_hud()`; `if _trust <= 0.0: _trigger_game_over()` `elif _trust >= TRUST_MAX: _trigger_victory()` `elif _trust >= 80.0 and not _renato_entered: _trigger_renato_entrance()`.
    `_update_hud()`: `%TrustBarFill.custom_minimum_size.x = 200.0 * (_trust / 100.0)`; `%TrustPctLabel.text = str(int(_trust)) + "%"`; step color: `< 20` → `Color("#E53935")`, `< 80` → `Color("#4CAF50")`, else `Color("#D4A017")` (set `%TrustBarFill.color`).
    `_show_prova_card(prova_id)`: set `%ProvaCard` visible, set the sprite/name (`Foto`/`Carta`/`Presente` mapping), `await get_tree().create_timer(1.5, true).timeout`, tween alpha 1→0 over 0.3s, hide. Return so the `await` in the sequence works.
    `_trigger_renato_entrance()`: `if _renato_entered: return`; `_renato_entered = true`; `$RenatoEntrance.visible = true`; tween x 290→270 over 0.5s; `Dialogic.start("boss_renato_entrada")`; `await Dialogic.timeline_ended`; `add_trust(20.0)` (brings to 100% per UI-SPEC 6b).
    `_trigger_game_over()` (D-20): set fill `#E53935`; shake the bar container (tween x offset); set TrustLabel text `CONFIANÇA PERDIDA` color `#E53935`; flash `%FlashRect` alpha 0→0.5→0; then `SceneTransition.go_to("res://scenes/world1/boss_pai.tscn")` — reloads; provas remain in SaveManager so the gate still passes.
    `_trigger_victory()` (D-21): set fill `#D4A017`; CPUParticles2D gold burst (add a `$VictoryParticles` CPUParticles2D node, 20 particles, `#D4A017` — NEVER GPUParticles2D); `AudioManager.play_sfx("vitoria")`; `Dialogic.start("boss_vitoria")`; `await Dialogic.timeline_ended`; white flash; `SceneTransition.go_to("res://scenes/world1/world1_end.tscn")` (world1_end is created in Plan 05; guard with ResourceLoader.exists and fall back to main_menu.tscn if absent, noting in SUMMARY).
    If `Dialogic.signal_event` proves unavailable in Alpha 19 (RESEARCH A1), fall back to Dialogic variables polled per the RESEARCH note and document the substitution in the SUMMARY.
  </action>
  <verify>
    <automated>grep -q 'func add_trust' scenes/world1/boss_pai.gd && grep -q 'current_save.get("provas_mundo1"' scenes/world1/boss_pai.gd && grep -q 'provas.size() < 2' scenes/world1/boss_pai.gd && grep -q 'Dialogic.signal_event.connect' scenes/world1/boss_pai.gd && grep -q 'clampf(_trust' scenes/world1/boss_pai.gd && grep -q 'layer = 51\|layer=51' scenes/world1/boss_pai.tscn && grep -vq 'GPUParticles2D' scenes/world1/boss_pai.tscn && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `boss_pai.gd` reads provas via `SaveManager.current_save.get("provas_mundo1", [])` (defensive)
    - Entry with `provas.size() < 2` shows the blocking line and returns to `fase3_restaurante.tscn` (no boss fight)
    - `add_trust` clamps with `clampf(_trust + amount, 0.0, TRUST_MAX)` and routes to game-over (≤0), victory (≥100), and Renato entrance (≥80 once)
    - `_update_hud` sets fill width proportionally and steps color red <20 / green <80 / gold ≥80
    - `boss_pai.gd` connects and later disconnects `Dialogic.signal_event`, matching `choice_correct`/`choice_wrong`/`renato_entrada`
    - `boss_pai.tscn` has BossHUD at CanvasLayer layer=51, ProvaCardLayer at 52, GameOverFlash at 99 (above Dialogic 50 per Pitfall 7); contains NO enemies/obstacles (D-18 no physical damage); contains NO GPUParticles2D
    - Behavior: F6 boss_pai.tscn with ≥2 provas in the save — provas auto-present (+20 each), bar updates with color steps; choosing wrong lowers trust; reaching 0 reloads scene; reaching 80 brings Renato; reaching 100 plays victory and exits. With <2 provas, it bounces back to fase3.
  </acceptance_criteria>
  <done>The boss fight is fully playable: provas gate entry, auto-present to raise trust, choices adjust trust with stepped HUD color, Renato arrives at ~80%, victory at 100% exits the world, and 0% reloads the boss with provas preserved — all non-violent.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SaveManager.provas_mundo1 → boss gate | Boss reads the persisted provas array to decide entry and starting trust |
| Dialogic timeline → boss script | Signal arguments from the timeline drive trust mutations |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-11 | Tampering | boss_pai.gd provas read | mitigate | Defensive `.get("provas_mundo1", [])` + `provas.size() < 2` gate prevents crash and unearned boss access |
| T-03-12 | Denial of Service | boss game-over reload loop | mitigate | Reload resets trust to 0 but preserves provas, so the ≥2 gate re-passes and the player is never soft-locked out of the boss (D-20) |
| T-03-13 | Spoofing | Dialogic.signal_event arguments | accept | Signals originate only from authored local timelines (no external/user input); arguments are trusted constants |
| T-03-14 | Denial of Service | boss victory exit target | mitigate | `ResourceLoader.exists` guard on world1_end.tscn with main_menu fallback prevents crash if Plan 05 hasn't created it yet |
</threat_model>

<verification>
- F6 boss_pai.tscn with a save containing ≥2 provas: provas auto-present, trust rises, color steps correct, Renato at 80%, victory at 100% exits.
- With <2 provas: blocking line + return to fase3.
- Wrong choice lowers trust + dialogo_errado SFX; trust 0 = game over flash + reload (provas kept).
- No physical damage possible in the boss scene (no enemies/obstacles present).
- HUD visible OVER Dialogic (layer 51 > 50). No GPUParticles2D.
- renato_restaurante.dtl authored here resolves Plan 03's guarded renato_npc.gd dialogue call (forward-reference closed).
</verification>

<success_criteria>
- Non-violent boss with verifiable victory (100%) and game-over (0%) conditions.
- Trust mechanic: +20 prova / +10 correct / -15 wrong, stepped HUD color.
- Renato entrance at ~80% as decisive proof; victory exits Mundo 1.
- Provas persist across boss reload.
- renato_restaurante.dtl exists so Plan 03's fase3 Renato dialogue is live by phase end.
</success_criteria>

<output>
After completion, create `.planning/phases/03-mundo-1-osasco-vertical-slice-completo/03-04-SUMMARY.md`
</output>
