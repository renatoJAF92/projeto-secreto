---
phase: 03-mundo-1-osasco-vertical-slice-completo
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - scenes/world1/malandro.gd
  - scenes/world1/malandro.tscn
  - scenes/world1/static_obstacle.tscn
  - scenes/world1/checkpoint.gd
  - scenes/world1/checkpoint.tscn
  - scenes/world1/prova_item.gd
  - scenes/world1/prova_item.tscn
autonomous: true
requirements: [WORLD-01, WORLD-03, BOSS-01]
must_haves:
  truths:
    - "A Malandro patrols horizontally and turns at walls/edges without falling into pits"
    - "Jumping on a Malandro (falling onto its top) kills it and bounces the player"
    - "Lateral contact with a Malandro or static obstacle calls the player's take_damage()"
    - "Walking into a Checkpoint saves the checkpoint_id and pulses the cartaz visual"
    - "Walking into a ProvaItem adds its id to provas_mundo1, saves, and removes the item"
  artifacts:
    - path: "scenes/world1/malandro.gd"
      provides: "Patrolling enemy with stomp-kill + lateral-damage + reset_to_origin"
      contains: "func reset_to_origin"
    - path: "scenes/world1/checkpoint.gd"
      provides: "Area2D checkpoint that calls SaveManager.set_checkpoint + pulse"
      contains: "SaveManager.set_checkpoint"
    - path: "scenes/world1/prova_item.gd"
      provides: "Area2D collectible that appends to provas_mundo1 + saves"
      contains: "provas_mundo1"
  key_links:
    - from: "scenes/world1/malandro.gd"
      to: "scenes/player/player.gd take_damage()"
      via: "lateral body_entered → body.take_damage(global_position)"
      pattern: "take_damage\\("
    - from: "scenes/world1/prova_item.gd"
      to: "SaveManager.current_save['provas_mundo1']"
      via: "append id + SaveManager.save_game()"
      pattern: "SaveManager\\.save_game\\(\\)"
    - from: "scenes/world1/checkpoint.gd"
      to: "AudioManager.play_sfx"
      via: "play 'checkpoint' sfx on activation"
      pattern: "AudioManager\\.play_sfx\\(\"checkpoint\"\\)"
---

<objective>
Build the four reusable world1 game objects that the playable phases assemble from: the Malandro patrolling enemy (stomp-kill + lateral damage + reset), the StaticObstacle hazard, the Checkpoint (McFly cartaz with activation pulse + save), and the ProvaItem collectible (persists to provas_mundo1 on contact).

Purpose: These objects are instanced repeatedly across fase1/fase2/fase3 (Plan 03). Defining them as standalone reusable scenes first (interface-first ordering) means Plan 03 only places instances — no per-scene logic duplication. Implements D-04 (Mario stomp), D-05 (lateral knockback), D-06 (two enemy types), D-08/D-11 (checkpoint visual + id format), D-12/D-15 (prova collectibles + persistence).

Output: malandro.tscn/.gd, static_obstacle.tscn, checkpoint.tscn/.gd, prova_item.tscn/.gd — all in scenes/world1/.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-mundo-1-osasco-vertical-slice-completo/03-PATTERNS.md
@.planning/phases/03-mundo-1-osasco-vertical-slice-completo/03-UI-SPEC.md

<interfaces>
<!-- Contracts these objects depend on (from Plan 01 + existing code). -->

player.gd public surface (existing):
  func take_damage(hit_from_position: Vector2) -> void   # applies knockback + flash + hit-stop
  @export var jump_velocity: float = -380.0              # used to compute stomp bounce
  velocity: Vector2                                       # read body.velocity.y > 0 for stomp
Player node is in group "player" (added in Plan 01).

SaveManager (existing + Plan 01):
  func set_checkpoint(checkpoint_id: String) -> void     # sets current_save["checkpoint_id"] + saves
  func save_game() -> void
  current_save: Dictionary  # has "provas_mundo1": [] (Plan 01)
  # ALWAYS read with .get("provas_mundo1", []) — never current_save["provas_mundo1"] direct

AudioManager (Plan 01): func play_sfx(key: String) -> void  # stub-safe, registered keys added in Plan 05

damage_trigger.gd analog (existing, exact Area2D body_entered pattern):
  extends Area2D
  func _ready(): body_entered.connect(_on_body_entered)
  func _on_body_entered(body): if body.has_method("take_damage"): body.take_damage(global_position)
</interfaces>

Palette tokens (UI-SPEC): laranja `#E07020`, vermelho quente `#C83030`, prompt yellow `#FFDD57`, grey inactive `#888888`. Malandro 16x32, obstacle 16x16, checkpoint 16x24, prova 16x16. CPUParticles2D ONLY (never GPUParticles2D).
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Malandro patrolling enemy (malandro.tscn + malandro.gd)</name>
  <files>scenes/world1/malandro.gd, scenes/world1/malandro.tscn</files>
  <read_first>
    - scenes/player/player.gd (analog: extends CharacterBody2D, @export_group, _physics_process order, _update_animation guard, create_timer(d, true) pattern at _start_hit_stop)
    - .planning/phases/03-mundo-1-osasco-vertical-slice-completo/03-PATTERNS.md (malandro.gd section lines 88-169 — full pattern with @onready, signal connect, set_deferred)
    - .planning/phases/03-mundo-1-osasco-vertical-slice-completo/03-RESEARCH.md (Pattern 1 stomp detection + Pitfall 3 set_deferred + Pitfall 5 edge RayCast)
    - .planning/phases/03-mundo-1-osasco-vertical-slice-completo/03-UI-SPEC.md (Section 3a Malandro: 16x32, #C83030 fill, walk/death anims, CapsuleShape2D radius 6 height 22)
  </read_first>
  <behavior>
    - Malandro moves horizontally at patrol_speed and flips _direction (and sprite.flip_h) when `is_on_wall()` OR when the front edge RayCast2D stops detecting floor
    - When the player falls onto the StompZone (player.velocity.y > 0 and is_in_group("player")), Malandro dies and the player bounces (body.velocity.y = body.jump_velocity * 0.6)
    - When the player touches the Malandro's body laterally (main hitbox, not stomp), the player's take_damage(global_position) is called once
    - reset_to_origin() returns the Malandro to its spawn position, re-enables collision, clears _is_dead, and restarts the walk animation
  </behavior>
  <action>
    Create `scenes/world1/malandro.gd` as `extends CharacterBody2D` per PATTERNS.md lines 88-169. Add `@export_group("Patrol")` with `@export var patrol_speed: float = 40.0`. Runtime state (explicit types): `var _origin: Vector2`, `var _direction: float = 1.0`, `var _is_dead: bool = false`, `const GRAVITY: float = 900.0`.
    `@onready var sprite: AnimatedSprite2D = $AnimatedSprite2D`, `@onready var stomp_zone: Area2D = $StompZone`, `@onready var edge_ray: RayCast2D = $EdgeRayCast`.
    In `_ready()`: `_origin = global_position`; add self to group "enemies" via `add_to_group("enemies")`; connect `stomp_zone.body_entered.connect(_on_stomp_zone_body_entered)`; connect the body hitbox lateral contact via a second Area2D `$BodyHitbox` (`body_entered` → `_on_body_hitbox_entered`).
    In `_physics_process(delta)`: guard `if _is_dead: return` first; apply `velocity.y += GRAVITY * delta`; `velocity.x = _direction * patrol_speed`; `move_and_slide()`; then turn logic — `if is_on_wall() or not edge_ray.is_colliding(): _direction *= -1.0; sprite.flip_h = _direction < 0.0` and flip the edge_ray position to the new front; call `_update_animation()`.
    `_on_stomp_zone_body_entered(body)`: `if body.is_in_group("player") and body.velocity.y > 0.0: die(); body.velocity.y = body.jump_velocity * 0.6`.
    `_on_body_hitbox_entered(body)`: `if body.is_in_group("player") and not _is_dead and body.velocity.y <= 0.0: body.take_damage(global_position)` (the velocity.y guard prevents lateral-damage firing on the same frame as a stomp).
    `die()`: `_is_dead = true`; `sprite.play("death")`; `$CollisionShape2D.set_deferred("disabled", true)`; `AudioManager.play_sfx("stomp")`; `get_tree().create_timer(0.3, true).timeout.connect(queue_free, CONNECT_ONE_SHOT)`.
    `reset_to_origin()`: `global_position = _origin`; `_is_dead = false`; `velocity = Vector2.ZERO`; `$CollisionShape2D.set_deferred("disabled", false)`; `sprite.play("walk")`.
    `_update_animation()`: guard `_is_dead` → "death" else "walk", with the `if sprite.sprite_frames and sprite.animation != new_anim` guard (prevents frame-0 freeze, per player.gd).
    Create `scenes/world1/malandro.tscn`: root CharacterBody2D (script attached), CollisionShape2D with CapsuleShape2D radius=6 height=22, AnimatedSprite2D with a 16x32 `#C83030` placeholder rectangle (2-frame walk + 1-frame `#888888` flattened death — geometric placeholder acceptable per UI-SPEC), StompZone Area2D (RectangleShape2D 16x4 at the top, y offset ~-16), BodyHitbox Area2D (RectangleShape2D ~14x28 covering the body below the stomp zone), EdgeRayCast RayCast2D pointing down (target_position Vector2(0, 20)) offset to the front (x=+10). Set both Area2D monitoring=true. Place the root in group "enemies" via the .tscn groups attribute too.
  </action>
  <verify>
    <automated>grep -q 'func reset_to_origin' scenes/world1/malandro.gd && grep -q 'set_deferred("disabled", true)' scenes/world1/malandro.gd && grep -q 'body.velocity.y > 0' scenes/world1/malandro.gd && grep -vq 'GPUParticles2D' scenes/world1/malandro.tscn && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `scenes/world1/malandro.gd` contains `extends CharacterBody2D`, `func reset_to_origin`, `func die`
    - die() uses `$CollisionShape2D.set_deferred("disabled", true)` (never direct `.disabled =`)
    - die() uses `get_tree().create_timer(0.3, true)` (the `true` process_always arg is present)
    - Stomp branch checks `body.velocity.y > 0.0` AND `body.is_in_group("player")`
    - Lateral branch calls `body.take_damage(global_position)`
    - `scenes/world1/malandro.tscn` contains NO `GPUParticles2D` node
    - Behavior: place a Malandro + Player in a scratch test, F6 — malandro patrols and flips at a wall; player jumping on top kills it and bounces; lateral touch flashes/knockbacks the player
  </acceptance_criteria>
  <done>Malandro patrols, turns at walls/edges, dies+bounces on stomp, damages player on lateral contact, and resets cleanly via reset_to_origin().</done>
</task>

<task type="auto">
  <name>Task 2: StaticObstacle hazard (static_obstacle.tscn)</name>
  <files>scenes/world1/static_obstacle.tscn, scenes/world1/static_obstacle.gd</files>
  <read_first>
    - scenes/test_movement/damage_trigger.gd (exact analog: Area2D body_entered → take_damage)
    - .planning/phases/03-mundo-1-osasco-vertical-slice-completo/03-UI-SPEC.md (Section 3b Static Obstacle: 16x16, #C83030 with #FFDD57 2x2 corner marker, 14x14 collision, dano SFX)
  </read_first>
  <action>
    Create `scenes/world1/static_obstacle.tscn`: root Area2D with the existing `scenes/test_movement/damage_trigger.gd` script attached IF that script already plays no SFX — OR create a tiny dedicated script `scenes/world1/static_obstacle.gd` extending Area2D that, on `body_entered`, checks `if body.is_in_group("player") and body.has_method("take_damage"): body.take_damage(global_position); AudioManager.play_sfx("dano")`. Prefer the dedicated script so the `dano` SFX fires per UI-SPEC Section 3b.
    Add: CollisionShape2D with RectangleShape2D 14x14 (1px inset per side). A 16x16 placeholder sprite: ColorRect or Sprite2D filled `#C83030` with a 2x2 `#FFDD57` corner marker (geometric placeholder). Set monitoring=true. The obstacle never moves and is never destroyed.
    If you create static_obstacle.gd, add it to files_modified by including it in this task's output; it is within this plan's scope.
  </action>
  <verify>
    <automated>test -f scenes/world1/static_obstacle.tscn && grep -lq 'take_damage' scenes/world1/static_obstacle.gd scenes/test_movement/damage_trigger.gd 2>/dev/null && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `scenes/world1/static_obstacle.tscn` exists with root type Area2D
    - The attached script calls `take_damage(global_position)` on a body in group "player"
    - The obstacle scene has a CollisionShape2D (RectangleShape2D ~14x14) and a 16x16 placeholder visual using `#C83030`
    - On contact, `AudioManager.play_sfx("dano")` is called (if dedicated script created)
    - Behavior: drop the obstacle into a scratch scene with the Player, F6 — walking into it triggers the player's white-flash knockback
  </acceptance_criteria>
  <done>StaticObstacle is a non-moving Area2D that damages the player on contact via the existing take_damage knockback system.</done>
</task>

<task type="auto">
  <name>Task 3: Checkpoint (checkpoint.tscn + checkpoint.gd) and ProvaItem (prova_item.tscn + prova_item.gd)</name>
  <files>scenes/world1/checkpoint.gd, scenes/world1/checkpoint.tscn, scenes/world1/prova_item.gd, scenes/world1/prova_item.tscn</files>
  <read_first>
    - scenes/test_save/test_save.gd (SaveManager.set_checkpoint usage + is_in_group("player"))
    - scenes/test_movement/damage_trigger.gd (Area2D body_entered analog)
    - .planning/phases/03-mundo-1-osasco-vertical-slice-completo/03-PATTERNS.md (checkpoint.gd lines 172-227 + prova_item.gd lines 231-266 — exact patterns)
    - .planning/phases/03-mundo-1-osasco-vertical-slice-completo/03-UI-SPEC.md (Section 1 Checkpoint states/modulate + Section 2 Prova types/particles/collection)
  </read_first>
  <action>
    CHECKPOINT — create `scenes/world1/checkpoint.gd` as `extends Area2D` per PATTERNS.md lines 196-221. `@export var checkpoint_id: String = "mundo1_fase1_cp1"` (format `mundo1_fase{N}_cp1` per D-11). `var _activated: bool = false`. `_ready()`: `body_entered.connect(_on_body_entered)`. `_on_body_entered(body)`: `if _activated: return`; `if body.is_in_group("player"): _activated = true; SaveManager.set_checkpoint(checkpoint_id); AudioManager.play_sfx("checkpoint"); _play_activate_animation()`. `_play_activate_animation()`: tween `$AnimatedSprite2D` scale 1.0→1.25 (0.1s) then →1.0 (0.15s); set `$AnimatedSprite2D.modulate = Color("#E07020")`.
    Create `scenes/world1/checkpoint.tscn`: root Area2D (script), CollisionShape2D RectangleShape2D 16x24, AnimatedSprite2D 16x24 placeholder cartaz (laranja `#E07020` rectangle with a 4px white top stripe to suggest a band poster). Default modulate `#888888` (inactive grey per UI-SPEC). monitoring=true.
    PROVA — create `scenes/world1/prova_item.gd` as `extends Area2D` per PATTERNS.md lines 235-265. `@export var prova_id: String = "prova_foto"`. `_ready()`: `body_entered.connect(_on_body_entered)`. `_on_body_entered(body)`: `if not body.is_in_group("player"): return`; `var provas: Array = SaveManager.current_save.get("provas_mundo1", [])` (DEFENSIVE .get — never direct index, per RESEARCH Pitfall 2); `if prova_id not in provas: provas.append(prova_id); SaveManager.current_save["provas_mundo1"] = provas; SaveManager.save_game()` (the `not in` guard mitigates duplicate collection T-03-05); `AudioManager.play_sfx("prova_coletada")`; `$CPUParticles2D.emitting = true`; `$AnimatedSprite2D.visible = false`; `$CollisionShape2D.set_deferred("disabled", true)`; `get_tree().create_timer(0.25, true).timeout.connect(queue_free, CONNECT_ONE_SHOT)`.
    Create `scenes/world1/prova_item.tscn`: root Area2D (script), CollisionShape2D 16x16, AnimatedSprite2D 16x16 placeholder (2-frame bobbing idle at 4 FPS; default `prova_foto` = white `#E8E8F0` square), CPUParticles2D child (amount=6, lifetime=1.2, upward, color `#E07020`, emission box 16x4 at base — per UI-SPEC Section 2). monitoring=true. CPUParticles2D ONLY — never GPUParticles2D.
  </action>
  <verify>
    <automated>grep -q 'SaveManager.set_checkpoint(checkpoint_id)' scenes/world1/checkpoint.gd && grep -q 'AudioManager.play_sfx("checkpoint")' scenes/world1/checkpoint.gd && grep -q 'SaveManager.current_save.get("provas_mundo1"' scenes/world1/prova_item.gd && grep -q 'prova_id not in provas' scenes/world1/prova_item.gd && grep -vq 'GPUParticles2D' scenes/world1/prova_item.tscn && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `scenes/world1/checkpoint.gd` calls `SaveManager.set_checkpoint(checkpoint_id)` and `AudioManager.play_sfx("checkpoint")`, guarded by `_activated` and `is_in_group("player")`
    - `checkpoint.gd` `@export var checkpoint_id: String` default matches format `mundo1_fase{N}_cp1`
    - `scenes/world1/prova_item.gd` reads via `SaveManager.current_save.get("provas_mundo1", [])` (NOT direct index)
    - `prova_item.gd` guards duplicate collection with `if prova_id not in provas` before append
    - `prova_item.gd` calls `SaveManager.save_game()` after appending, and `$CollisionShape2D.set_deferred("disabled", true)` (never direct)
    - `prova_item.tscn` and `checkpoint.tscn` contain NO `GPUParticles2D` node
    - Behavior: in a scratch scene, walk Player into checkpoint → cartaz pulses + modulate turns `#E07020`; walk into prova → it disappears, particle burst plays, and SaveManager.current_save["provas_mundo1"] contains the id after reopening the save
  </acceptance_criteria>
  <done>Checkpoint saves checkpoint_id and animates on first contact; ProvaItem persists its id to provas_mundo1 (dedup-guarded, defensive read) and self-removes with a particle burst.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| player body → enemy Area2D | Player CharacterBody2D enters enemy stomp/hitbox/checkpoint/prova zones |
| collectible → SaveManager.current_save | ProvaItem mutates the persisted provas array |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | Denial of Service | malandro.gd patrol | mitigate | EdgeRayCast turns the Malandro at floor edges so it never falls into pits and vanishes off-screen (RESEARCH Pitfall 5) |
| T-03-05 | Tampering | prova_item.gd provas array | mitigate | `if prova_id not in provas` guard before append prevents duplicate collection inflating provas count (RESEARCH Security Domain) |
| T-03-06 | Tampering | prova_item.gd save read | mitigate | Defensive `.get("provas_mundo1", [])` read avoids "Invalid get index" on any save lacking the key |
| T-03-07 | Denial of Service | malandro die() physics | mitigate | `set_deferred("disabled", ...)` avoids "Cannot change CollisionShape2D during physics" crash (RESEARCH Pitfall 3) |
</threat_model>

<verification>
- Scratch test scene with Player + one Malandro + one StaticObstacle + one Checkpoint + one ProvaItem:
  - Stomp the Malandro → death anim + bounce + queue_free after 0.3s.
  - Lateral touch Malandro or obstacle → player white-flash knockback.
  - Touch Checkpoint → pulse + `set_checkpoint` saved (confirm via SaveManager.current_save["checkpoint_id"]).
  - Touch ProvaItem → disappears, particle burst, provas_mundo1 contains id.
- No GPUParticles2D anywhere; all collision disables are deferred.
</verification>

<success_criteria>
- Four reusable world1 scenes exist and function in isolation.
- Stomp-kill, lateral-damage, checkpoint-save, and prova-persistence all observable.
- Defensive save reads and dedup guard in place.
</success_criteria>

<output>
After completion, create `.planning/phases/03-mundo-1-osasco-vertical-slice-completo/03-02-SUMMARY.md`
</output>
