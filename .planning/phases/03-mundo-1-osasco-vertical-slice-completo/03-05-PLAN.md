---
phase: 03-mundo-1-osasco-vertical-slice-completo
plan: 05
type: execute
wave: 5
depends_on: [01, 03, 04]
files_modified:
  - dialogic/timelines/mundo1_abertura.dtl
  - scenes/world1/mundo1_abertura.tscn
  - scenes/world1/mundo1_abertura.gd
  - scenes/world1/world1_end.tscn
  - scenes/world1/world1_end.gd
  - scenes/main_menu/main_menu.gd
  - autoloads/audio_manager.gd
  - assets/audio/sfx/.gitkeep
  - assets/audio/music/.gitkeep
autonomous: false
requirements: [NARR-05, AUDIO-02, WORLD-02]
user_setup:
  - service: bfxr
    why: "Generate placeholder chiptune SFX WAV files for Mundo 1 (real SFX deferred to Phase 12)"
    dashboard_config:
      - task: "Generate 7 SFX WAVs (checkpoint, prova_coletada, prova_apresentada, dialogo_errado, stomp, dano, vitoria) at bfxr.net and export to assets/audio/sfx/"
        location: "https://www.bfxr.net/"
must_haves:
  truths:
    - "Starting New Game or Continue from the main menu opens the Mundo 1 opening narrative"
    - "The opening narrative plays narrator text then transitions into fase1_rua"
    - "AudioManager registers and plays the 7 Mundo 1 SFX at the correct gameplay moments"
    - "Mundo 1 SFX (jump, dano, checkpoint, stomp, prova) audibly fire during gameplay"
    - "Completing the boss reaches a world1_end placeholder scene with a return-to-menu option"
  artifacts:
    - path: "scenes/world1/mundo1_abertura.gd"
      provides: "Auto-start opening narrative → SceneTransition to fase1"
      contains: "fase1_rua.tscn"
    - path: "scenes/main_menu/main_menu.gd"
      provides: "New Game / Continue route to mundo1_abertura"
      contains: "mundo1_abertura.tscn"
    - path: "autoloads/audio_manager.gd"
      provides: "register_sfx calls binding the 7 SFX keys to WAV streams in _ready"
      contains: "register_sfx"
  key_links:
    - from: "scenes/main_menu/main_menu.gd"
      to: "scenes/world1/mundo1_abertura.tscn"
      via: "SceneTransition.go_to on New Game + Continue"
      pattern: "mundo1_abertura\\.tscn"
    - from: "scenes/world1/mundo1_abertura.gd"
      to: "scenes/world1/fase1_rua.tscn"
      via: "SceneTransition.go_to after Dialogic.timeline_ended"
      pattern: "fase1_rua\\.tscn"
    - from: "autoloads/audio_manager.gd"
      to: "assets/audio/sfx/*.wav"
      via: "register_sfx(key, load(path)) in _ready"
      pattern: "register_sfx\\("
---

<objective>
Complete the Mundo 1 vertical slice end-to-end by adding the bookends: the opening narrative cutscene (NARR-05), the main-menu wiring that routes New Game / Continue into Mundo 1 (WORLD-02 entry), the AudioManager SFX registration that makes the placeholder sounds actually play (AUDIO-02), and a placeholder world1_end scene so boss victory has somewhere to go.

Purpose: Implements NARR-05 (opening narrative before fase1, D-02), wires AUDIO-02 (register the 7 placeholder SFX so the stub `play_sfx` calls scattered across Plans 02/04 produce sound), and connects main_menu → mundo1_abertura → fase1 (D-02). Creates the world1_end placeholder flagged in RESEARCH Open Question 3. This is the final integration pass — after it, the full chain menu → abertura → fase1 → fase2 → fase3 → boss → end is playable.

Output: mundo1_abertura.dtl/.tscn/.gd, world1_end.tscn/.gd, modified main_menu.gd, modified audio_manager.gd (SFX registration), placeholder WAV/OGG assets. Includes a human-verify checkpoint for the full playthrough.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-mundo-1-osasco-vertical-slice-completo/03-PATTERNS.md
@.planning/phases/03-mundo-1-osasco-vertical-slice-completo/03-UI-SPEC.md

<interfaces>
<!-- Contracts from prior plans + existing code. -->

main_menu.gd (existing): three handlers currently go_to "res://scenes/test_movement/test_movement.tscn":
  _on_continue_pressed(), _on_new_game_pressed() (after no-save branch), _on_new_game_confirmed()
  — replace all 3 targets with mundo1_abertura.tscn.

AudioManager (Plan 01): register_sfx(key: String, stream: AudioStream); play_sfx(key); play_music(stream).
  Currently _ready() only creates _music_player. This plan ADDS register_sfx() calls in _ready() to bind WAV files.

test_dialogue.gd / mundo1_abertura.gd analog (PATTERNS.md lines 398-423): auto-start in _ready (no StartButton),
  skip_button.visible = SaveManager.has_seen_cutscene(...), Dialogic.start, await timeline_ended,
  SaveManager.mark_cutscene_seen, then SceneTransition.go_to(fase1).

SFX keys consumed across the phase (must be registered): checkpoint (Plan02 checkpoint.gd), prova_coletada (Plan02 prova_item.gd),
  dano (Plan02 static_obstacle.gd), stomp (Plan02 malandro.gd), prova_apresentada + dialogo_errado + vitoria (Plan04 boss_pai.gd).

UI-SPEC Section 9 opening narrative copy (Portuguese, narrator no portrait):
  L1 "Osasco. Uma cidade que não pede licença."
  L2 "É aqui que a história de Natália começa —"
  L3 "e é aqui que ela precisará provar tudo."
</interfaces>

CPUParticles2D only. Placeholder SFX = bfxr/sfxr chiptune WAVs (D-26). Music tracks may be silent/empty .ogg placeholders (D-27); music wiring is optional this phase — SFX is the AUDIO-02 requirement.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Opening narrative (mundo1_abertura) + main_menu wiring + world1_end placeholder</name>
  <files>dialogic/timelines/mundo1_abertura.dtl, scenes/world1/mundo1_abertura.gd, scenes/world1/mundo1_abertura.tscn, scenes/main_menu/main_menu.gd, scenes/world1/world1_end.gd, scenes/world1/world1_end.tscn</files>
  <read_first>
    - scenes/test_dialogue/test_dialogue.gd (full pattern: auto-start, skip-on-seen, timeline_ended)
    - dialogic/timelines/test_dialogue.dtl (timeline format; narrator lines without portrait)
    - .planning/phases/03-mundo-1-osasco-vertical-slice-completo/03-PATTERNS.md (mundo1_abertura.gd lines 398-423; main_menu.gd MODIFY lines 486-507)
    - scenes/main_menu/main_menu.gd (the 3 go_to targets to replace)
    - .planning/phases/03-mundo-1-osasco-vertical-slice-completo/03-UI-SPEC.md (Section 9 opening narrative; copy contract)
    - .planning/phases/02-infraestrutura/02-005-SUMMARY.md (Dialogic narrator-no-portrait + skip behavior)
  </read_first>
  <action>
    Create `dialogic/timelines/mundo1_abertura.dtl`: a narrator timeline (NO portrait — per UI-SPEC Section 9 the narrator has no character) with the three exact Portuguese lines from the copy contract. No choices. If Dialogic requires a speaker, use a narrator character with portrait hidden, or plain narrator-text events; document the approach in SUMMARY.
    Create `scenes/world1/mundo1_abertura.tscn`: root Node2D (mundo1_abertura.gd). A ColorRect 320x180 `#1A1A2E` background. A SkipButton inside a CanvasLayer layer=2 (per 02-005 fix that SkipButton must be above Dialogic). Dialogic auto-instantiates its layer at 50.
    Create `scenes/world1/mundo1_abertura.gd` per PATTERNS.md lines 398-423: `extends Node2D`, `const TIMELINE_ID := "mundo1_abertura"`. `_ready()`: connect skip button; `skip_button.visible = SaveManager.has_seen_cutscene(TIMELINE_ID)`; AUTO-START (no StartButton): `Dialogic.start(TIMELINE_ID)`; `await Dialogic.timeline_ended`; `SaveManager.mark_cutscene_seen(TIMELINE_ID)`; `SceneTransition.go_to("res://scenes/world1/fase1_rua.tscn")`. Skip handler enables auto_skip (one-way) per 02-005.
    Modify `scenes/main_menu/main_menu.gd`: replace ALL 3 occurrences of `"res://scenes/test_movement/test_movement.tscn"` (in `_on_continue_pressed`, `_on_new_game_pressed`, `_on_new_game_confirmed`) with `"res://scenes/world1/mundo1_abertura.tscn"`. No structural change — same SceneTransition.go_to + SaveManager.new_game calls.
    Create `scenes/world1/world1_end.tscn` + `world1_end.gd` (RESEARCH Open Question 3 placeholder): root Node2D/Control with a ColorRect `#1A1A2E`, a 16px title Label "Fim do Mundo 1", and a Button "Menu" that on press calls `SceneTransition.go_to("res://scenes/main_menu/main_menu.tscn")`. This satisfies the boss victory exit target. Real credits are Phase 10.
  </action>
  <verify>
    <automated>test -f dialogic/timelines/mundo1_abertura.dtl && grep -q 'fase1_rua.tscn' scenes/world1/mundo1_abertura.gd && grep -q 'mundo1_abertura.tscn' scenes/main_menu/main_menu.gd && ! grep -q 'test_movement.tscn' scenes/main_menu/main_menu.gd && test -f scenes/world1/world1_end.tscn && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `dialogic/timelines/mundo1_abertura.dtl` contains the three exact opening lines (`Osasco. Uma cidade que não pede licença.` etc.)
    - `scenes/world1/mundo1_abertura.gd` auto-starts the timeline in `_ready()` and calls `SceneTransition.go_to("res://scenes/world1/fase1_rua.tscn")` after `timeline_ended`, and marks the cutscene seen
    - `scenes/main_menu/main_menu.gd` has ZERO remaining references to `test_movement.tscn` and 3 references to `mundo1_abertura.tscn`
    - `scenes/world1/world1_end.tscn` exists with a "Menu" button returning to main_menu
    - Behavior: F5 from main_menu → New Game → opening narrative plays → auto-transitions to fase1_rua; second run shows the skip button
  </acceptance_criteria>
  <done>Main menu routes into the Mundo 1 opening narrative, which plays and flows into fase1; boss victory has a world1_end landing scene.</done>
</task>

<task type="auto">
  <name>Task 2: Register the 7 placeholder SFX in AudioManager and place WAV/OGG assets</name>
  <files>autoloads/audio_manager.gd, assets/audio/sfx/.gitkeep, assets/audio/music/.gitkeep</files>
  <read_first>
    - autoloads/audio_manager.gd (Plan 01 — _ready() and register_sfx(); add registration calls here)
    - .planning/phases/03-mundo-1-osasco-vertical-slice-completo/03-UI-SPEC.md (Audio Contract table — the 7 SFX keys + trigger + tone direction; music tracks)
    - .planning/phases/03-mundo-1-osasco-vertical-slice-completo/03-CONTEXT.md (D-26, D-27 — placeholder bfxr; jump SFX check)
    - scenes/player/player.gd (check whether a jump SFX is already played; if not, AUDIO-02 jump is satisfied by adding AudioManager.play_sfx in the jump branch — note in SUMMARY)
    - assets/audio/sfx/, assets/audio/music/ (currently empty with .gitkeep)
  </read_first>
  <action>
    Place 7 placeholder SFX WAV files in `assets/audio/sfx/`: `checkpoint.wav`, `prova_coletada.wav`, `prova_apresentada.wav`, `dialogo_errado.wav`, `stomp.wav`, `dano.wav`, `vitoria.wav`. These come from the bfxr human-setup step below. If the human has not yet provided them at execution time, create minimal valid silent WAV placeholders programmatically so registration does not fail, and flag in SUMMARY that real bfxr tones replace them.
    Modify `autoloads/audio_manager.gd` `_ready()`: after creating `_music_player`, call `register_sfx(key, load(path))` for each of the 7 keys, guarding each with `if ResourceLoader.exists(path):` so a missing WAV does not crash (silent-fail philosophy from Plan 01). Map key → `res://assets/audio/sfx/<key>.wav`.
    JUMP SFX (AUDIO-02 lists jump): inspect player.gd's jump-execution branch. If no jump SFX is currently played, add `AudioManager.play_sfx("jump")` in the jump branch of player.gd AND register a `jump.wav` (this brings the player.gd modification into this plan's scope — add `scenes/player/player.gd` to files_modified if you make this edit, and note it in SUMMARY). If a jump sound already exists, leave it.
    Optionally place silent `mundo1_theme.ogg` / `boss_pai_theme.ogg` in `assets/audio/music/` and wire `AudioManager.play_music()` in fase1_rua.gd `_ready()` and boss_pai.gd — music is optional this phase (D-27); SFX is the AUDIO-02 gate.
  </action>
  <verify>
    <automated>grep -c 'register_sfx(' autoloads/audio_manager.gd | awk '$1>=7{print "PASS"; exit} {print "FAIL count="$1; exit 1}'</automated>
  </verify>
  <acceptance_criteria>
    - `autoloads/audio_manager.gd` `_ready()` contains at least 7 `register_sfx(` calls for keys checkpoint, prova_coletada, prova_apresentada, dialogo_errado, stomp, dano, vitoria
    - Each registration is guarded by `ResourceLoader.exists(...)` so missing WAVs do not crash
    - `assets/audio/sfx/` contains the 7 `.wav` files (real bfxr tones OR valid silent placeholders)
    - If player.gd had no jump SFX, it now calls `AudioManager.play_sfx("jump")` in the jump branch
    - Behavior: play through fase1 — checkpoint activation, prova collection, and stomp produce audible (or registered, non-crashing) SFX; Output shows no "sfx not registered" warnings for the 7 keys
  </acceptance_criteria>
  <done>AudioManager registers all 7 Mundo 1 SFX from WAV assets; the play_sfx calls scattered across the phase now produce sound; jump SFX is covered.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verify — full Mundo 1 playthrough against the 6 success criteria</name>
  <files>(no files — human verification only)</files>
  <action>Pause for the developer to play the full Mundo 1 slice in the Godot editor and confirm all 6 Phase 3 success criteria pass. This is a verification gate, not a code task — do not modify files. Present the how-to-verify steps below and wait for the resume signal.</action>
  <what-built>
    Complete Mundo 1 vertical slice: main menu → opening narrative → fase1_rua → fase2_parque → fase3_restaurante → boss_pai → world1_end, with checkpoints, instant respawn, two enemy types, collectible provas, the non-violent trust-bar boss, Renato NPC appearances, Luis foreshadowing, and placeholder SFX.
  </what-built>
  <how-to-verify>
    1. Open the project in the Godot editor and press F5 (run main scene / main_menu).
    2. Click "Novo Jogo" → confirm the opening narrative plays (3 narrator lines, NARR-05) then auto-transitions into Rua de Osasco. (Success criterion 4)
    3. In fase1: confirm the Osasco grey-urban palette and at least one Malandro + obstacle (criterion 1). Jump on a Malandro → it dies and you bounce. Touch one laterally → white-flash knockback.
    4. Walk into the McFly checkpoint → it pulses/glows; collect a prova → it disappears with a particle burst. Listen for SFX on jump, stomp, checkpoint, prova, dano (criterion 5).
    5. Walk off a pit / take damage until death → confirm respawn at the checkpoint feels instant (under ~0.5s, no black fade) and the killed enemy reappears (criterion 2).
    6. Progress fase1 → fase2 (Renato visible faded in background, criterion 6) → fase3 (Luis faintly visible occluded in background; approach Renato, press interact, his dialogue plays).
    7. Collect at least 2 provas total across the phases before the boss.
    8. Enter boss_pai: confirm provas auto-present and raise the CONFIANÇA bar; answer dialogue choices (correct raises, wrong lowers with buzzer); confirm NO physical damage occurs (criterion 3). Reach ~80% → Renato enters as final proof. Reach 100% → victory cutscene → world1_end.
    9. (Negative test) Restart, reach the boss with fewer than 2 provas → confirm the blocking line and bounce back to fase3.
  </how-to-verify>
  <resume-signal>Type "approved" if all 6 success criteria pass, or describe which criterion failed and what you observed.</resume-signal>
  <verify>
    <automated>Manual human-verify in Godot editor — no automated command. Developer plays menu → abertura → fase1 → fase2 → fase3 → boss → world1_end and checks the 6 criteria.</automated>
  </verify>
  <done>Developer types "approved" after confirming all 6 Phase 3 success criteria pass in a full playthrough.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| disk → AudioManager | WAV/OGG files loaded at startup may be missing or malformed |
| SaveManager.seen_cutscenes → abertura | Opening narrative reads/writes the seen-cutscene flag |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-15 | Denial of Service | audio_manager.gd registration | mitigate | Each `register_sfx` guarded by `ResourceLoader.exists` so a missing WAV (e.g. human hasn't run bfxr yet) never crashes startup |
| T-03-16 | Tampering | mundo1_abertura seen flag | mitigate | `has_seen_cutscene`/`mark_cutscene_seen` use the defensive SaveManager dict pattern; a missing key defaults false, not crash |
| T-03-17 | Denial of Service | main_menu → mundo1_abertura | accept | Hard-coded internal scene path; no user input; SceneTransition already battle-tested in Phase 2 |
</threat_model>

<verification>
- F5 from main_menu: New Game → narrative → fase1 (no test_movement reference remains).
- 7 SFX registered, guarded, audible during fase1 gameplay.
- Boss victory → world1_end → Menu button returns to main_menu.
- Human-verify checkpoint covers all 6 Phase 3 success criteria.
</verification>

<success_criteria>
- Opening narrative (NARR-05) plays from the menu and flows into fase1.
- AUDIO-02 SFX register and fire at correct moments without crashes.
- Full chain menu → abertura → fase1 → fase2 → fase3 → boss → world1_end is playable.
- Human verifies all 6 Phase 3 success criteria pass.
</success_criteria>

<output>
After completion, create `.planning/phases/03-mundo-1-osasco-vertical-slice-completo/03-05-SUMMARY.md`
</output>
