---
phase: 03-mundo-1-osasco-vertical-slice-completo
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - autoloads/audio_manager.gd
  - project.godot
  - scenes/player/player.gd
  - scenes/player/player.tscn
  - autoloads/save_manager.gd
autonomous: true
requirements: [AUDIO-02, WORLD-05, BOSS-01]
must_haves:
  truths:
    - "AudioManager.play_sfx(key) can be called from any script without crash"
    - "player.gd emits a `died` signal when the death animation finishes"
    - "The Player node is in the \"player\" group so Area2D triggers detect it"
    - "SaveManager.current_save has a \"provas_mundo1\" array on a fresh save"
  artifacts:
    - path: "autoloads/audio_manager.gd"
      provides: "Centralized SFX/music playback autoload (stub-safe play_sfx)"
      contains: "func play_sfx"
    - path: "scenes/player/player.gd"
      provides: "died signal + emit on death animation finished"
      contains: "signal died"
    - path: "autoloads/save_manager.gd"
      provides: "provas_mundo1 key in _default_save() + SCHEMA_VERSION bump"
      contains: "provas_mundo1"
  key_links:
    - from: "autoloads/audio_manager.gd"
      to: "project.godot [autoload]"
      via: "AudioManager registered as global autoload"
      pattern: "AudioManager=\"\\*res://autoloads/audio_manager.gd\""
    - from: "scenes/player/player.gd"
      to: "fase scripts (Plan 03) / boss (Plan 04)"
      via: "died.emit() in _on_animated_sprite_2d_animation_finished"
      pattern: "died\\.emit\\(\\)"
---

<objective>
Build the missing infrastructure that ALL Phase 3 gameplay code depends on: the AudioManager autoload (referenced by every game object's SFX call), the player `died` signal (the respawn hook), the Player group membership (every Area2D trigger checks `is_in_group("player")`), and the `provas_mundo1` save schema key (read by ProvaItem and the boss).

Purpose: RESEARCH.md identifies three hard-blockers that must exist before any other Phase 3 work compiles: AudioManager autoload, player `signal died`, and `provas_mundo1` in the save schema. PATTERNS.md and UI-SPEC additionally rely on the Player being in the `"player"` group — which it currently is NOT (verified in player.tscn). This plan removes every blocker in one foundation pass.

Output: `autoloads/audio_manager.gd` (new autoload), modified `player.gd` (signal + emit), modified `player.tscn` (group), modified `save_manager.gd` (schema), updated `project.godot` (AudioManager registration).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-mundo-1-osasco-vertical-slice-completo/03-RESEARCH.md
@.planning/phases/03-mundo-1-osasco-vertical-slice-completo/03-PATTERNS.md

<interfaces>
<!-- Existing autoload structure the executor must mirror (from save_manager.gd). -->
<!-- AudioManager extends Node, creates child AudioStreamPlayer nodes in _ready(). -->

save_manager.gd current _default_save() returns:
  {"version": SCHEMA_VERSION, "checkpoint_id": "", "worlds_completed": [],
   "powers_unlocked": [], "seen_cutscenes": {}}
SCHEMA_VERSION is currently 1. load_game() resets save when stored version != SCHEMA_VERSION.

player.gd existing signal-relevant method (to modify):
  func _on_animated_sprite_2d_animation_finished() -> void:
      if sprite.animation == "hurt":
          _is_hurt = false
      elif sprite.animation == "death":
          print("Player death animation finished — respawn hooked in Phase 3")

project.godot [autoload] section currently has:
  SaveManager, SceneTransition, ControlsManager (AudioManager is ABSENT).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create AudioManager autoload and register it in project.godot</name>
  <files>autoloads/audio_manager.gd, project.godot</files>
  <read_first>
    - autoloads/save_manager.gd (autoload structure: extends Node, _ready, typed vars)
    - .planning/phases/03-mundo-1-osasco-vertical-slice-completo/03-PATTERNS.md (audio_manager.gd section — exact core pattern lines 52-82)
    - project.godot (the [autoload] section — confirm SaveManager/SceneTransition/ControlsManager entries)
    - .planning/phases/03-mundo-1-osasco-vertical-slice-completo/03-UI-SPEC.md (Audio Contract table — the 8 SFX keys: checkpoint, prova_coletada, prova_apresentada, dialogo_errado, stomp, dano, vitoria, dialogo_beep)
  </read_first>
  <action>
    Create `autoloads/audio_manager.gd` as `extends Node`. Declare `var _sfx_players: Dictionary = {}` and `var _music_player: AudioStreamPlayer`. In `_ready()`, instantiate `_music_player = AudioStreamPlayer.new()`, set `_music_player.bus = "Master"`, and `add_child(_music_player)`.
    Implement four methods: `register_sfx(key: String, stream: AudioStream) -> void` (creates a new AudioStreamPlayer on bus "Master", sets its stream, add_child, stores in `_sfx_players[key]`); `play_sfx(key: String) -> void` (if `_sfx_players.has(key)` then `_sfx_players[key].play()`, ELSE silent-fail with `push_warning("AudioManager: sfx '" + key + "' not registered")` — must NOT crash, this is the stub behavior until Plan 05 registers real WAV files); `play_music(stream: AudioStream) -> void` (set `_music_player.stream = stream`, `_music_player.play()`); `stop_music() -> void` (`_music_player.stop()`).
    Use explicit types everywhere (warnings-as-errors is active per CLAUDE.md). The silent-fail in play_sfx mirrors controls_manager.gd's "never crash on missing asset" philosophy.
    Register the autoload by adding the line `AudioManager="*res://autoloads/audio_manager.gd"` to the `[autoload]` section of project.godot, AFTER the ControlsManager entry.
  </action>
  <verify>
    <automated>grep -q 'func play_sfx(key: String)' autoloads/audio_manager.gd && grep -q 'AudioManager="\*res://autoloads/audio_manager.gd"' project.godot && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `autoloads/audio_manager.gd` contains `extends Node`
    - `autoloads/audio_manager.gd` contains `func play_sfx(key: String) -> void`
    - `autoloads/audio_manager.gd` contains `func register_sfx(key: String, stream: AudioStream) -> void`
    - `autoloads/audio_manager.gd` contains `func play_music(stream: AudioStream) -> void` and `func stop_music() -> void`
    - `play_sfx` does NOT crash on an unregistered key (guarded by `_sfx_players.has(key)`)
    - `project.godot` `[autoload]` section contains `AudioManager="*res://autoloads/audio_manager.gd"`
    - Behavior: open project in Godot, run any scene — Output shows no "Identifier 'AudioManager' is not declared" error
  </acceptance_criteria>
  <done>AudioManager is a registered global autoload with stub-safe play_sfx/register_sfx/play_music/stop_music methods that never crash on missing keys.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add `died` signal to player.gd and put Player node in the "player" group</name>
  <files>scenes/player/player.gd, scenes/player/player.tscn</files>
  <read_first>
    - scenes/player/player.gd (current full file — especially `_on_animated_sprite_2d_animation_finished` at the death branch with the print stub, and the class top after `extends CharacterBody2D`)
    - scenes/player/player.tscn (the `[node name="Player" type="CharacterBody2D"]` block — currently has NO groups)
    - .planning/phases/03-mundo-1-osasco-vertical-slice-completo/03-PATTERNS.md (player.gd MODIFY section lines 427-451 — exact change)
    - scenes/test_save/test_save.gd (confirms `body.is_in_group("player")` is the detection contract that requires the group)
  </read_first>
  <behavior>
    - When the player's death animation finishes, player emits the `died` signal (no arguments)
    - The `hurt` animation branch still clears `_is_hurt = false` (unchanged behavior)
    - Any Area2D calling `body.is_in_group("player")` on the Player node returns true
  </behavior>
  <action>
    In `scenes/player/player.gd`: add the bare signal declaration `signal died` near the class top — immediately after `extends CharacterBody2D` and before the `@export_group("Movement")` line. The signal takes no parameters (scene scripts read player state directly per PATTERNS.md).
    Modify `_on_animated_sprite_2d_animation_finished()`: in the `elif sprite.animation == "death":` branch, REPLACE the line `print("Player death animation finished — respawn hooked in Phase 3")` with `died.emit()`. Leave the `if sprite.animation == "hurt": _is_hurt = false` branch exactly as-is.
    In `scenes/player/player.tscn`: add the Player node to the `"player"` group. Edit the root node line `[node name="Player" type="CharacterBody2D"]` to `[node name="Player" type="CharacterBody2D" groups=["player"]]`. This is mandatory — every checkpoint, prova, malandro, and obstacle trigger checks `is_in_group("player")` and currently fails because the group is absent (verified: player.tscn has no groups).
  </action>
  <verify>
    <automated>grep -q '^signal died' scenes/player/player.gd && grep -q 'died.emit()' scenes/player/player.gd && grep -q 'groups=\["player"\]' scenes/player/player.tscn && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `scenes/player/player.gd` contains a line `signal died` (no parameters) located before `@export_group("Movement")`
    - `scenes/player/player.gd` `_on_animated_sprite_2d_animation_finished` death branch contains `died.emit()` and NO longer contains the `print(` stub
    - `scenes/player/player.gd` hurt branch still contains `_is_hurt = false`
    - `scenes/player/player.tscn` root Player node line contains `groups=["player"]`
    - Behavior: F6 on `scenes/test_movement/test_movement.tscn` — player still runs/jumps with no parse error in Output
  </acceptance_criteria>
  <done>player.gd declares `signal died` and emits it on death animation finish; the Player node is in the "player" group so all Phase 3 Area2D triggers can detect it.</done>
</task>

<task type="auto">
  <name>Task 3: Add provas_mundo1 to the save schema and bump SCHEMA_VERSION</name>
  <files>autoloads/save_manager.gd</files>
  <read_first>
    - autoloads/save_manager.gd (full file — `SCHEMA_VERSION` const at line 4, `_default_save()` at the bottom, and `load_game()` version-check migration at lines ~19-26)
    - .planning/phases/03-mundo-1-osasco-vertical-slice-completo/03-PATTERNS.md (save_manager.gd MODIFY section lines 454-482 — exact two changes)
    - .planning/phases/03-mundo-1-osasco-vertical-slice-completo/03-RESEARCH.md (Pitfall 2 — schema migration safety)
  </read_first>
  <action>
    In `autoloads/save_manager.gd`, make two changes per RESEARCH.md Pattern 3:
    Change 1: bump the constant `const SCHEMA_VERSION := 1` to `const SCHEMA_VERSION := 2`. This forces Phase 2 saves (which lack `provas_mundo1`) to be detected as version-incompatible by the existing `load_game()` check (`data.get("version", 0) == SCHEMA_VERSION`) and reset to the new default — no extra migration code needed.
    Change 2: add the entry `"provas_mundo1": [],` to the dictionary returned by `_default_save()`, after the `"seen_cutscenes": {},` line.
    Do NOT change any other method. The defensive `.get("provas_mundo1", [])` reads happen in consumer scripts (Plan 02 ProvaItem, Plan 04 boss) — this plan only owns the schema default.
  </action>
  <verify>
    <automated>grep -q 'const SCHEMA_VERSION := 2' autoloads/save_manager.gd && grep -q '"provas_mundo1": \[\]' autoloads/save_manager.gd && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `autoloads/save_manager.gd` contains `const SCHEMA_VERSION := 2`
    - `autoloads/save_manager.gd` `_default_save()` returns a dict containing `"provas_mundo1": []`
    - The `load_game()` version-check block is unchanged (still `data.get("version", 0) == SCHEMA_VERSION`)
    - Behavior: F6 on `scenes/test_save/test_save.tscn`, then in Godot's Remote inspector / Output confirm `SaveManager.current_save` has a `provas_mundo1` key equal to `[]` on a fresh save
  </acceptance_criteria>
  <done>A fresh save (or any save from before this bump) initializes with `provas_mundo1: []` and SCHEMA_VERSION is 2.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| disk → SaveManager | `user://save.dat` is read into `current_save`; an old/edited save may lack expected keys |
| game object → AudioManager | Arbitrary string SFX keys passed to `play_sfx` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | save_manager.gd schema | mitigate | SCHEMA_VERSION bump to 2 forces incompatible/old saves to reset via existing `load_game()` version check — prevents "Invalid get index 'provas_mundo1'" crash (RESEARCH Pitfall 2) |
| T-03-02 | Denial of Service | audio_manager.gd play_sfx | mitigate | `play_sfx` guards with `_sfx_players.has(key)` and silent-fails (push_warning) on missing key — a bad key never crashes the game (RESEARCH Pitfall 6) |
| T-03-03 | Tampering | user://save.dat contents | accept | Offline personal game, no online ranking; save tampering has no security impact (RESEARCH Security Domain) |
</threat_model>

<verification>
- F6 on `scenes/test_movement/test_movement.tscn`: player moves with no parse errors; Output has no "AudioManager not declared".
- F6 on `scenes/test_save/test_save.tscn`: fresh save shows `provas_mundo1: []`.
- `grep -n 'AudioManager' project.godot` shows the autoload registered.
</verification>

<success_criteria>
- AudioManager is callable globally without crashing on unregistered keys.
- player.gd emits `died`; Player node is in the "player" group.
- save schema includes `provas_mundo1` and SCHEMA_VERSION is 2.
- No regressions in existing test scenes (movement, save).
</success_criteria>

<output>
After completion, create `.planning/phases/03-mundo-1-osasco-vertical-slice-completo/03-01-SUMMARY.md`
</output>
