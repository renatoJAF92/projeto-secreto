---
phase: 03-mundo-1-osasco-vertical-slice-completo
plan: 03
type: execute
wave: 3
depends_on: [01, 02]
files_modified:
  - scenes/world1/fase1_rua.tscn
  - scenes/world1/fase1_rua.gd
  - scenes/world1/fase2_parque.tscn
  - scenes/world1/fase2_parque.gd
  - scenes/world1/fase3_restaurante.tscn
  - scenes/world1/fase3_restaurante.gd
  - scenes/world1/renato_npc.gd
  - scenes/world1/osasco_tileset.tres
autonomous: true
requirements: [WORLD-01, WORLD-02, WORLD-03, WORLD-05, NPC-01]
must_haves:
  truths:
    - "Each of fase1/fase2/fase3 is a playable scene with Osasco-palette TileMapLayer ground, the Player, enemies, a checkpoint, and provas"
    - "Dying respawns the player at the active checkpoint position in under 500ms with no fade"
    - "Killed enemies reset to their origin when the player respawns"
    - "Completing a phase transitions to the next via SceneTransition.go_to()"
    - "Renato appears as a background NPC sprite in fase2_parque and as a dialogue NPC in fase3_restaurante"
    - "Luis appears hidden/occluded in the fase3_restaurante background (foreshadowing)"
  artifacts:
    - path: "scenes/world1/fase1_rua.gd"
      provides: "Phase controller: respawn-on-death + enemy reset + go-to-next"
      contains: "died.connect"
    - path: "scenes/world1/fase1_rua.tscn"
      provides: "Playable Rua de Osasco scene with tileset, player, enemies, checkpoint, provas"
      min_lines: 20
    - path: "scenes/world1/osasco_tileset.tres"
      provides: "Placeholder geometric tileset with Osasco palette"
      contains: "TileSet"
  key_links:
    - from: "scenes/world1/fase1_rua.gd"
      to: "player.died signal"
      via: "player.died.connect(_on_player_died) → respawn"
      pattern: "died\\.connect"
    - from: "scenes/world1/fase3_restaurante.gd"
      to: "scenes/world1/boss_pai.tscn"
      via: "ExitTrigger SceneTransition.go_to to boss"
      pattern: "boss_pai\\.tscn"
    - from: "scenes/world1/fase2_parque.tscn"
      to: "Renato NPC sprite (background)"
      via: "Sprite2D placeholder NPC, no interaction"
      pattern: "Renato"
---

<objective>
Assemble the three linear playable phases of Mundo 1 — Rua de Osasco, Parque, Restaurante — from the Plan 02 game objects, wire instant (<500ms) checkpoint respawn with enemy reset, and place the narrative NPCs (Renato background in fase2, Renato dialogue NPC + Luis foreshadowing in fase3). This is the first fully playable vertical slice: a player can run through three Osasco phases, fight enemies, hit checkpoints, collect provas, die-and-respawn, and reach the boss door.

Purpose: Implements WORLD-01 (Osasco palette + themed enemies), WORLD-02 (3 linear phases + boss exit), WORLD-03 (visual checkpoints), WORLD-05 (respawn <500ms — CRITICAL: must NOT use SceneTransition's 600ms fade per RESEARCH Pitfall 1), and NPC-01 (Renato visible in fase2 + interactive in fase3). Honors D-01 (4 scenes), D-07 (enemy reset on respawn), D-10 (provas persist), D-23 (Luis foreshadowing), D-24 (Renato in fase2 bg + fase3 dialogue).

Output: fase1_rua, fase2_parque, fase3_restaurante (.tscn + .gd each), renato_npc.gd, and a shared osasco_tileset.tres.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-mundo-1-osasco-vertical-slice-completo/03-PATTERNS.md
@.planning/phases/03-mundo-1-osasco-vertical-slice-completo/03-UI-SPEC.md

<interfaces>
<!-- Contracts from Plan 01, Plan 02, and existing code. -->

player.gd (Plan 01): signal died  (emitted on death animation finish); Player node in group "player".
  Respawn touches: _is_dead, _is_hurt, velocity, global_position.

Plan 02 objects (instance these — do NOT reimplement):
  scenes/world1/malandro.tscn        — patrol enemy, in group "enemies", has reset_to_origin()
  scenes/world1/static_obstacle.tscn — Area2D hazard
  scenes/world1/checkpoint.tscn      — @export checkpoint_id (set per fase: "mundo1_fase1_cp1" etc.)
  scenes/world1/prova_item.tscn      — @export prova_id (prova_foto/prova_carta/prova_presente)

SceneTransition (existing autoload): func go_to(scene_path: String) -> void  # 600ms fade — phase transitions ONLY, NEVER respawn.

PATTERNS.md fase script pattern (lines 367-394):
  extends Node2D
  @onready var player ; @onready var checkpoint
  _ready(): _checkpoint_position = checkpoint.global_position; player.died.connect(_on_player_died)
  _on_player_died(): player.global_position = _checkpoint_position; player.velocity = Vector2.ZERO
                     player._is_dead=false; player._is_hurt=false; _reset_enemies()
  _reset_enemies(): for e in get_tree().get_nodes_in_group("enemies"): if e.has_method("reset_to_origin"): e.reset_to_origin()
</interfaces>

Palette (D-03): base `#1A1A2E`, mid `#16213E`, laranja `#E07020`, vermelho `#C83030`. Renato NPC 16x32 `#2980B9`. Luis foreshadow 16x32 `#8B0000`. TileMapLayer (Godot 4.4+), NOT TileMap. Tiles: geometric placeholder acceptable (Claude's Discretion + deferred to Phase 12).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Osasco placeholder tileset + fase1_rua (playable scene + respawn controller)</name>
  <files>scenes/world1/osasco_tileset.tres, scenes/world1/fase1_rua.gd, scenes/world1/fase1_rua.tscn</files>
  <read_first>
    - .planning/phases/03-mundo-1-osasco-vertical-slice-completo/03-PATTERNS.md (fase_N.gd pattern lines 344-394; SceneTransition.go_to usage lines 591-599)
    - .planning/phases/03-mundo-1-osasco-vertical-slice-completo/03-RESEARCH.md (Pattern 2 respawn <500ms; Pitfall 1 do NOT use SceneTransition for respawn)
    - scenes/test_movement/test_movement.tscn (analog scene with Player instance + floor geometry)
    - scenes/player/player.tscn (the Player scene to instance; confirm group "player")
    - scenes/world1/malandro.tscn, scenes/world1/checkpoint.tscn, scenes/world1/prova_item.tscn (Plan 02 — instance these)
    - .planning/phases/03-mundo-1-osasco-vertical-slice-completo/03-UI-SPEC.md (palette table; Scene File Paths)
  </read_first>
  <action>
    Create `scenes/world1/osasco_tileset.tres`: a TileSet resource with 16x16 tile size and at least 2-3 placeholder tile entries (solid ground tile filled `#16213E`, a darker `#1A1A2E` background tile, and an accent `#E07020` decorative tile). Each ground tile MUST have a physics collision polygon (16x16 box) so the player can stand on it. Geometric placeholder colors are acceptable (art deferred to Phase 12).
    Create `scenes/world1/fase1_rua.gd` as `extends Node2D` per PATTERNS.md fase pattern: `@onready var player: CharacterBody2D = $Player`, `@onready var checkpoint: Area2D = $Checkpoint`, `var _checkpoint_position: Vector2`. `_ready()`: `_checkpoint_position = checkpoint.global_position`; `player.died.connect(_on_player_died)`. Add a commented hook `# AudioManager.play_music(...) wired in Plan 05` — do NOT block on missing music.
    `_on_player_died()`: RESPAWN DIRECTLY (no SceneTransition — must be <500ms per WORLD-05): `player.global_position = _checkpoint_position`; `player.velocity = Vector2.ZERO`; `player._is_dead = false`; `player._is_hurt = false`; then `_reset_enemies()`.
    `_reset_enemies()`: `for enemy in get_tree().get_nodes_in_group("enemies"): if enemy.has_method("reset_to_origin"): enemy.reset_to_origin()`.
    Add a phase-exit Area2D `$ExitTrigger` at the right end of the level: on `body_entered` with a body in group "player", call `SceneTransition.go_to("res://scenes/world1/fase2_parque.tscn")`. Wire its signal in `_ready()`.
    Create `scenes/world1/fase1_rua.tscn`: root Node2D (fase1_rua.gd attached). Add TileMapLayer node(s) using osasco_tileset.tres — paint a linear ground path with at least one platform and one pit (so the Malandro edge-turn is exercised). Instance the Player (player.tscn) at the left start. Instance 2-3 Malandro (Claude's discretion difficulty) and 1-2 StaticObstacle along the path. Instance 1 Checkpoint at mid-level with `checkpoint_id = "mundo1_fase1_cp1"`. Instance 1-2 ProvaItem (set one `prova_id = "prova_foto"`). Add the ExitTrigger Area2D at the right edge. Background ColorRect `#1A1A2E` behind the tilemap.
    IMPORTANT: place the checkpoint and the level start ABOVE any pit/hazard so respawn never lands the player into instant death (mitigates infinite respawn loop, RESEARCH Security Domain).
  </action>
  <verify>
    <automated>test -f scenes/world1/osasco_tileset.tres && grep -q 'died.connect(_on_player_died)' scenes/world1/fase1_rua.gd && grep -q 'reset_to_origin' scenes/world1/fase1_rua.gd && grep -q 'fase2_parque.tscn' scenes/world1/fase1_rua.gd && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `scenes/world1/osasco_tileset.tres` exists and contains `TileSet` with a 16x16 ground tile that has collision
    - `scenes/world1/fase1_rua.gd` connects `player.died.connect(_on_player_died)` in `_ready()`
    - `_on_player_died()` sets `player.global_position = _checkpoint_position` and does NOT call `SceneTransition` (respawn must be instant, <500ms — WORLD-05)
    - `_on_player_died()` calls `_reset_enemies()` which iterates group "enemies" and calls `reset_to_origin()`
    - The ExitTrigger calls `SceneTransition.go_to("res://scenes/world1/fase2_parque.tscn")`
    - `fase1_rua.tscn` instances Player, ≥1 Checkpoint (`checkpoint_id = "mundo1_fase1_cp1"`), ≥1 Malandro, ≥1 ProvaItem, and uses a TileMapLayer with osasco_tileset.tres
    - Behavior: F6 on fase1_rua.tscn — player runs on tile ground; killing then dying respawns at checkpoint within <500ms (no black fade); a previously-stomped Malandro reappears after respawn
  </acceptance_criteria>
  <done>fase1_rua is fully playable with Osasco tiles, enemies, checkpoint, provas, instant respawn with enemy reset, and a transition to fase2.</done>
</task>

<task type="auto">
  <name>Task 2: fase2_parque (playable scene + Renato background NPC)</name>
  <files>scenes/world1/fase2_parque.gd, scenes/world1/fase2_parque.tscn</files>
  <read_first>
    - scenes/world1/fase1_rua.gd (the fase controller pattern just created — reuse identically)
    - scenes/world1/fase1_rua.tscn (the scene assembly pattern to mirror)
    - .planning/phases/03-mundo-1-osasco-vertical-slice-completo/03-UI-SPEC.md (Section 7a Renato Parque: 16x32 `#2980B9`, x=220 y=110, z_index -1, modulate alpha 0.8, NO interaction/collision)
  </read_first>
  <action>
    Create `scenes/world1/fase2_parque.gd` as `extends Node2D` — identical controller pattern to fase1_rua.gd (player.died respawn + enemy reset + ExitTrigger). The ExitTrigger calls `SceneTransition.go_to("res://scenes/world1/fase3_restaurante.tscn")`.
    Create `scenes/world1/fase2_parque.tscn`: root Node2D (fase2_parque.gd). TileMapLayer with osasco_tileset.tres painted as a park-themed linear path (Claude's discretion layout; reuse the same tileset). Player at left start. 2-3 Malandro + 1-2 StaticObstacle. 1 Checkpoint with `checkpoint_id = "mundo1_fase2_cp1"`. 1-2 ProvaItem (set one `prova_id = "prova_carta"`). ExitTrigger at right edge.
    Add the Renato background NPC per UI-SPEC 7a: a `Renato` Node2D containing a Sprite2D, 16x32 `#2980B9` placeholder rectangle with `#5DADE2` 2x2 eye dots, positioned x=220 y=110, `z_index = -1`, `modulate = Color(1, 1, 1, 0.8)`. NO collision, NO Area2D, NO interaction — static background sprite only (D-24 fase2 = bg only).
  </action>
  <verify>
    <automated>grep -q 'died.connect(_on_player_died)' scenes/world1/fase2_parque.gd && grep -q 'fase3_restaurante.tscn' scenes/world1/fase2_parque.gd && grep -q 'Renato' scenes/world1/fase2_parque.tscn && grep -q 'mundo1_fase2_cp1' scenes/world1/fase2_parque.tscn && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `scenes/world1/fase2_parque.gd` reuses the respawn+reset controller (connects `player.died`, no SceneTransition in respawn path)
    - ExitTrigger calls `SceneTransition.go_to("res://scenes/world1/fase3_restaurante.tscn")`
    - `fase2_parque.tscn` contains a node named `Renato` with a Sprite2D, `z_index = -1`, `modulate` alpha 0.8, and NO CollisionShape2D/Area2D under it
    - `fase2_parque.tscn` instances Player, Checkpoint (`mundo1_fase2_cp1`), ≥1 Malandro, ≥1 ProvaItem with `prova_id = "prova_carta"`
    - Behavior: F6 on fase2_parque.tscn — Renato is visible faded in the background and cannot be collided with; respawn + enemy reset work as in fase1
  </acceptance_criteria>
  <done>fase2_parque is playable with the same systems as fase1, plus Renato standing faded in the background (NPC-01 first appearance), transitioning to fase3.</done>
</task>

<task type="auto">
  <name>Task 3: fase3_restaurante (playable scene + Luis foreshadowing + Renato dialogue NPC)</name>
  <files>scenes/world1/fase3_restaurante.gd, scenes/world1/fase3_restaurante.tscn, scenes/world1/renato_npc.gd</files>
  <read_first>
    - scenes/world1/fase2_parque.gd (controller pattern to reuse)
    - scenes/test_dialogue/test_dialogue.gd (Dialogic.start + ResourceLoader.exists guard patterns)
    - .planning/phases/03-mundo-1-osasco-vertical-slice-completo/03-UI-SPEC.md (Section 7b Renato dialogue trigger; Section 8 Luis foreshadowing: 16x32 `#8B0000`, x=60 y=115, z_index -2, modulate Color(0.5,0.5,0.5,0.7), occluded by foreground tile, NO interaction)
    - .planning/phases/03-mundo-1-osasco-vertical-slice-completo/03-CONTEXT.md (D-23 Luis hidden in fase3 background; D-24 Renato active dialogue in fase3)
    - autoloads/controls_manager.gd (confirm whether an "interact" action exists; if not, use existing "jump" as the trigger and note in SUMMARY)
  </read_first>
  <action>
    Create `scenes/world1/fase3_restaurante.gd` as `extends Node2D` — same respawn+reset controller. The ExitTrigger calls `SceneTransition.go_to("res://scenes/world1/boss_pai.tscn")`.
    Create `scenes/world1/fase3_restaurante.tscn`: root Node2D (fase3_restaurante.gd). TileMapLayer (osasco_tileset.tres) painted as a restaurant interior linear path. Player at left start. 1-2 Malandro + 1-2 StaticObstacle (lighter difficulty — narrative phase). 1 Checkpoint `checkpoint_id = "mundo1_fase3_cp1"`. 1-2 ProvaItem (set one `prova_id = "prova_presente"`). ExitTrigger at right edge → boss_pai.tscn.
    LUIS FORESHADOWING (D-23, UI-SPEC Section 8): add a `LuisForeshadow` Node2D + Sprite2D, 16x32 `#8B0000` rectangle, position x=60 y=115, `z_index = -2`, `modulate = Color(0.5, 0.5, 0.5, 0.7)`. Place a foreground TileMapLayer tile (pillar/doorframe, z_index 1) overlapping it for partial occlusion. NO collision, NO label, NO interaction.
    RENATO DIALOGUE NPC (D-24 fase3, UI-SPEC Section 7b): create `scenes/world1/renato_npc.gd` as `extends StaticBody2D`. The RenatoNPC node: 16x32 `#2980B9` Sprite2D at x=200 y=120, z_index 0, with a child Area2D `$DialogueZone` (CollisionShape2D ~32x16 in front) and a child Label `$Prompt` (text `[E]`, color `#FFDD57`, 8px, positioned above head, hidden by default). In renato_npc.gd `_ready()`: connect `$DialogueZone.body_entered`/`body_exited`. On enter (body in group "player"): `_player_in_zone = true`, `$Prompt.visible = true`. On exit: `_player_in_zone = false`, `$Prompt.visible = false`. In `_unhandled_input(event)`: if `_player_in_zone` and `event.is_action_pressed("interact")` — if ControlsManager has no "interact" action, use `"jump"` and document the substitution in the SUMMARY — guard `if not ResourceLoader.exists("res://dialogic/timelines/renato_restaurante.dtl"): return` (the timeline is authored in Plan 04; this guard prevents a crash before Plan 04 runs), then `Dialogic.start("renato_restaurante")`.
    Instance RenatoNPC (renato_npc.gd scene or inline node tree) into fase3_restaurante.tscn.
  </action>
  <verify>
    <automated>grep -q 'died.connect(_on_player_died)' scenes/world1/fase3_restaurante.gd && grep -q 'boss_pai.tscn' scenes/world1/fase3_restaurante.gd && grep -q 'LuisForeshadow' scenes/world1/fase3_restaurante.tscn && grep -q 'mundo1_fase3_cp1' scenes/world1/fase3_restaurante.tscn && grep -q 'renato_restaurante' scenes/world1/renato_npc.gd && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `scenes/world1/fase3_restaurante.gd` reuses the respawn+reset controller; ExitTrigger calls `SceneTransition.go_to("res://scenes/world1/boss_pai.tscn")`
    - `fase3_restaurante.tscn` contains a `LuisForeshadow` node with z_index -2 and modulate `Color(0.5, 0.5, 0.5, 0.7)`, partially occluded by a foreground tile, with NO collision/interaction
    - `scenes/world1/renato_npc.gd` calls `Dialogic.start("renato_restaurante")` only after `ResourceLoader.exists(...)` returns true, gated on `_player_in_zone` + interact input
    - `fase3_restaurante.tscn` instances Player, Checkpoint (`mundo1_fase3_cp1`), ≥1 Malandro, ≥1 ProvaItem with `prova_id = "prova_presente"`, and a RenatoNPC
    - Behavior: F6 on fase3_restaurante.tscn — Luis is faintly visible occluded in the background; approaching Renato shows the `[E]` prompt; pressing interact (no crash even before Plan 04 authors the timeline)
  </acceptance_criteria>
  <done>fase3_restaurante is playable with the same systems, plus an occluded Luis foreshadow figure and an interactive Renato NPC that triggers his dialogue (guarded until Plan 04 authors the timeline), transitioning to the boss.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| player → ExitTrigger Area2D | Player entering an exit zone triggers a scene change |
| player respawn position → hazards | Respawn point geometry relative to pits/obstacles |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-08 | Denial of Service | fase respawn position | mitigate | Checkpoint + start placed above any pit/hazard so respawn never lands the player into instant death (RESEARCH Security Domain — infinite respawn loop) |
| T-03-09 | Denial of Service | renato_npc.gd Dialogic.start | mitigate | `ResourceLoader.exists(...)` guard before `Dialogic.start("renato_restaurante")` prevents crash when the timeline is absent (pre-Plan-04 ordering safety) |
| T-03-10 | Information Disclosure | LuisForeshadow visual | accept | Intentional foreshadowing (D-23); no name/label/identity revealed — by design, not a leak |
</threat_model>

<verification>
- F6 each scene (fase1, fase2, fase3) individually: player runs on tiles, enemies patrol, checkpoint pulses+saves, provas collect+persist.
- Death → respawn at checkpoint <500ms (time with external stopwatch per RESEARCH validation), enemies reset.
- Full chain (when entered from menu later): fase1 → fase2 → fase3 → boss door via SceneTransition.
- fase2 Renato faded in background (no collision); fase3 Luis occluded foreshadow; fase3 Renato `[E]` prompt + dialogue trigger (guarded).
</verification>

<success_criteria>
- Three playable, linear Osasco phases exist and chain to the boss.
- Respawn is instant (<500ms) with enemy reset; provas persist across death.
- Renato (fase2 bg + fase3 dialogue) and Luis (fase3 foreshadow) placed per UI-SPEC.
</success_criteria>

<output>
After completion, create `.planning/phases/03-mundo-1-osasco-vertical-slice-completo/03-03-SUMMARY.md`
</output>
