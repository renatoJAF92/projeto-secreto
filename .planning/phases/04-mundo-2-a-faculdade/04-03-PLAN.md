---
phase: 04
plan: 03
type: execute
wave: 2
depends_on: [04-01, 04-02]
files_modified:
  - scenes/world2/fase1_campus.tscn
  - scenes/world2/fase1_campus.gd
  - scenes/world2/fase2_atelie.tscn
  - scenes/world2/fase2_atelie.gd
  - scenes/world2/fase3_madrugada.tscn
  - scenes/world2/fase3_madrugada.gd
  - scenes/world2/impressora_raivosa.tscn
  - scenes/world2/impressora_raivosa.gd
  - scenes/world2/maquete_rustica.tscn
  - scenes/world2/tfg_item.tscn
  - scenes/world2/tfg_item.gd
  - scenes/world2/renato_cafe_npc.tscn
  - scenes/world2/renato_cafe_npc.gd
requirements: [POWER-01, BOSS-02]
autonomous: true
must_haves:
  truths:
    - "Player can traverse fase1_campus linear path without crashes; enemies reset on death"
    - "Player can collect 5 TFG items across the 3 phases; items persist in itens_tfg_mundo2[]"
    - "Impressora Raivosa fires projectiles periodically and dies on stomp"
    - "Maquete Rústica acts as floor trap (1 damage on contact) and doesn't die"
    - "Renato NPC in fase3_madrugada heals +1 HP and has dialogue on proximity"
    - "Exit trigger in each fase loads next scene via SceneTransition"
  artifacts:
    - path: "scenes/world2/fase1_campus.tscn"
      provides: "Linear playable phase with platform layout, enemies, items, checkpoint, exit"
      contains: "Player, Enemy groups, ExitTrigger, Checkpoint"
    - path: "scenes/world2/fase1_campus.gd"
      provides: "Phase controller (enemy reset, respawn, exit)"
      exports: ["_reset_enemies", "_on_exit_trigger_body_entered"]
    - path: "scenes/world2/impressora_raivosa.gd"
      provides: "Projectile-spawning enemy (stomp-killable)"
      exports: ["die", "reset_to_origin"]
    - path: "scenes/world2/tfg_item.gd"
      provides: "Collectible item persistence (itens_tfg_mundo2[])"
      exports: ["_on_body_entered"]
    - path: "scenes/world2/renato_cafe_npc.gd"
      provides: "NPC with healing dialogue in fase3 (+1 HP)"
      exports: ["_on_dialogue_zone_body_entered"]
  key_links:
    - from: "fase1_campus.gd"
      to: "impressora_raivosa.gd"
      via: "_reset_enemies() calls reset_to_origin() on all enemies"
      pattern: "get_nodes_in_group.*enemies"
    - from: "tfg_item.gd"
      to: "save_manager.gd"
      via: "SaveManager.current_save[\"itens_tfg_mundo2\"]"
      pattern: "itens_tfg_mundo2"
    - from: "renato_cafe_npc.gd"
      to: "player.gd"
      via: "player.heal(1)"
      pattern: "has_method.*heal"
---

<objective>
**Vertical Slice: Playable World 2 Phases + Enemies + Items** (POWER-01, BOSS-02 dependencies)

Implement the three playable fases of Mundo 2 (campus, ateliê, madrugada) with the three enemy types (Impressora Raivosa, Maquete Rústica, Professor Careca), 5 TFG collectible items, checkpoint healing, and Renato NPC café healing in fase3. This builds the complete content scaffold that feeds into the boss (Plan 05).

**Purpose:** Without playable phases, the boss gate (minimum 3 TFG items) cannot be tested. This vertical slice establishes the full chain: player navigates → collects items → fights boss.

**Output:** Three linear playable fases, 5 collectible TFG items persisted, enemy reset on death, Renato heals in fase3, all integrated with the power and HP systems from Plans 01-02.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md — BOSS-02 (gate: ≥3 items), POWER-01 (powers available after unlock)
@.planning/ROADMAP.md — Phase 4 success criteria (fases, checkpoints, respawn <500ms)
@.planning/phases/04-mundo-2-a-faculdade/04-CONTEXT.md — D-10 to D-13 (enemy specs), D-15, D-16 (TFG items)
@.planning/phases/04-mundo-2-a-faculdade/04-RESEARCH.md — Don't Hand-Roll section, Common Pitfalls 1, 3, 4
@.planning/phases/04-mundo-2-a-faculdade/04-PATTERNS.md — fase1_campus.gd (lines 84-130, exact pattern), impressora_raivosa.gd (lines 304-350, reskin malandro), tfg_item.gd (lines 482-522), renato_cafe_npc.gd (lines 525-584)
@.planning/phases/03-mundo-1-osasco-vertical-slice-completo/03-03-SUMMARY.md — reference implementation (fase1_rua, enemies, items)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fase scenes (fase1_campus, fase2_atelie, fase3_madrugada)</name>
  <files>scenes/world2/fase1_campus.tscn, scenes/world2/fase1_campus.gd, scenes/world2/fase2_atelie.tscn, scenes/world2/fase2_atelie.gd, scenes/world2/fase3_madrugada.tscn, scenes/world2/fase3_madrugada.gd</files>
  <read_first>
    - scenes/world1/fase1_rua.tscn (reference layout: Player, platforms, enemies, checkpoint, exit)
    - scenes/world1/fase1_rua.gd (reference controller logic, enemy reset, respawn, exit trigger)
  </read_first>
  <action>
    **Create three playable fase scenes by copying the Mundo 1 fase pattern.**

    For each fase (fase1_campus, fase2_atelie, fase3_madrugada):

    1. **Copy fase1_rua.tscn to {fase}.tscn:**
       - Duplicate node structure: Player, Platforms (StaticBody2D), Enemies group, Checkpoint, ExitTrigger, TileMapLayer (if present)
       - Keep Player positioned at (100, 100) [adjusted per fase layout preference]
       - Place Checkpoint ~80% through level
       - Place ExitTrigger at level end
       - Instance enemies: Malandro → Impressora Raivosa (fase1, fase2, fase3), Maquete Rústica (as floor traps, fase1 and fase2)
       - No Professor Careca in fase1/fase2 (only fase3)

    2. **Copy fase1_rua.gd to {fase}.gd:**
       - Change @onready assignments to match new scene node names
       - In _on_exit_trigger_body_entered(), update SceneTransition.go_to() path:
         - fase1_campus.gd → "res://scenes/world2/fase2_atelie.tscn"
         - fase2_atelie.gd → "res://scenes/world2/fase3_madrugada.tscn"
         - fase3_madrugada.gd → "res://scenes/world2/boss_tfg.tscn"
       - All other logic identical (player.died signal, checkpoint, enemy reset)

    3. **Fase-specific layout** (Claude's discretion per D-03 palette):
       - fase1_campus: Outdoor campus courtyard (bege/creme, grassy), ~300px length, 3-4 Impressora, 1-2 Maquete
       - fase2_atelie: Interior ateliê (bege walls, workbenches), ~350px, 3-4 Impressora, 2-3 Maquete
       - fase3_madrugada: Lab/studio late night (darker bege, papers scattered), ~300px, 2 Impressora, 1 Professor Careca (static), Renato NPC

    4. **Reference:** PATTERNS.md lines 84-130 show exact fase1_campus.gd logic. Copy verbatim, only change exit target path.

    **Note:** Tileset and enemy placement are visual; placeholder geometric sprites acceptable per D-03. Artist refines later.
  </action>
  <verify>
    <automated>
      test -f /Users/renatojaf/jogo-natalia/scenes/world2/fase1_campus.tscn
      # Expected: file exists
      grep -c "go_to.*fase2_atelie" /Users/renatojaf/jogo-natalia/scenes/world2/fase1_campus.gd
      # Expected output: 1
      grep -c "_reset_enemies" /Users/renatojaf/jogo-natalia/scenes/world2/fase1_campus.gd
      # Expected output: 1
    </automated>
  </verify>
  <done>
    Three fase scenes created (fase1_campus, fase2_atelie, fase3_madrugada). Each has Player, platforms, enemies, checkpoint, exit trigger. Controllers implement respawn <500ms and enemy reset on death. Exit transitions to next fase.
  </done>
</task>

<task type="auto">
  <name>Task 2: Impressora Raivosa (projectile-spawning enemy)</name>
  <files>scenes/world2/impressora_raivosa.tscn, scenes/world2/impressora_raivosa.gd</files>
  <read_first>
    - scenes/world1/malandro.tscn (reference: CharacterBody2D, AnimatedSprite2D, StompZone, BodyHitbox colliders)
    - scenes/world1/malandro.gd (reference: stomp detection, die(), reset_to_origin(), sprite animation)
  </read_first>
  <action>
    **Create Impressora Raivosa by adapting Malandro but replacing patrol with projectile fire.**

    1. **Copy malandro.tscn to impressora_raivosa.tscn:**
       - Keep node structure: CharacterBody2D (root), AnimatedSprite2D, CollisionShape2D, StompZone, BodyHitbox
       - Change sprite reference from "malandro.png" to "impressora_raivosa.png" (printer sprite, TBD asset)
       - Keep collision shapes unchanged (stomp zone and body hitbox)

    2. **Create impressora_raivosa.gd** (based on malandro.gd with fire logic):
       - Import all stomp detection code from malandro (lines 55-72 _on_stomp_zone_body_entered, lines 75-88 die/reset)
       - Add @export fire_rate: float = 2.0 (fire every 2 seconds per D-11)
       - Add @export projectile_scene: PackedScene (ref to paper projectile, TBD)
       - In _physics_process(), replace patrol logic with fire timer:
         - _fire_timer -= delta
         - If _fire_timer <= 0: _spawn_projectile(), _fire_timer = fire_rate
       - _spawn_projectile(): Instance projectile_scene, position at enemy center + offset, set velocity.x (right direction)
       - Keep die() and reset_to_origin() identical to malandro

    3. **Reference:** PATTERNS.md lines 304-350 show exact impressora_raivosa.gd implementation. Copy stomp detection verbatim, adapt fire logic.

    **Note:** Projectile_scene is placeholder (will be paper projectile in Plan 03b). For now, spawn and discard immediately or skip if null.
  </action>
  <verify>
    <automated>
      test -f /Users/renatojaf/jogo-natalia/scenes/world2/impressora_raivosa.tscn
      # Expected: file exists
      grep -c "func _spawn_projectile" /Users/renatojaf/jogo-natalia/scenes/world2/impressora_raivosa.gd
      # Expected output: 1
      grep -c "_stomped_this_frame" /Users/renatojaf/jogo-natalia/scenes/world2/impressora_raivosa.gd
      # Expected output: 3 (declaration, guard, set)
    </automated>
  </verify>
  <done>
    impressora_raivosa.tscn and impressora_raivosa.gd created. Enemy fires projectiles every 2s, dies on stomp, resets to origin on player death. Stomp detection is double-stage (StompZone + BodyHitbox).
  </done>
</task>

<task type="auto">
  <name>Task 3: Maquete Rústica (static floor trap)</name>
  <files>scenes/world2/maquete_rustica.tscn</files>
  <read_first>
    - scenes/world1/static_obstacle.tscn (reference: StaticBody2D, AnimatedSprite2D, Area2D damage zone)
  </read_first>
  <action>
    **Create Maquete Rústica by copying StaticObstacle without changes (reskin only).**

    1. **Copy static_obstacle.tscn to maquete_rustica.tscn:**
       - Keep all nodes and logic identical (StaticBody2D, Area2D damage zone, sprite)
       - Change sprite reference from "static_obstacle.png" to "maquete_rustica.png" (fragmented model sprite, TBD asset)
       - Keep collision and damage logic as-is (1 damage on contact, no death)

    2. **No .gd file needed** — maquete_rustica uses the existing static_obstacle.gd pattern (inherited from StaticBody2D parent if referenced, or reuse parent script).

    3. **Reference:** PATTERNS.md mentions maquete_rustica is a sprite variation of static_obstacle (lines 29, 39). Exact duplication is correct.

    **Note:** Maquete does not patrol, does not die, does not respawn. It's a permanent hazard like spike floor.
  </action>
  <verify>
    <automated>
      test -f /Users/renatojaf/jogo-natalia/scenes/world2/maquete_rustica.tscn
      # Expected: file exists
      grep -c "StaticBody2D" /Users/renatojaf/jogo-natalia/scenes/world2/maquete_rustica.tscn
      # Expected output: 1
    </automated>
  </verify>
  <done>
    maquete_rustica.tscn created. Floor trap (1 damage on contact, no death, no reset).
  </done>
</task>

<task type="auto">
  <name>Task 4: TFG Items (5 collectibles)</name>
  <files>scenes/world2/tfg_item.tscn, scenes/world2/tfg_item.gd</files>
  <read_first>
    - scenes/world1/prova_item.tscn (reference: Area2D, AnimatedSprite2D, CPUParticles2D, collision)
    - scenes/world1/prova_item.gd (reference: collection logic, SaveManager persistence, dedup guard, despawn)
  </read_first>
  <action>
    **Create TFG item by copying ProvaItem but changing persistence key and SFX.**

    1. **Copy prova_item.tscn to tfg_item.tscn:**
       - Keep all nodes (Area2D, AnimatedSprite2D, CPUParticles2D, CollisionShape2D)
       - Change sprite reference to "tfg_item.png" (generic item sprite, TBD; can vary per item type later)
       - Keep particle effect and collision unchanged

    2. **Create tfg_item.gd** (based on prova_item.gd):
       - Add @export prova_id: String = "tfg_pesquisa_campo" (unique ID per TFG item, list per D-16)
       - In _on_body_entered():
         - Check SaveManager.current_save.get("itens_tfg_mundo2", []) (defensive read)
         - Guard against duplicate: if prova_id not in itens: append and save
         - Play SFX: AudioManager.play_sfx("prova_tfg_coletada") [registered in Plan 02]
         - Show particle effect, hide sprite, set collision disabled, despawn after 0.25s
       - Copy pattern from prova_item.gd lines 1-27 exactly, only change key names

    3. **Five item instances** to place across fases:
       - Pesquisa de Campo (fase1)
       - Masterplan Urbano (fase1)
       - Complexo Misto (fase2)
       - Análise de Fluxos Humanos (fase2)
       - Princípios de Sustentabilidade (fase3)
       Per D-16, these are the exact 5 items. Placement per Claude discretion.

    4. **Reference:** PATTERNS.md lines 482-522 show exact tfg_item.gd adaptation from prova_item.gd.
  </action>
  <verify>
    <automated>
      test -f /Users/renatojaf/jogo-natalia/scenes/world2/tfg_item.tscn
      # Expected: file exists
      grep -c "itens_tfg_mundo2" /Users/renatojaf/jogo-natalia/scenes/world2/tfg_item.gd
      # Expected output: 2 or more (read, write)
      grep -c "prova_tfg_coletada" /Users/renatojaf/jogo-natalia/scenes/world2/tfg_item.gd
      # Expected output: 1
    </automated>
  </verify>
  <done>
    tfg_item.tscn and tfg_item.gd created. Items persist to itens_tfg_mundo2[]. Dedup guard and despawn timer prevent re-collection. Five items placed across three fases.
  </done>
</task>

<task type="auto">
  <name>Task 5: Renato café NPC (fase3 healer)</name>
  <files>scenes/world2/renato_cafe_npc.tscn, scenes/world2/renato_cafe_npc.gd</files>
  <read_first>
    - scenes/world1/renato_npc.tscn (reference: StaticBody2D, sprite, DialogueZone, Prompt label)
    - scenes/world1/renato_npc.gd (reference: dialogue zone, input handling, Dialogic integration)
  </read_first>
  <action>
    **Create Renato café NPC in fase3 by copying renato_npc but adding healing mechanic.**

    1. **Copy renato_npc.tscn to renato_cafe_npc.tscn:**
       - Keep all nodes: StaticBody2D, AnimatedSprite2D (Renato sprite), DialogueZone (Area2D), Prompt label
       - Keep positioning (static, no movement)

    2. **Create renato_cafe_npc.gd** (based on renato_npc.gd with heal):
       - Copy dialogue zone and input handling from renato_npc.gd (lines 1-32)
       - In _unhandled_input(), after Dialogic.start("renato_cafe_fase3"):
         - await Dialogic.timeline_ended
         - Get player reference: get_tree().first_child_of_type(CharacterBody2D)
         - Call player.heal(1) if has_method ("heal")
         - Play SFX: AudioManager.play_sfx("checkpoint") [healing sound per D-08]
         - Set input as handled
       - Copy pattern from PATTERNS.md lines 525-584 exactly

    3. **One instance** placed in fase3_madrugada.tscn at ~200px horizontal position (mid-level, easy to find).

    4. **Reference:** PATTERNS.md lines 525-584 show exact renato_cafe_npc.gd implementation with healing addition.

    **Note:** This is the only NPC healing source in Mundo 2; checkpoint is the primary heal. Both grant +1 HP per D-08.
  </action>
  <verify>
    <automated>
      test -f /Users/renatojaf/jogo-natalia/scenes/world2/renato_cafe_npc.tscn
      # Expected: file exists
      grep -c "player.heal" /Users/renatojaf/jogo-natalia/scenes/world2/renato_cafe_npc.gd
      # Expected output: 1
      grep -c "renato_cafe_fase3" /Users/renatojaf/jogo-natalia/scenes/world2/renato_cafe_npc.gd
      # Expected output: 1
    </automated>
  </verify>
  <done>
    renato_cafe_npc.tscn and renato_cafe_npc.gd created. NPC heals +1 HP on dialogue end in fase3. SFX plays on heal.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Fase controller ↔ Enemy group | Phase resets enemies via get_tree().get_nodes_in_group("enemies"); assumes all enemies are registered |
| TFG item ↔ SaveManager | Items persisted as String IDs; duplicate check prevents over-save |
| Impressora projectile ↔ Player | Projectile spawned at enemy position but offset by 12px to avoid immediate collision (handled in Plan 03b) |
| Renato ↔ Player | NPC heals player; healing is one-time event per dialogue, not repeatable per visit |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-09 | **T** (Tampering) | Enemy reset on death | mitigate | reset_to_origin() restores enemy to _origin position (cached at spawn); no mid-battle modification possible |
| T-04-10 | **T** (Tampering) | Item dedup guard | mitigate | Dedup check `if prova_id not in itens` prevents duplicate collection; despawn timer prevents re-trigger |
| T-04-11 | **D** (Denial) | Renato dialogue re-trigger | accept | NPC interaction requires Z key press; dialogue can be repeated but heal only happens once (if coded one-shot in future) |
| T-04-12 | **I** (Information Disclosure) | TFG item collection UI | accept | Item collection is visible to player (intentional feedback); counts toward gate progress |

**Rationale:** Phase 4 is single-player, offline. No network or multi-player concerns. Enemy and item state is transient (not persisted save-wise, reset on death/respawn). Healing is symmetric (Checkpoint and Renato both +1).
</threat_model>

<verification>
**Phase 4, Plan 03 Completion Checklist:**

1. Playable phases
   - [ ] fase1_campus, fase2_atelie, fase3_madrugada exist with layout, enemies, items, checkpoint, exit
   - [ ] Each fase controller resets enemies on player death
   - [ ] Exit triggers load next fase (<500ms transition per WORLD-05)

2. Enemy implementations
   - [ ] Impressora Raivosa fires projectiles every ~2s
   - [ ] Impressora dies on stomp (inherits from malandro)
   - [ ] Maquete Rústica acts as floor trap (1 damage, no death)
   - [ ] All enemies registered in "enemies" group for reset

3. TFG items
   - [ ] 5 items placed: Pesquisa, Masterplan, Complexo, Fluxos, Sustentabilidade
   - [ ] Items persisted to itens_tfg_mundo2[] on collection
   - [ ] Dedup guard prevents re-collection after despawn
   - [ ] SFX plays on collection (prova_tfg_coletada)

4. Renato NPC
   - [ ] Located in fase3_madrugada
   - [ ] Dialogue triggers on player proximity (DialogueZone)
   - [ ] Heals +1 HP on dialogue end
   - [ ] Checkpoint SFX plays on heal

**Manual Playtest (checkpoint:human-verify in next wave):**
- Load fase1_campus, walk to end without dying → collects items, checkpoint heals, exit loads fase2
- Walk through all 3 fases, collect at least 3 items → saves to itens_tfg_mundo2
- Get hit by Impressora, confirm 1 damage and knockback
- Stand on Maquete, confirm 1 damage and no death animation
- Meet Renato in fase3, trigger dialogue → see +1 HP
</verification>

<success_criteria>
**Plan 03 Success:**

1. Three linear playable fases exist (fase1_campus, fase2_atelie, fase3_madrugada) with platform layout, enemies, items, and checkpoints.
2. Player can traverse all three fases sequentially (exit trigger → next fase).
3. All 5 TFG items are collectible and persist to SaveManager.current_save["itens_tfg_mundo2"].
4. Enemy reset on player death works correctly; enemies return to origin positions.
5. Impressora Raivosa fires projectiles and is stompable.
6. Maquete Rústica deals 1 damage on contact (untouchable).
7. Renato NPC in fase3 heals +1 HP on dialogue interaction.
8. All TFG items, enemies, and NPCs are registered correctly with groups and signals.

**This plan unblocks:**
- Plan 04 (Professor Careca homing hazard) — depends on phase layout
- Plan 05 (Boss + Sketch projectile + HP HUD) — depends on 3+ items gated and phases complete
- Plan 06 (Opening cutscene + end-to-end playtest) — depends on all phases playable
</success_criteria>

<output>
After completion, create `.planning/phases/04-mundo-2-a-faculdade/04-03-SUMMARY.md` documenting:
- Three fase scenes and controllers (respawn, enemy reset, transitions)
- Impressora Raivosa implementation and placement
- Maquete Rústica static obstacle
- TFG item persistence and placement (5 items across 3 fases)
- Renato café NPC in fase3 with healing
- Integration points for boss gate (Plan 05)
</output>
