---
phase: 04
plan: 05
type: execute
wave: 3
depends_on: [04-01, 04-02, 04-03, 04-04]
files_modified:
  - scenes/world2/boss_tfg.tscn
  - scenes/world2/boss_tfg.gd
  - scenes/world2/mundo2_abertura.tscn
  - scenes/world2/mundo2_abertura.gd
  - scenes/world2/world2_end.tscn
  - scenes/world2/world2_end.gd
  - scenes/world1/boss_pai.gd
  - dialogic/timelines/mundo2_abertura.dtl
  - dialogic/timelines/boss_abertura_tfg.dtl
  - dialogic/timelines/boss_vitoria_tfg.dtl
  - dialogic/characters/ProfessorPerpetuo.dch
requirements: [BOSS-02, POWER-01]
autonomous: false
must_haves:
  truths:
    - "Player cannot enter boss fight without ≥3 TFG items (gate check before banca)"
    - "Boss quality bar starts at 0%, rises on correct dialogue and item presentation (+10-20%)"
    - "Professor Perpétuo increases difficulty by raising quality threshold mid-fight (+15%)"
    - "Victory at 100% quality unlocks Sketch and Amor powers retroactively, saves progress"
    - "Defeat on <70% quality reloads boss scene, player tries again with items intact"
    - "Mundo 2 opening cutscene (abertura) displays narrative, auto-transitions to fase1"
  artifacts:
    - path: "scenes/world2/boss_tfg.tscn"
      provides: "Boss arena with HUD (quality bar), 3 NPC nodes, player spawn"
      contains: "Node2D, CanvasLayer, ColorRect, Label, Player"
    - path: "scenes/world2/boss_tfg.gd"
      provides: "Quality bar FSM, gate logic (≥3 items), Dialogic signal handling, victory/defeat paths"
      exports: ["_start_boss_sequence", "_trigger_victory", "_trigger_game_over"]
    - path: "scenes/world2/mundo2_abertura.tscn"
      provides: "Opening cutscene scene (black background, skip button, Dialogic display)"
      contains: "Node2D, CanvasLayer, Button, Dialogic viewport"
    - path: "scenes/world2/mundo2_abertura.gd"
      provides: "Cutscene auto-start, skip logic, transition to fase1_campus"
      exports: ["_ready", "_on_skip_pressed"]
    - path: "scenes/world2/world2_end.tscn"
      provides: "End-of-world placeholder (victory screen, menu button)"
      contains: "Node2D, Label, Button"
    - path: "scenes/world2/world2_end.gd"
      provides: "Menu transition on button press"
      exports: ["_on_menu_pressed"]
    - path: "dialogic/timelines/mundo2_abertura.dtl"
      provides: "Opening narrative (tone: exaustão + esperança)"
    - path: "dialogic/timelines/boss_abertura_tfg.dtl"
      provides: "Banca intro + dialogue choices (correct/wrong paths with signals)"
    - path: "dialogic/timelines/boss_vitoria_tfg.dtl"
      provides: "Victory narrative (aprovação, poder desbloqueado)"
    - path: "dialogic/characters/ProfessorPerpetuo.dch"
      provides: "Character definition for Professor Perpétuo (sprite, name, color)"
  key_links:
    - from: "boss_tfg.gd"
      to: "save_manager.gd"
      via: "itens_tfg_mundo2.size() >= 3 gate check, worlds_completed update"
      pattern: "itens_tfg_mundo2|worlds_completed"
    - from: "boss_tfg.gd"
      to: "dialogic"
      via: "Dialogic.signal_event → quality adjustments"
      pattern: "choice_correct|choice_wrong|professor_increases_requirement"
    - from: "boss_tfg.gd"
      to: "player.gd"
      via: "player.unlock_power(\"sketch\"), unlock_power(\"amor\")"
      pattern: "unlock_power"
    - from: "mundo2_abertura.gd"
      to: "scene_transition.gd"
      via: "SceneTransition.go_to(fase1_campus)"
      pattern: "SceneTransition.go_to"
---

<objective>
**Vertical Slice: Boss Fight + Opening Cutscene + World End** (BOSS-02, POWER-01)

Implement the complete boss sequence: Mundo 2 opening cutscene (narrative setup), Professor Perpétuo banca fight with quality bar mechanics (gate on 3+ items, escalating difficulty, victory/defeat paths), power unlock on victory (Sketch and Amor retroactively), and world end placeholder. This is the capstone of Mundo 2, integrating all prior systems and setting the stage for world revisit with new powers.

**Purpose:** The boss is the moment where player proves mastery of Mundo 2 challenges and receives narrative payoff (academic approval) plus mechanical reward (power unlock). Without this, collected items are meaningless and powers are unearned. This plan ties together Plans 01-04 into a complete vertical slice experience.

**Output:** Fully playable Mundo 2 from abertura → 3 fases → boss → victory/defeat → end screen, with persistent power unlock and save progression.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md — BOSS-02 (gate ≥3 items, quality bar escalation, victory unlock), POWER-01 (Sketch + Amor unlock)
@.planning/ROADMAP.md — Phase 4 success (complete mundo2 vertical slice, powers persist in save)
@.planning/phases/04-mundo-2-a-faculdade/04-CONTEXT.md — D-14 to D-20 (boss mechanics), D-21 (Amor unlock), D-22 (Sketch unlock)
@.planning/phases/04-mundo-2-a-faculdade/04-RESEARCH.md — Pitfall 2 (quality threshold capping), assumption A2 (Dialogic signal ordering)
@.planning/phases/04-mundo-2-a-faculdade/04-PATTERNS.md — boss_tfg.gd (lines 134-240, quality bar pattern), mundo2_abertura.gd (lines 41-80), world2_end.gd (lines 587-602)
@.planning/phases/03-mundo-1-osasco-vertical-slice-completo/03-04-SUMMARY.md — Reference boss implementation (boss_pai)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Boss TFG scene and quality bar FSM</name>
  <files>scenes/world2/boss_tfg.tscn, scenes/world2/boss_tfg.gd</files>
  <read_first>
    - scenes/world1/boss_pai.tscn (reference: scene layout, HUD nodes, NPC positions)
    - scenes/world1/boss_pai.gd (reference: gate logic, Dialogic signal handling, victory/defeat paths, trust bar HUD)
    - autoloads/save_manager.gd (verify worlds_completed key exists from Phase 3)
  </read_first>
  <action>
    **Create boss_tfg.tscn and boss_tfg.gd by adapting boss_pai pattern with quality bar instead of trust bar.**

    1. **Create boss_tfg.tscn:**
       - Copy boss_pai.tscn node structure
       - Root: Node2D (boss scene)
       - Children: Player (at spawn position ~100, 100), 3 NPC nodes (Professor center, 2 students flanking), Checkpoint (optional), CanvasLayer for HUD
       - HUD layer: ColorRect (quality bar background), ColorRect (quality bar fill), Label (percentage text), Label (title "Qualidade")
       - Position quality bar at screen top-center (160, 20), 200px wide, 12px tall
       - Save node paths as @onready (quality_bar_fill, quality_pct_label, player)

    2. **Create boss_tfg.gd:**
       - Copy boss_pai.gd structure (lines 1-45)
       - Replace _trust with _quality (var _quality: float = 0.0)
       - Replace TRUST_MAX with QUALITY_MAX: float = 100.0
       - Add _quality_threshold: float = 70.0 (initial pass threshold, raised by Professor)
       - In _ready(): call _start_boss_sequence()

       **Gate and item presentation** (lines 46-60):
       - In _start_boss_sequence():
         - var itens = SaveManager.current_save.get("itens_tfg_mundo2", [])
         - If itens.size() < 3: await _show_blocking_dialogue(), return (gate blocks entry)
         - Else: initialize _quality = 0.0, loop through itens, await _show_item_card(item), add_quality(20.0)
         - After items: connect Dialogic.signal_event, start "boss_abertura_tfg" timeline

       **Signal handling** (lines 61-75):
       - In _on_dialogic_signal(argument):
         - "choice_correct": add_quality(10.0)
         - "choice_wrong": add_quality(-15.0), AudioManager.play_sfx("dialogo_errado")
         - "professor_increases_requirement": _quality_threshold = minf(_quality_threshold + 15.0, QUALITY_MAX)

       **Victory and defeat** (lines 76-110):
       - add_quality(amount): _quality = clampf(...), _update_hud(), check conditions
         - If _quality < _quality_threshold: _trigger_game_over()
         - Elif _quality >= QUALITY_MAX: _trigger_victory()
       - _trigger_victory(): AudioManager.play_sfx("vitoria"), player.unlock_power("sketch"), player.unlock_power("amor"), SaveManager marks mundo2 complete (add "mundo2" to worlds_completed[]), starts "boss_vitoria_tfg" timeline, transitions to mundo2_end
       - _trigger_game_over(): red flash, reload boss_tfg.tscn

       **HUD update** (lines 111-125):
       - _update_hud(): Set quality_bar_fill.custom_minimum_size.x = 200.0 * (_quality / QUALITY_MAX)
         - Color progression: <20% red (#CC2200), 70-99% green (#228B22), ≥100% gold (#FFD700)
         - Update percentage label

    3. **Critical fixes** (per PATTERNS.md and Phase 3):
       - Connect Dialogic.signal_event BEFORE calling Dialogic.start()
       - Disconnect after timeline ends
       - Cap threshold with minf() to prevent >100% requirement
       - Verify worlds_completed key exists in SaveManager.current_save (added in Plan 01 v3 migration)

    4. **Reference:** PATTERNS.md lines 134-240 show exact boss_tfg.gd implementation. Copy pattern verbatim, swap trust→quality.

    **Note:** _show_blocking_dialogue() and _show_item_card() are stubs for now (async waits). Dialogic messages can be minimal.
  </action>
  <verify>
    <automated>
      test -f /Users/renatojaf/jogo-natalia/scenes/world2/boss_tfg.tscn
      # Expected: file exists
      grep -c "var _quality: float" /Users/renatojaf/jogo-natalia/scenes/world2/boss_tfg.gd
      # Expected output: 1
      grep -c "itens_tfg_mundo2" /Users/renatojaf/jogo-natalia/scenes/world2/boss_tfg.gd
      # Expected output: 2 or more (gate check, presentation)
      grep -c "minf.*_quality_threshold.*QUALITY_MAX" /Users/renatojaf/jogo-natalia/scenes/world2/boss_tfg.gd
      # Expected output: 1 (cap threshold)
      grep -c "worlds_completed" /Users/renatojaf/jogo-natalia/scenes/world2/boss_tfg.gd
      # Expected output: 1 or more (mundo2 completion marking)
    </automated>
  </verify>
  <done>
    boss_tfg.tscn with quality bar HUD created. boss_tfg.gd implements quality bar FSM with gate (≥3 items), escalation, victory/defeat, and power unlock. Threshold capped at QUALITY_MAX. Victory marks mundo2 complete in worlds_completed[].
  </done>
</task>

<task type="auto">
  <name>Task 2: Mundo 2 opening cutscene (abertura)</name>
  <files>scenes/world2/mundo2_abertura.tscn, scenes/world2/mundo2_abertura.gd</files>
  <read_first>
    - scenes/world1/mundo1_abertura.tscn (reference: scene layout, skip button, Dialogic integration)
    - scenes/world1/mundo1_abertura.gd (reference: auto-start, skip logic, transition)
  </read_first>
  <action>
    **Create mundo2_abertura by copying mundo1_abertura pattern.**

    1. **Copy mundo1_abertura.tscn to mundo2_abertura.tscn:**
       - Keep node structure: Node2D (root), CanvasLayer (UI), Button (skip), Label (title)
       - No changes needed to layout (reuses pattern)

    2. **Create mundo2_abertura.gd:**
       - Copy mundo1_abertura.gd logic (lines 1-27)
       - Change TIMELINE_ID from "mundo1_abertura" to "mundo2_abertura"
       - Change final transition from fase1_rua to "res://scenes/world2/fase1_campus.tscn"
       - Copy skip button and Dialogic handling verbatim

    3. **Reference:** PATTERNS.md lines 41-80 show exact mundo2_abertura.gd. Change only timeline ID and exit path.

    **Note:** Cutscene can have black background + white text (minimal visual), Dialogic handles narrative display.
  </action>
  <verify>
    <automated>
      test -f /Users/renatojaf/jogo-natalia/scenes/world2/mundo2_abertura.tscn
      # Expected: file exists
      grep -c "mundo2_abertura" /Users/renatojaf/jogo-natalia/scenes/world2/mundo2_abertura.gd
      # Expected output: 2 (TIMELINE_ID, timeline start)
      grep -c "fase1_campus" /Users/renatojaf/jogo-natalia/scenes/world2/mundo2_abertura.gd
      # Expected output: 1 (transition path)
    </automated>
  </verify>
  <done>
    mundo2_abertura.tscn and mundo2_abertura.gd created. Opening cutscene auto-starts timeline, allows skip, transitions to fase1_campus.
  </done>
</task>

<task type="auto">
  <name>Task 3: World 2 end screen placeholder</name>
  <files>scenes/world2/world2_end.tscn, scenes/world2/world2_end.gd</files>
  <read_first>
    - scenes/world1/world1_end.tscn (reference: layout, menu button)
    - scenes/world1/world1_end.gd (reference: button handler, menu transition)
  </read_first>
  <action>
    **Create world2_end by copying world1_end pattern (no changes).**

    1. **Copy world1_end.tscn to world2_end.tscn:**
       - Keep node structure: Node2D, Label ("Parabéns!" or victory message), Button ("Menu")
       - No changes to logic

    2. **Copy world1_end.gd to world2_end.gd:**
       - Verbatim copy (transitions to main_menu on button press)

    3. **Reference:** PATTERNS.md lines 587-602 show exact world2_end.gd. No changes needed.

    **Note:** Placeholder can show victory message or remain blank; main_menu.gd routes to mundo2_abertura for next play.
  </action>
  <verify>
    <automated>
      test -f /Users/renatojaf/jogo-natalia/scenes/world2/world2_end.tscn
      # Expected: file exists
      grep -c "main_menu" /Users/renatojaf/jogo-natalia/scenes/world2/world2_end.gd
      # Expected output: 1 (menu transition)
    </automated>
  </verify>
  <done>
    world2_end.tscn and world2_end.gd created. End screen placeholder with menu button.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    - Mundo 2 complete vertical slice: mundo2_abertura → fase1_campus → fase2_atelie → fase3_madrugada → boss_tfg → world2_end
    - Power system (Sketch, Amor) fully integrated and unlockable
    - HP system (3 PV) with checkpoint healing
    - TFG item persistence and boss gate (≥3 items)
  </what-built>
  <how-to-verify>
    **Full end-to-end playthrough in Godot editor:**

    1. **Opening:** Launch mundo2_abertura.tscn, watch opening narrative, press skip or wait for auto-transition
    2. **Phase 1:** In fase1_campus, collect at least 1 item (Pesquisa or Masterplan), walk to exit without dying, exit loads fase2
    3. **Phase 2:** In fase2_atelie, take 1 damage (hp: 3→2 shown in hearts), collect ≥1 item, reach checkpoint (hp: 2→3), exit to fase3
    4. **Phase 3:** In fase3_madrugada, meet Renato NPC, interact (dialogue appears, +1 HP), collect final item to reach ≥3 items total
    5. **Boss gate:** Enter boss_tfg.tscn (if <3 items, blocking dialogue appears; try with 3+ items), banca starts
    6. **Boss fight:** Present TFG items (quality bar jumps +20% each), answer ≥2 questions correctly (+10% each), reach 100% → victory dialogue → Sketch icon appears
    7. **Power unlock:** After victory, press Z in any scene → Sketch projectile fires (visible confirmation of unlock)
    8. **Save/load test:** Close game after phase completion, reopen → load save shows powers_unlocked includes "sketch" and "amor", worlds_completed includes "mundo2"

    **Expected outcomes:**
    - No crashes during phase progression
    - Items persist across scenes and death/respawn
    - HP system displays correctly (hearts in top-left)
    - Boss gate blocks with <3 items, allows with ≥3
    - Quality bar color transitions (red→green→gold)
    - Powers unlock retroactively and persist in save
    - worlds_completed saved and loaded correctly
    - Rapid Z presses throttled by 0.5s cooldown

    **Record any issues and continue** (issues do not block execution of remaining tasks, but do inform later adjustment phase).
  </how-to-verify>
  <resume-signal>
    Type one of:
    - "approved" — vertical slice plays without critical issues
    - "approved with issues: [list]" — works but minor bugs (e.g., HUD positioning, cosmetics)
    - "blocked: [issue]" — game-breaking issue prevents playthrough (e.g., crash, infinite loop)
  </resume-signal>
</task>

<task type="auto">
  <name>Task 4: Dialogic timelines and character definition</name>
  <files>dialogic/timelines/mundo2_abertura.dtl, dialogic/timelines/boss_abertura_tfg.dtl, dialogic/timelines/boss_vitoria_tfg.dtl, dialogic/characters/ProfessorPerpetuo.dch</files>
  <read_first>
    - dialogic/timelines/mundo1_abertura.dtl (reference: narrative structure, signal events)
    - dialogic/timelines/boss_abertura.dtl (reference: boss dialogue, choice handling)
    - dialogic/characters/Luis.dch (reference: character definition format)
  </read_first>
  <action>
    **Create Dialogic timelines and character definition for Mundo 2 boss sequence.**

    1. **mundo2_abertura.dtl (opening narrative):**
       - Use Dialogic 2 syntax (simple text-based timeline)
       - Write 3-4 narrative lines with tone: exaustão + esperança (per D-02, UI-SPEC copywriting)
       - Example: "Noites sem dormir. Maquetes que desabam... Mas aqui na faculdade, encontrei pessoas que entendem."
       - Optional: Add [Dialogic.Inputs.auto_skip] to enable auto-advance
       - Save as .dtl file (Dialogic text format)

    2. **boss_abertura_tfg.dtl (banca introduction):**
       - Character: ProfessorPerpetuo with intro line
       - Dialogue choices (3-4 options):
         - Choice A (correct): Text appears, [signal_event("choice_correct")] emitted
         - Choice B (wrong): Text appears, [signal_event("choice_wrong")] emitted
         - Choice C (wrong): Text appears, [signal_event("choice_wrong")] emitted
       - Midway: [signal_event("professor_increases_requirement")] — raises difficulty
       - Example structure: "Professor asks about urban inclusion..." → choices → outcome signals

    3. **boss_vitoria_tfg.dtl (victory cutscene):**
       - Character: ProfessorPerpetuo approves
       - Text: "Aprovado! Excelente trabalho na abordagem de inclusão urbana."
       - Colegas comemoram (stage direction or new character)
       - Renato appears (optional)
       - Final: [signal_event("power_unlock_sketch")] or handled by boss_tfg.gd after this timeline

    4. **ProfessorPerpetuo.dch (character definition):**
       - Character name: "Professor Perpétuo"
       - Display name: "Prof. Perpétuo"
       - Color: #8B4513 (brown, academic tone) or custom
       - Sprite reference: (TBD, placeholder OK)
       - Save in dialogic/characters/

    5. **Reference:** CONTEXT.md D-17 names "Professor Perpétuo". PATTERNS.md references boss_abertura.dtl signal events. Follow existing Mundo 1 boss timeline structure.

    **Note:** Dialogic syntax is Godot-specific. Check Dialogic 2 documentation for exact signal_event() syntax. For now, stubs with placeholder signals are acceptable.
  </action>
  <verify>
    <automated>
      test -f /Users/renatojaf/jogo-natalia/dialogic/timelines/mundo2_abertura.dtl
      # Expected: file exists
      test -f /Users/renatojaf/jogo-natalia/dialogic/timelines/boss_abertura_tfg.dtl
      # Expected: file exists
      grep -c "ProfessorPerpetuo" /Users/renatojaf/jogo-natalia/dialogic/characters/ProfessorPerpetuo.dch
      # Expected output: 1 or more
    </automated>
  </verify>
  <done>
    Dialogic timelines created (mundo2_abertura, boss_abertura_tfg, boss_vitoria_tfg). Character definition for Professor Perpétuo created. Signal events for quality adjustments in boss dialogue.
  </done>
</task>

<task type="auto">
  <name>Task 5: Retroactive Amor power unlock in boss_pai.gd</name>
  <files>scenes/world1/boss_pai.gd</files>
  <read_first>
    - scenes/world1/boss_pai.gd (victory path, power unlock logic)
  </read_first>
  <action>
    **Add Amor power unlock to boss_pai.gd victory path (retroactive unlock per D-21).**

    In boss_pai.gd, in _trigger_victory() method (around line 170):
    - Add: player.unlock_power("amor") after any existing power unlocks
    - This retroactively makes Amor available when Mundo 1 boss is defeated, even though it will be unlocked again in Mundo 2 boss (idempotent, no harm in duplicate unlock)

    **Reference:** PATTERNS.md lines 973-991 show power unlock pattern. Add unlock_power("amor") call alongside sketch in boss_tfg.gd.

    **Note:** This is a Phase 4 modification to Phase 3 code. Idempotent (calling unlock twice is safe via the dedup guard in player.unlock_power()).
  </action>
  <verify>
    <automated>
      grep -c "unlock_power.*amor" /Users/renatojaf/jogo-natalia/scenes/world1/boss_pai.gd
      # Expected output: 1 or more
    </automated>
  </verify>
  <done>
    boss_pai.gd modified to unlock Amor power on victory. Amor retroactively available after Mundo 1 completion.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Boss gate ↔ SaveManager | Gate checks itens_tfg_mundo2.size() ≥ 3; items must be in SaveManager or gate blocks |
| Dialogic signals ↔ Quality bar | Boss script listens for "choice_correct", "choice_wrong", "professor_increases_requirement" signals; must match timeline emits |
| Victory path ↔ Power unlock | Victory calls player.unlock_power(); player.gd must have unlock_power() method or crashes |
| SaveManager v2→v3 → Boss gate | Old v2 saves lack itens_tfg_mundo2 key; migration in Plan 01 ensures key exists (set to []), worlds_completed exists from Phase 3 |
| Victory path ↔ World completion | Victory marks "mundo2" in worlds_completed[]; key must exist in SaveManager.current_save |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-17 | **T** (Tampering) | Item gate bypass | mitigate | Gate check is boolean (size >= 3), cannot be spoofed; blocking dialogue prevents boss fight entry without items |
| T-04-18 | **T** (Tampering) | Quality threshold escape | mitigate | Threshold capped at QUALITY_MAX with minf() to prevent >100% impossible-to-win scenario |
| T-04-19 | **I** (Information Disclosure) | Dialogic signals | accept | Signals are game mechanics (dialogue choices → quality changes), visible to player intentionally |
| T-04-20 | **A** (Availability) | Power unlock failure | mitigate | unlock_power() is in player.gd from Plan 01; dedup guard prevents crash on repeat calls |
| T-04-21 | **D** (Denial) | Save corruption on v2→v3 load | mitigate | load_game() in SaveManager handles migration gracefully; old saves loaded and upgraded in-place, no data loss |
| T-04-22 | **D** (Denial) | worlds_completed missing from save | mitigate | Key added in Phase 3, migrated in Plan 01 v3 upgrade; defensive .get() calls with fallback [] handle missing key |

**Rationale:** Boss sequence is single-player, offline. Item gating is purely cosmetic (player cannot win without effort). Quality bar is transparent (player sees choices → quality adjustments). World completion tracking is transient (not exploitable). No economic or security implications.
</threat_model>

<verification>
**Phase 4, Plan 05 Completion Checklist:**

1. Boss scene and logic
   - [ ] boss_tfg.tscn has quality bar HUD (ColorRect, percentage label, title)
   - [ ] boss_tfg.gd gates on ≥3 items, blocks entry with dialogue if <3
   - [ ] Quality bar starts at 0%, rises on item presentation (+20% each)
   - [ ] Dialogue choices trigger signals (choice_correct +10%, choice_wrong -15%)
   - [ ] Professor escalates requirement mid-fight (threshold rises +15%)
   - [ ] Victory at 100% unlocks Sketch and Amor, saves mundo2 completion to worlds_completed
   - [ ] Defeat at <70% reloads boss_tfg.tscn (retry with items intact)

2. Opening and closing cutscenes
   - [ ] mundo2_abertura.tscn exists with skip button, auto-transitions to fase1
   - [ ] mundo2_abertura.gd handles Dialogic timeline and skip logic
   - [ ] world2_end.tscn displays victory message (placeholder OK)
   - [ ] world2_end.gd returns to main_menu on button press

3. Dialogic content
   - [ ] mundo2_abertura.dtl has opening narrative (tone: exaustão + esperança)
   - [ ] boss_abertura_tfg.dtl has dialogue choices with signal events
   - [ ] boss_vitoria_tfg.dtl has victory narrative
   - [ ] ProfessorPerpetuo.dch defined with character name and display name

4. Retroactive unlock
   - [ ] boss_pai.gd modified to unlock_power("amor") on Mundo 1 victory
   - [ ] Amor retroactively available after Phase 3 completion

5. Full integration
   - [ ] All prior plans (01-04) integrated: powers work, HP system active, items persist, Sketch fires
   - [ ] No circular dependencies, no missing method calls
   - [ ] Save/load cycle preserves powers_unlocked, itens_tfg_mundo2, and worlds_completed

**Manual Playtest (checkpoint:human-verify, Task 4):**
- Vertical slice playthrough (abertura → 3 fases → boss → world2_end) completes without crash
- Items persist across phases and death
- Boss gate blocks <3 items, allows ≥3
- Quality bar displays and updates correctly
- Powers unlock on victory and persist across save/load
- worlds_completed saves "mundo2" and loads correctly
</verification>

<success_criteria>
**Plan 05 Success:**

1. Boss fight is gateable: Player cannot enter boss without ≥3 TFG items.
2. Quality bar mechanics work: Starts at 0%, rises on correct dialogue (+10%) and item presentation (+20%), descends on wrong answers (-15%).
3. Professor escalation works: Threshold rises during banca, preventing easy wins, but capped at QUALITY_MAX.
4. Victory path: At 100% quality, victory dialogue plays, Sketch and Amor powers unlock, Mundo 2 marked complete in worlds_completed[], save persists.
5. Defeat path: At <70% quality, defeat dialogue plays, boss_tfg reloads (items retained, player retries).
6. Opening cutscene: mundo2_abertura displays narrative and transitions to fase1_campus.
7. End screen: world2_end shows victory message and returns to menu on button press.
8. Retroactive unlock: Amor power available after Mundo 1 boss defeat (via boss_pai.gd modification).
9. Full integration: All systems from Plans 01-04 work together: powers fire, HP decreases, items save, boss gate validates, world completion tracked.

**This plan unblocks:**
- Plan 06 (Opening narrative + HUD + final integration) — depends on all vertical slices complete
- Full Phase 4 completion and readiness for Phase 5 (Mundo 3)
</success_criteria>

<output>
After completion, create `.planning/phases/04-mundo-2-a-faculdade/04-05-SUMMARY.md` documenting:
- Boss TFG scene and quality bar FSM
- Gate logic (≥3 items required)
- Quality mechanics (escalation, victory/defeat)
- Power unlock on victory (Sketch + Amor)
- World completion tracking (worlds_completed update)
- Mundo 2 opening and closing cutscenes
- Dialogic timelines and character definitions
- Retroactive Amor unlock in boss_pai.gd
- Full Mundo 2 vertical slice integration
</output>
